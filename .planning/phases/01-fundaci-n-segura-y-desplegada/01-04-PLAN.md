---
phase: 01-fundaci-n-segura-y-desplegada
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - fava-control-tecnico/docs/ENTRA-SETUP.md
  - fava-control-tecnico/docs/ENV.md
  - fava-control-tecnico/backend/.env.example
  - fava-control-tecnico/frontend/.env.example
autonomous: false
requirements: [AUTH-01]
user_setup:
  - service: microsoft-entra-id
    why: "Registrar las dos apps (SPA + API) en el tenant dev — requiere credenciales de admin del tenant"
    env_vars:
      - name: ENTRA_TENANT_ID
        source: "Entra admin center → Overview → Tenant ID"
      - name: ENTRA_API_CLIENT_ID
        source: "App registration 'FAVA Control Tecnico API' → Application (client) ID"
      - name: VITE_ENTRA_SPA_CLIENT_ID
        source: "App registration 'FAVA Control Tecnico SPA' → Application (client) ID"
      - name: VITE_API_SCOPE
        source: "API registration → Expose an API → api://<api-client-id>/access_as_user"
    dashboard_config:
      - task: "Crear los 2 app registrations siguiendo docs/ENTRA-SETUP.md"
        location: "entra.microsoft.com → App registrations"
must_haves:
  truths:
    - "Existen dos app registrations en el tenant dev: SPA (redirect URI a /redirect.html) y API (scope access_as_user expuesto, SPA autorizada)"
    - "El manifiesto de la API tiene requestedAccessTokenVersion: 2 y un token real emitido muestra ver 2.0"
    - "docs/ENTRA-SETUP.md permite recrear ambos registros desde cero sin arqueología (restricción de migración de cuenta)"
    - "docs/ENV.md lista todas las variables con su origen y cuáles son secretas; los .env.example las reflejan"
  artifacts:
    - path: "fava-control-tecnico/docs/ENTRA-SETUP.md"
      provides: "receta paso a paso de los 2 registros + variante de 1 registro como fallback"
      contains: "requestedAccessTokenVersion"
    - path: "fava-control-tecnico/docs/ENV.md"
      provides: "variables de backend y frontend con origen y sensibilidad"
      contains: "ENTRA_TENANT_ID"
    - path: "fava-control-tecnico/backend/.env.example"
      provides: "plantilla de las 8 variables del backend"
      contains: "ENTRA_API_CLIENT_ID"
    - path: "fava-control-tecnico/frontend/.env.example"
      provides: "plantilla VITE_ del frontend"
      contains: "VITE_API_SCOPE"
  key_links:
    - from: "fava-control-tecnico/docs/ENTRA-SETUP.md"
      to: "frontend/redirect.html"
      via: "redirect URIs exactos: http://localhost:5173/redirect.html y https://<dominio>/redirect.html"
      pattern: "redirect\\.html"
---

<objective>
Resolver la pregunta abierta 2 del research: DOS app registrations en Entra (SPA + API con scope expuesto), documentadas de forma reproducible, con la variante de un solo registro como fallback de env vars. El usuario ejecuta el registro en su tenant dev (única acción que exige sus credenciales de admin del tenant) y entrega los 4 valores.

Purpose: AUTH-01 — sin registros no hay login; sin doc reproducible, la migración de cuenta personal→empresa es arqueología (Pitfall 8).
Output: 2 docs + 2 .env.example + los 4 valores reales cargados en los .env locales.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundaci-n-segura-y-desplegada/01-RESEARCH.md

Nota de repo: los commits NO llevan Co-Authored-By ni atribución a Claude.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir ENTRA-SETUP.md, ENV.md y los .env.example</name>
  <files>fava-control-tecnico/docs/ENTRA-SETUP.md, fava-control-tecnico/docs/ENV.md, fava-control-tecnico/backend/.env.example, fava-control-tecnico/frontend/.env.example</files>
  <action>
1. `docs/ENTRA-SETUP.md` — receta paso a paso (fuente: research § Entra ID + Open Questions 2 y 5):
   **Registro A — "FAVA Control Tecnico API":**
   - App registration nuevo, single tenant.
   - Expose an API → Application ID URI `api://<client-id>` → scope `access_as_user` (admins and users).
   - Manage → Manifest: dentro del bloque `"api"` poner `"requestedAccessTokenVersion": 2` (el nombre del campo varía según el formato del manifiesto — buscarlo dentro de `api`). Sin esto el token sale v1 y el guard rechaza con "invalid signature" fantasma.
   **Registro B — "FAVA Control Tecnico SPA":**
   - App registration nuevo, single tenant, plataforma **Single-page application**.
   - Redirect URI: `http://localhost:5173/redirect.html` (dev). El de Railway (`https://<dominio>.up.railway.app/redirect.html`) se añade durante el deploy (Plan 06). NOTA: apuntan al PUENTE `/redirect.html`, no a la home (MSAL v5 / COOP).
   - API permissions → añadir el scope `access_as_user` del Registro A → Grant admin consent.
   **De vuelta en el Registro A:** Expose an API → Authorized client applications → añadir el client id del SPA (suprime la pantalla de consentimiento).
   **Verificación (2 minutos que ahorran un día de debug):** obtener un access token de prueba y comprobar: `ver: "2.0"`, `aud` = client id del API. Si `aud` es `00000003-0000-0000-c000-000000000000`, es un token de Graph (invalidable por diseño) — revisar que solo se pide `api://<api-id>/access_as_user`, nunca User.Read mezclado.
   **Variante fallback (un solo registro):** si IT de FAVA objeta dos registros en el tenant real: un registro con plataforma SPA + Expose an API en el mismo. El cambio es solo env: `ENTRA_API_CLIENT_ID = <mismo client id>` y `VITE_API_SCOPE = api://<mismo-id>/access_as_user`. Cero código.
   **Tenant swap dev→FAVA:** repetir la receta en el tenant FAVA y cambiar las variables — nada más (AUTH-01).
2. `docs/ENV.md`: tabla con TODAS las variables, origen y sensibilidad:
   - Backend: DATABASE_URL (secreta), MIGRATE_DATABASE_URL (secreta), APP_DB_PASSWORD (secreta), ENTRA_TENANT_ID, ENTRA_API_CLIENT_ID, ENTRA_REQUIRED_SCOPE (=access_as_user), SEED_SUPERADMIN_EMAIL, PORT.
   - Frontend (build-time): VITE_ENTRA_TENANT_ID, VITE_ENTRA_SPA_CLIENT_ID, VITE_API_SCOPE.
   Regla escrita en el doc: ninguna variable existe SOLO en el dashboard de Railway — todas están listadas aquí (Pitfall 8).
3. `.env.example` en backend y frontend con esas claves y placeholders comentados. Verificar que `.env` está en el .gitignore de ambos paquetes (añadirlo si falta).
  </action>
  <verify>
    <automated>grep -q requestedAccessTokenVersion fava-control-tecnico/docs/ENTRA-SETUP.md && grep -q ENTRA_TENANT_ID fava-control-tecnico/docs/ENV.md && grep -q VITE_API_SCOPE fava-control-tecnico/frontend/.env.example && grep -q ENTRA_API_CLIENT_ID fava-control-tecnico/backend/.env.example</automated>
  </verify>
  <done>Ambos docs completos y reproducibles; .env.example en los dos paquetes; .env gitignored.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Crear los registros en el tenant dev y entregar los 4 valores</name>
  <what-built>docs/ENTRA-SETUP.md con la receta exacta de los dos app registrations y docs/ENV.md con la lista de variables.</what-built>
  <files>fava-control-tecnico/backend/.env, fava-control-tecnico/frontend/.env</files>
  <action>
1. Abrir https://entra.microsoft.com con la cuenta del tenant dev.
2. Seguir docs/ENTRA-SETUP.md de arriba a abajo (Registro A: API con scope + manifiesto v2; Registro B: SPA con redirect http://localhost:5173/redirect.html; autorizar el SPA en el API; admin consent).
3. Pegar aquí los 4 valores:
   - ENTRA_TENANT_ID
   - ENTRA_API_CLIENT_ID
   - VITE_ENTRA_SPA_CLIENT_ID (client id del registro SPA)
   - VITE_API_SCOPE (api://<api-client-id>/access_as_user)
4. Indicar el email de la cuenta dev del tenant (será SEED_SUPERADMIN_EMAIL).
Al recibirlos, Claude los escribe en backend/.env y frontend/.env locales (gitignored) y confirma que coinciden con los formatos esperados (UUIDs, scope api://).
Nota: la verificación de `ver: "2.0"` del token emitido se completa en el primer login real (Plan 06) — si el login falla con "invalid signature", el primer sospechoso es el manifiesto.
  </action>
  <verify>Los 4 valores tienen el formato esperado (2 UUIDs de client id, 1 UUID de tenant, scope api://<uuid>/access_as_user) y quedaron escritos en backend/.env y frontend/.env locales (gitignored, nunca commiteados).</verify>
  <done>Registros A y B existen en el tenant dev conforme a ENTRA-SETUP.md; los .env locales tienen los valores reales; SEED_SUPERADMIN_EMAIL definido.</done>
  <resume-signal>Pegar los 4 valores + email de la cuenta dev, o describir el problema encontrado</resume-signal>
</task>

</tasks>

<verification>
- ENTRA-SETUP.md contiene: los 2 registros, requestedAccessTokenVersion: 2, redirect URIs a /redirect.html, la variante de 1 registro, y el procedimiento de tenant swap.
- Los 4 valores reales están en los .env locales (no en el repo).
</verification>

<success_criteria>
- AUTH-01 (infraestructura de identidad): registros creados y documentados de forma reproducible; el swap dev→FAVA es solo repetir la receta y cambiar env vars.
- Plans 05 y 06 tienen los valores que necesitan para el login real.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-segura-y-desplegada/01-04-SUMMARY.md`
</output>
