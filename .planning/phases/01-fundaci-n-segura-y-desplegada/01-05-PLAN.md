---
phase: 01-fundaci-n-segura-y-desplegada
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - fava-control-tecnico/frontend/redirect.html
  - fava-control-tecnico/frontend/vite.config.ts
  - fava-control-tecnico/frontend/package.json
  - fava-control-tecnico/frontend/src/lib/auth/msal.ts
  - fava-control-tecnico/frontend/src/lib/api/client.ts
  - fava-control-tecnico/frontend/src/state.tsx
  - fava-control-tecnico/frontend/src/App.tsx
  - fava-control-tecnico/frontend/src/Login.tsx
  - fava-control-tecnico/frontend/src/Layout.tsx
  - fava-control-tecnico/frontend/src/screens/NoAccess.tsx
  - fava-control-tecnico/frontend/src/screens/Users.tsx
  - fava-control-tecnico/frontend/src/i18n.ts
  - fava-control-tecnico/frontend/src/vite-env.d.ts
autonomous: true
requirements: [AUTH-01, INFRA-03]
must_haves:
  truths:
    - "El botón «Iniciar sesión con Microsoft» de Login.tsx dispara loginRedirect real de MSAL (la pantalla se conserva visualmente)"
    - "Tras el login, state.tsx carga GET /api/me y Layout muestra nombre y roles reales — el usuario mock de sesión salió del flujo"
    - "Cuenta MS válida no invitada → pantalla «sin acceso» con nombre de la cuenta, botón salir y botón «solicitar acceso» que crea la solicitud"
    - "Cuenta desactivada → mensaje propio «tu cuenta fue desactivada», distinto del de no-invitado"
    - "El switcher T·A·S solo aparece para usuarios con más de un rol y limitado a SUS roles"
    - "La pantalla Usuarios muestra la lista/badge de solicitudes de acceso pendientes desde el API"
  artifacts:
    - path: "fava-control-tecnico/frontend/redirect.html"
      provides: "puente de redirección MSAL v5 (COOP)"
      contains: "broadcastResponseToMainFrame"
    - path: "fava-control-tecnico/frontend/src/lib/auth/msal.ts"
      provides: "PublicClientApplication + initAuth/login/logout/getToken"
      contains: "acquireTokenSilent"
    - path: "fava-control-tecnico/frontend/src/lib/api/client.ts"
      provides: "apiFetch tipado con Bearer + tipos MeResponse/AccessRequest a mano"
      contains: "MeResponse"
    - path: "fava-control-tecnico/frontend/src/screens/NoAccess.tsx"
      provides: "pantallas sin-acceso y desactivado (decisiones locked del CONTEXT)"
      min_lines: 30
  key_links:
    - from: "fava-control-tecnico/frontend/src/state.tsx"
      to: "/api/me"
      via: "apiFetch al montar con cuenta activa → sessionStatus"
      pattern: "api/me"
    - from: "fava-control-tecnico/frontend/src/lib/api/client.ts"
      to: "lib/auth/msal.ts"
      via: "getToken() adjunta el Bearer en cada llamada"
      pattern: "getToken"
    - from: "fava-control-tecnico/frontend/vite.config.ts"
      to: "redirect.html"
      via: "rollupOptions.input.redirect (segunda entrada del build)"
      pattern: "redirect"
---

<objective>
Cablear el frontend existente a la identidad real: MSAL Browser v5 directo (SIN @azure/msal-react — no soporta React 18), puente redirect.html, cliente API tipado, y las tres pantallas de sesión decididas en el CONTEXT (ok / sin acceso / desactivada). Solo se toca lo de sesión — el resto de mocks de data.ts sigue vivo hasta Fase 2+.

Purpose: AUTH-01 (login real) + INFRA-03 (cliente tipado + MSAL, mocks retirados pantalla por pantalla).
Output: frontend compilable con login Microsoft real, listo para verificarse desplegado en Plan 06.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundaci-n-segura-y-desplegada/01-CONTEXT.md
@.planning/phases/01-fundaci-n-segura-y-desplegada/01-RESEARCH.md
@fava-control-tecnico/frontend/src/state.tsx
@fava-control-tecnico/frontend/src/App.tsx
@fava-control-tecnico/frontend/src/Login.tsx
@fava-control-tecnico/frontend/src/Layout.tsx
@fava-control-tecnico/frontend/src/types.ts
@fava-control-tecnico/frontend/src/i18n.ts

Nota de repo: los commits NO llevan Co-Authored-By ni atribución a Claude.
</context>

<interfaces>
<!-- Contrato del backend (Plan 03 lo implementa idéntico — NO cambiar nombres): -->
```ts
type Role = 'T' | 'A' | 'S';
type MeResponse =
  | { status: 'ok'; user: { id: string; displayName: string; email: string; roles: Role[]; technicianId: string | null } }
  | { status: 'not_invited'; entra: { displayName: string; email: string }; requestPending: boolean }
  | { status: 'deactivated'; entra: { displayName: string; email: string } };

// POST /api/access-requests → 201 { id, status: 'pending' } (sin body)
// GET  /api/access-requests → { id, email, displayName, status, createdAt }[]  (roles A,S)
// PATCH /api/access-requests/:id  body { status: 'dismissed' }
// 401 = token inválido/ausente; 403 = sin permiso
```

<!-- Del código existente (leído — no explorar):
state.tsx: AppProvider con login()/logout()/switchRole(role); state.loggedIn, state.role,
  state.route; App.tsx renderiza Login | loading | Layout según loggedIn/loading.
Login.tsx: botón MS llama ctx.login() — se conserva visual, solo cambia el handler.
types.ts: Role = 'T'|'A'|'S' ya definido.
Estilos: inline + variables CSS .fava — NO instalar UI kits ni Tailwind. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: MSAL Browser v5 + puente de redirección + entrada Vite</name>
  <files>fava-control-tecnico/frontend/package.json, fava-control-tecnico/frontend/redirect.html, fava-control-tecnico/frontend/vite.config.ts, fava-control-tecnico/frontend/src/lib/auth/msal.ts, fava-control-tecnico/frontend/src/vite-env.d.ts</files>
  <action>
1. `npm install @azure/msal-browser@5.17.1` en frontend. Pinear `"typescript": "5.9.3"` SIN caret (npm latest es TS 7 Go — un install limpio en otra máquina rompería el build). NO instalar @azure/msal-react (exige React ≥19.2.1; el proyecto es React 18.3 — Pitfall 1 del research).
2. `redirect.html` JUNTO a index.html (no en src/), Code Example 2 del research literal: `<script type="module">` que importa `broadcastResponseToMainFrame` de `@azure/msal-browser/redirect-bridge` y lo invoca con .catch.
3. `vite.config.ts`: añadir `build.rollupOptions.input = { main: resolve(__dirname,'index.html'), redirect: resolve(__dirname,'redirect.html') }`. Tras el build, verificar en dist/ que redirect.html NO conserva script inline (Rollup lo convierte en chunk con src — relevante para la CSP).
4. `src/lib/auth/msal.ts` — Code Example 1 del research literal: PublicClientApplication con clientId `VITE_ENTRA_SPA_CLIENT_ID`, authority con `VITE_ENTRA_TENANT_ID`, `redirectUri: ${window.location.origin}/redirect.html`, cache sessionStorage. Exports: `initAuth()` (initialize + handleRedirectPromise + setActiveAccount → AccountInfo|null), `login()` (loginRedirect con scopes [VITE_API_SCOPE]), `logout()`, `getToken()` (acquireTokenSilent; en interaction_required/login_required → acquireTokenRedirect; SIEMPRE ramificar por errorCode, no por message — en v5 message es un enlace).
   CRÍTICO: scopes SIEMPRE [VITE_API_SCOPE] solo — nunca User.Read ni scopes de Graph mezclados (un token sirve para UN recurso; los de Graph no son validables por el backend).
5. `vite-env.d.ts`: declarar los tipos de las 3 VITE_ vars en ImportMetaEnv.
  </action>
  <verify>
    <automated>cd fava-control-tecnico/frontend && npm run build && grep -rq redirect dist/ --include=*.html</automated>
  </verify>
  <done>Build emite index.html Y redirect.html; msal.ts exporta initAuth/login/logout/getToken; msal-react NO está en package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Cliente API tipado y sesión real en state.tsx</name>
  <files>fava-control-tecnico/frontend/src/lib/api/client.ts, fava-control-tecnico/frontend/src/state.tsx, fava-control-tecnico/frontend/src/App.tsx</files>
  <action>
1. `src/lib/api/client.ts`: tipos a mano del bloque interfaces (MeResponse, AccessRequest, ApiError) — sin codegen, son 4 interfaces (decisión research; codegen llega en Fase 2). `apiFetch<T>(path, init?)`: adjunta `Authorization: Bearer ${await getToken()}`, `content-type: application/json`, base `/api` relativa (mismo origen — servicio único), lanza `ApiError { status, message }` en !ok. Helper `getMe(): Promise<MeResponse>` y `requestAccess(): Promise<void>`.
2. `state.tsx` — cirugía mínima, conservar TODO lo demás (mocks de notes/projects/etc. siguen vivos):
   - Nuevo estado: `sessionStatus: 'boot' | 'anon' | 'ok' | 'not_invited' | 'deactivated'`, `me: MeResponse | null`, `myRoles: Role[]`.
   - `useEffect` al montar el provider: `initAuth()`; sin cuenta → sessionStatus 'anon'; con cuenta → `getMe()` y mapear el status de la unión; en status 'ok' además `loggedIn: true`, `role: user.roles[0] (preferir 'S' > 'A' > 'T' si multi-rol)`, myRoles = user.roles.
   - `login()`: reemplazar el setTimeout fake por `import { login as msalLogin } from './lib/auth/msal'` → `msalLogin()` (redirect completo — el efecto de arriba retoma al volver). Conservar la lógica de onboarding (localStorage fava_onboard) en el camino de sessionStatus 'ok'.
   - `logout()`: `msalLogout()` + reset de sesión.
   - `switchRole(role)`: solo permitir si `myRoles.includes(role)` — el switcher deja de ser un toggle de demo. NOTA en comentario: cambiar de rol cambia navegación y filtros, NUNCA permisos (los permisos son del servidor — Pattern 4 del research).
   - En un 401 de apiFetch tras sesión ok → volver a 'anon' (token muerto).
3. `App.tsx`: el árbol de render pasa a decidir por sessionStatus: 'boot' → spinner existente; 'anon' → Login; 'not_invited'/'deactivated' → NoAccess (Task 3); 'ok' → Layout. Mantener el div.fava wrapper con theme/density.
  </action>
  <verify>
    <automated>cd fava-control-tecnico/frontend && npm run build && grep -q "api/me" src/state.tsx && grep -q "sessionStatus" src/App.tsx</automated>
  </verify>
  <done>state.tsx sin login fake; sesión real derivada de /api/me; switcher limitado a myRoles; build verde.</done>
</task>

<task type="auto">
  <name>Task 3: Pantallas sin-acceso/desactivada, Layout con usuario real y solicitudes en Usuarios</name>
  <files>fava-control-tecnico/frontend/src/screens/NoAccess.tsx, fava-control-tecnico/frontend/src/Login.tsx, fava-control-tecnico/frontend/src/Layout.tsx, fava-control-tecnico/frontend/src/screens/Users.tsx, fava-control-tecnico/frontend/src/i18n.ts</files>
  <action>
1. `screens/NoAccess.tsx` (decisiones LOCKED del CONTEXT — implementar exactamente):
   - Variante `not_invited`: muestra displayName/email de la cuenta MS, texto «tu cuenta no está habilitada», botón «solicitar acceso» (llama requestAccess(); si me.requestPending o tras éxito → estado «solicitud enviada», botón deshabilitado) y botón «salir» (logout). NO auto-registro.
   - Variante `deactivated`: mensaje específico «tu cuenta fue desactivada» + botón salir. DISTINTO del de no-invitado (app interna: claridad sobre opacidad).
   - Estilos inline + variables CSS .fava, mismo lenguaje visual de Login.tsx. Strings en `i18n.ts` (es + it).
2. `Login.tsx`: el botón ya llama ctx.login() — no tocar el visual (decisión locked). Retirar/ajustar el bloque «demo» (login_demo) que ya no aplica: reemplazar por una nota corta de «acceso con cuenta corporativa» en i18n.
3. `Layout.tsx`: el bloque de usuario del header/sidebar muestra displayName y email reales de state.me; el switcher T·A·S se renderiza SOLO si myRoles.length > 1 y solo con los roles de myRoles (discreción aprobada en CONTEXT). Logout del menú llama al logout real.
4. `screens/Users.tsx`: añadir sección «Solicitudes de acceso» ARRIBA de la lista mock existente: fetch GET /api/access-requests al montar (solo si rol activo A/S), badge con el count pending en el título, cada fila con email/nombre/fecha y botón «descartar» (PATCH dismissed). La lista de usuarios mock NO se toca (su cutover real es Fase 2 con invitaciones). El feed de notificaciones es Fase 7 — NO construir nada de notificaciones aquí (deferred explícito).
5. `i18n.ts`: añadir las claves nuevas en es e it (no_access_*, deactivated_*, access_requests_*).
  </action>
  <verify>
    <automated>cd fava-control-tecnico/frontend && npm run build && grep -q "solicitar" src/i18n.ts && grep -q "access-requests" src/screens/Users.tsx</automated>
  </verify>
  <done>Tres pantallas de sesión conforme al CONTEXT; Layout con identidad real y switcher restringido; Usuarios lista solicitudes pendientes; build verde. La verificación funcional end-to-end (login real) queda para el checkpoint del Plan 06.</done>
</task>

</tasks>

<verification>
- `npm run build` en frontend verde con TS 5.9.3 pineado.
- dist/ contiene index.html y redirect.html sin scripts inline en redirect.
- Grep: `msal-react` ausente de package.json; `User.Read` ausente de src/; scopes = [VITE_API_SCOPE].
- state.tsx: cero setTimeout de login fake; sessionStatus gobierna App.tsx.
</verification>

<success_criteria>
- INFRA-03: cliente API tipado a mano + MSAL Browser directo; el mock de sesión salió de state.tsx (los demás mocks siguen — cutover pantalla por pantalla).
- AUTH-01 (lado cliente): loginRedirect real con el scope del API, puente COOP listo.
- Las 3 decisiones locked del CONTEXT (no-invitado con solicitar acceso, desactivado con mensaje propio, vinculación transparente al usuario) tienen su UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-segura-y-desplegada/01-05-SUMMARY.md`
</output>
