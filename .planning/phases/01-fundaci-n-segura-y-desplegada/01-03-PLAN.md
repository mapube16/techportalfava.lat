---
phase: 01-fundaci-n-segura-y-desplegada
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - fava-control-tecnico/backend/src/common/auth/jwks.provider.ts
  - fava-control-tecnico/backend/src/common/auth/entra.guard.ts
  - fava-control-tecnico/backend/src/common/auth/entra.guard.spec.ts
  - fava-control-tecnico/backend/src/common/auth/roles.guard.ts
  - fava-control-tecnico/backend/src/common/auth/roles.decorator.ts
  - fava-control-tecnico/backend/src/common/auth/allow-unprovisioned.decorator.ts
  - fava-control-tecnico/backend/src/common/auth/current-user.decorator.ts
  - fava-control-tecnico/backend/src/modules/me/me.module.ts
  - fava-control-tecnico/backend/src/modules/me/me.controller.ts
  - fava-control-tecnico/backend/src/modules/access-requests/access-requests.module.ts
  - fava-control-tecnico/backend/src/modules/access-requests/access-requests.controller.ts
  - fava-control-tecnico/backend/src/modules/access-requests/access-requests.service.ts
  - fava-control-tecnico/backend/src/modules/users/users.module.ts
  - fava-control-tecnico/backend/src/modules/users/users.controller.ts
  - fava-control-tecnico/backend/src/modules/users/users.service.ts
  - fava-control-tecnico/backend/src/app.module.ts
  - fava-control-tecnico/backend/test/helpers/tokens.ts
  - fava-control-tecnico/backend/test/auth.e2e-spec.ts
  - fava-control-tecnico/backend/test/tenant-swap.e2e-spec.ts
  - fava-control-tecnico/backend/test/users-roles.e2e-spec.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04]
must_haves:
  truths:
    - "Un token válido del tenant con el scope correcto resuelve al usuario de BD y GET /api/me devuelve status ok con nombre y roles"
    - "Token expirado, aud ajeno, tid ajeno o sin scp → 401/403 (4 casos unit)"
    - "Desactivar a un usuario corta el acceso en su siguiente petición con el MISMO token — sin cache"
    - "Un Admin que asigna el rol Admin recibe 403; un Super Admin lo consigue"
    - "Cuenta MS válida pero no invitada → me responde not_invited y puede crear una solicitud de acceso; cuenta desactivada → me responde deactivated"
    - "Cambiar de tenant es solo env: la misma suite pasa con dos ENTRA_TENANT_ID distintos"
  artifacts:
    - path: "fava-control-tecnico/backend/src/common/auth/entra.guard.ts"
      provides: "jwtVerify jose + lookup por oid sin cache + vinculación atómica por email"
      contains: "jwtVerify"
    - path: "fava-control-tecnico/backend/src/modules/me/me.controller.ts"
      provides: "GET /api/me — unión discriminada ok|not_invited|deactivated"
      contains: "not_invited"
    - path: "fava-control-tecnico/backend/src/modules/users/users.service.ts"
      provides: "regla de escalada en un solo sitio + anti-lockout"
      contains: "SOLO_SUPER_ADMIN_ASIGNA_ADMIN"
    - path: "fava-control-tecnico/backend/test/helpers/tokens.ts"
      provides: "firma de tokens de prueba con createLocalJWKSet — sin red, sin tenant"
      contains: "generateKeyPair"
  key_links:
    - from: "fava-control-tecnico/backend/src/common/auth/entra.guard.ts"
      to: "users (BD)"
      via: "findUnique por entraOid en CADA petición (AUTH-04: sin cache)"
      pattern: "entraOid"
    - from: "fava-control-tecnico/backend/src/common/auth/jwks.provider.ts"
      to: "tests"
      via: "token de inyección JWKS — los tests lo sustituyen por createLocalJWKSet"
      pattern: "JWKS_RESOLVER|provide"
    - from: "fava-control-tecnico/backend/src/app.module.ts"
      to: "EntraGuard"
      via: "APP_GUARD global (todo protegido por defecto; opt-out explícito)"
      pattern: "APP_GUARD"
---

<objective>
Construir la cadena de identidad completa del backend: validación jose del token de Entra, lookup en BD sin cache, vinculación email→OID atómica, /api/me como unión discriminada, solicitudes de acceso, y el módulo users con la regla de escalada de roles y anti-lockout. Con tests que firman sus propios tokens — sin red y sin tenant.

Purpose: AUTH-01 (validación del token, swap de tenant solo por env), AUTH-02 (solo Super Admin asigna Admin), AUTH-04 (desactivado = fuera en la siguiente petición).
Output: guard global + 3 módulos + 4 suites (1 unit + 3 e2e) verdes.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundaci-n-segura-y-desplegada/01-CONTEXT.md
@.planning/phases/01-fundaci-n-segura-y-desplegada/01-RESEARCH.md
@fava-control-tecnico/backend/src/common/prisma/prisma.service.ts
@fava-control-tecnico/backend/test/helpers/db.ts

Nota de repo: los commits NO llevan Co-Authored-By ni atribución a Claude.
</context>

<interfaces>
<!-- Del Plan 01 (existen): PrismaService.base (cliente sin tx — el guard SIEMPRE usa base,
     corre antes del interceptor RLS), helpers/db.ts (ownerClient, truncateAll, TEC_A/TEC_B),
     env.ts con ENTRA_TENANT_ID / ENTRA_API_CLIENT_ID / ENTRA_REQUIRED_SCOPE. -->

<!-- Contrato que el frontend (Plan 05) consume — NO cambiar los nombres: -->
```ts
type Role = 'T' | 'A' | 'S';
type MeResponse =
  | { status: 'ok'; user: { id: string; displayName: string; email: string; roles: Role[]; technicianId: string | null } }
  | { status: 'not_invited'; entra: { displayName: string; email: string }; requestPending: boolean }
  | { status: 'deactivated'; entra: { displayName: string; email: string } };

// POST /api/access-requests  → 201 { id, status: 'pending' }  (sin body: identidad sale del token)
// GET  /api/access-requests  → AccessRequest[] { id, email, displayName, status, createdAt }  (roles A,S)
// PATCH /api/access-requests/:id  body { status: 'dismissed' }  (roles A,S)
// PATCH /api/users/:id/roles  body { roles: Role[] }  (roles A,S — regla de escalada en servicio)
// PATCH /api/users/:id/active body { isActive: boolean }  (roles A,S)
// Errores: { statusCode, error, message } estándar Nest; 401 token inválido, 403 sin permiso/no aprovisionado
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard de Entra con JWKS inyectable, decoradores y vinculación email→OID</name>
  <files>fava-control-tecnico/backend/src/common/auth/jwks.provider.ts, fava-control-tecnico/backend/src/common/auth/entra.guard.ts, fava-control-tecnico/backend/src/common/auth/entra.guard.spec.ts, fava-control-tecnico/backend/src/common/auth/roles.guard.ts, fava-control-tecnico/backend/src/common/auth/roles.decorator.ts, fava-control-tecnico/backend/src/common/auth/allow-unprovisioned.decorator.ts, fava-control-tecnico/backend/src/common/auth/current-user.decorator.ts, fava-control-tecnico/backend/test/helpers/tokens.ts, fava-control-tecnico/backend/src/app.module.ts</files>
  <behavior>
    - Token expirado → 401; aud ajeno → 401; tid ajeno → 401; sin ENTRA_REQUIRED_SCOPE en scp → 403; válido → req.entra y req.user poblados
    - oid desconocido con email que coincide con un user sin entraOid → vinculación atómica (updateMany where email+entraOid null, count===1) y la petición continúa como ese user
    - oid desconocido sin match → req.user null → 403 salvo @AllowUnprovisioned
    - user con isActive=false → req.user null (ruta protegida 403); @AllowUnprovisioned deja pasar con req.entra para que /api/me distinga deactivated
  </behavior>
  <action>
1. `test/helpers/tokens.ts` PRIMERO (los tests lo usan todo): `generateKeyPair('RS256')` de jose, `signTestToken({ oid, email, name, tid?, aud?, scp?, exp? })` con defaults válidos (iss `https://login.microsoftonline.com/${tid}/v2.0`), export `localJwks = createLocalJWKSet(publicJwk)`.
2. `jwks.provider.ts`: token de DI `JWKS = 'JWKS_RESOLVER'`, factory con `createRemoteJWKSet(new URL('https://login.microsoftonline.com/${tenantId}/discovery/v2.0/keys'), { cacheMaxAge: 600_000, cooldownDuration: 30_000, timeoutDuration: 5_000 })`. Separado A PROPÓSITO: los tests hacen overrideProvider(JWKS) → localJwks.
3. `entra.guard.ts` según Code Example 3 del research: extraer Bearer; `jwtVerify(token, this.jwks, { issuer, audience: ENTRA_API_CLIENT_ID, clockTolerance: 60 })`; verificar `payload.tid === ENTRA_TENANT_ID`; verificar `ENTRA_REQUIRED_SCOPE` en `payload.scp` split por espacio. Lookup `this.prisma.base.user.findUnique({ where: { entraOid: payload.oid } })` — SIN CACHE (AUTH-04: la desactivación corta en la siguiente petición; a ~50 usuarios un SELECT con índice único es ruido frente al coste de la verificación criptográfica que ya ocurre).
   Si no hay user por oid → intento de vinculación (Pitfall 6): email normalizado (lowercase+trim) del claim `email` (NUNCA preferred_username — mutable/reasignable); `updateMany({ where: { email, entraOid: null, isActive: true }, data: { entraOid: oid } })`; si count===1 re-fetch por oid; si 0, sin vínculo. Una vez escrito entra_oid, el email nunca vuelve a autenticar.
   `req.entra = { oid, email, name }` siempre; `req.user = user?.isActive ? user : null`. Si `!req.user` y el handler no tiene @AllowUnprovisioned → ForbiddenException.
4. `roles.decorator.ts` (@Roles('A','S') via SetMetadata) + `roles.guard.ts` (lee metadata, compara con req.user.roles — los roles REALES de BD, jamás el rol activo del switcher del cliente, que es estado de UI no confiable). `allow-unprovisioned.decorator.ts` y `current-user.decorator.ts` (param decorator → req.user).
5. `app.module.ts`: registrar EntraGuard y RolesGuard como APP_GUARD globales (orden: Entra primero). /health queda fuera con un decorador @Public o comprobando la ruta — elegir @Public (SetMetadata) y marcarlo en HealthController.
6. `entra.guard.spec.ts` (unit, sin BD real — mock de PrismaService.base): los 4 casos de rechazo + el caso válido + los 2 casos de vinculación. Comando rápido: `npm -w backend run test -- entra.guard`.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test -- entra.guard</automated>
  </verify>
  <done>5+ tests unit verdes; guard global registrado; tokens de prueba firmables sin red; vinculación atómica con updateMany count===1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/me (unión discriminada) + solicitudes de acceso + e2e de auth y tenant-swap</name>
  <files>fava-control-tecnico/backend/src/modules/me/me.module.ts, fava-control-tecnico/backend/src/modules/me/me.controller.ts, fava-control-tecnico/backend/src/modules/access-requests/access-requests.module.ts, fava-control-tecnico/backend/src/modules/access-requests/access-requests.controller.ts, fava-control-tecnico/backend/src/modules/access-requests/access-requests.service.ts, fava-control-tecnico/backend/test/auth.e2e-spec.ts, fava-control-tecnico/backend/test/tenant-swap.e2e-spec.ts</files>
  <behavior>
    - GET /api/me con user activo → { status:'ok', user: {...roles de BD} }
    - GET /api/me con oid desconocido → { status:'not_invited', entra, requestPending } (requestPending refleja si ya hay solicitud pending por ese oid)
    - GET /api/me con user isActive=false → { status:'deactivated', entra }
    - POST /api/access-requests con oid desconocido → 201 pending; repetir → no duplica (upsert por entraOid); con user ya activo → 409
    - Cualquier otro endpoint con oid desconocido o inactivo → 403
    - AUTH-04: user activo hace GET ok → owner lo desactiva en BD → la MISMA suite con el MISMO token recibe 403/deactivated en la petición inmediatamente siguiente
    - tenant-swap: la app construida con ENTRA_TENANT_ID=T1 acepta tokens de T1 y rechaza los de T2; reconstruida con T2, al revés — cero constantes de tenant en el código
  </behavior>
  <action>
1. `me.controller.ts`: GET /api/me con @AllowUnprovisioned. Deriva la unión del estado que dejó el guard: req.user → ok (con roles/technicianId de BD); sin user pero BD tiene el oid inactivo → deactivated; resto → not_invited con `requestPending = existe access_request pending por entraOid`. Usar `this.prisma.base` (me corre sin contexto RLS — el interceptor sale temprano sin req.user, y con req.user la tx también sirve; base es lo consistente aquí).
2. `access-requests.*`: POST @AllowUnprovisioned + @Throttle(5/hora por IP) — es el ÚNICO endpoint escribible por cualquier miembro del tenant (research: Don't Hand-Roll rate limit). Identidad SIEMPRE del token (req.entra), nunca del body. Upsert por entraOid. GET (lista pending primero) y PATCH :id { status:'dismissed' } con @Roles('A','S'). Las solicitudes aterrizan en la pantalla Usuarios (CONTEXT) — el feed de notificaciones es Fase 7, NO construirlo.
3. `auth.e2e-spec.ts`: overrideProvider(JWKS)→localJwks, app real con supertest, BD del compose vía helpers. Cubrir los behaviors 1-6. El caso AUTH-04 es el criterio 3 del roadmap: mismo token, desactivación vía ownerClient, siguiente petición 403.
4. `tenant-swap.e2e-spec.ts`: crear DOS apps de test con env distinto (T1/T2) y cruzar tokens. Prueba que el swap dev→FAVA es solo variables (AUTH-01).
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- auth && npm -w backend run test:e2e -- tenant-swap</automated>
  </verify>
  <done>Unión discriminada exacta al contrato del bloque interfaces; solicitudes idempotentes y throttled; AUTH-04 probado con el mismo token; swap de tenant probado sin tocar código.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Módulo users — escalada de roles en un solo sitio + anti-lockout (criterio 4)</name>
  <files>fava-control-tecnico/backend/src/modules/users/users.module.ts, fava-control-tecnico/backend/src/modules/users/users.controller.ts, fava-control-tecnico/backend/src/modules/users/users.service.ts, fava-control-tecnico/backend/test/users-roles.e2e-spec.ts</files>
  <behavior>
    - Admin asigna rol T a otro user → 200
    - Admin asigna rol A → 403 SOLO_SUPER_ADMIN_ASIGNA_ADMIN (criterio 4 del roadmap)
    - Super Admin asigna rol A → 200
    - Super Admin intenta quitarse su propio rol S → 400 NO_PUEDES_QUITARTE_SUPER_ADMIN
    - Quitar el rol S al último Super Admin activo → 400 DEBE_QUEDAR_UN_SUPER_ADMIN
    - PATCH /api/users/:id/active { isActive:false } → 200; el user desactivado recibe 403 en su siguiente petición (enlaza con AUTH-04)
  </behavior>
  <action>
`users.service.ts` — la regla vive AQUÍ, no en decoradores por endpoint (Pattern 6 del research; un @Roles('S') impediría que un Admin asigne el rol T, que sí puede — matriz §6):
```ts
const escalates = next.some((r) => r === 'A' || r === 'S');
if (escalates && !actor.roles.includes('S'))
  throw new ForbiddenException('SOLO_SUPER_ADMIN_ASIGNA_ADMIN');
if (target.id === actor.id && actor.roles.includes('S') && !next.includes('S'))
  throw new BadRequestException('NO_PUEDES_QUITARTE_SUPER_ADMIN');
if (target.roles.includes('S') && !next.includes('S') && (await this.countActiveSuperAdmins()) === 1)
  throw new BadRequestException('DEBE_QUEDAR_UN_SUPER_ADMIN');
```
Controller: PATCH /api/users/:id/roles y /:id/active con @Roles('A','S') (la condición fina la pone el servicio). GET /api/users mínimo (id, displayName, email, roles, isActive) — Fase 2 lo expande, no adelantar campos. Usar `this.prisma.client` (getter ALS) en el servicio: cuando el interceptor del Plan 02 esté activo las escrituras irán en la tx de la petición sin cambiar este código.
`users-roles.e2e-spec.ts`: los 6 behaviors, sembrando admin/superadmin/técnico vía ownerClient y firmando tokens con tokens.ts.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- users-roles</automated>
  </verify>
  <done>Criterio 4 del roadmap probado (403 Admin→Admin, 200 SuperAdmin→Admin); 2 guards anti-lockout activos; desactivación por endpoint funcional.</done>
</task>

</tasks>

<verification>
- `npm -w backend run test -- entra.guard` + las 3 suites e2e (auth, tenant-swap, users-roles) verdes.
- Grep de seguridad: ningún `preferred_username` en src/ (el matching usa el claim email); ningún cache/TTL sobre el lookup de users; `audience` y `issuer` salen de env, no de constantes.
- Todo endpoint /api/* sin token → 401 (guard global, opt-out explícito solo /health y @AllowUnprovisioned).
</verification>

<success_criteria>
- AUTH-01: token validado con jose (firma+iss+aud+exp+tid+scp), tenant intercambiable solo por env — probado por tenant-swap.
- AUTH-02: solo Super Admin asigna Admin (403/200 probados) + anti-lockout.
- AUTH-04: desactivar corta en la siguiente petición con el mismo token — sin cache que invalidar. La ventana de 60-90 min de la baja EN EL DIRECTORIO queda documentada en el SUMMARY como limitación conocida (Pitfall 7 — CAE fuera de alcance), no como bug.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-segura-y-desplegada/01-03-SUMMARY.md`
</output>
