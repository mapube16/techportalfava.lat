---
phase: 01-fundaci-n-segura-y-desplegada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fava-control-tecnico/package.json
  - fava-control-tecnico/docker-compose.yml
  - fava-control-tecnico/backend/package.json
  - fava-control-tecnico/backend/tsconfig.json
  - fava-control-tecnico/backend/nest-cli.json
  - fava-control-tecnico/backend/src/main.ts
  - fava-control-tecnico/backend/src/app.module.ts
  - fava-control-tecnico/backend/src/config/env.ts
  - fava-control-tecnico/backend/src/common/health/health.module.ts
  - fava-control-tecnico/backend/src/common/prisma/prisma.service.ts
  - fava-control-tecnico/backend/src/common/prisma/rls.interceptor.ts
  - fava-control-tecnico/backend/prisma/schema.prisma
  - fava-control-tecnico/backend/prisma/prisma.config.ts
  - fava-control-tecnico/backend/prisma/seed.ts
  - fava-control-tecnico/backend/prisma/migrations/
  - fava-control-tecnico/backend/scripts/db-bootstrap.ts
  - fava-control-tecnico/backend/test/jest-e2e.json
  - fava-control-tecnico/backend/test/bootstrap.e2e-spec.ts
  - fava-control-tecnico/backend/test/helpers/db.ts
autonomous: true
requirements: [INFRA-01, INFRA-02]
must_haves:
  truths:
    - "npm run build en fava-control-tecnico/ compila frontend y backend con un solo comando"
    - "La app arranca con env completo y /health responde 200"
    - "Si falta una variable de entorno, el proceso falla al arrancar, no en la primera petición"
    - "db-bootstrap crea el rol fava_app (NOBYPASSRLS) y es idempotente: correrlo 2 veces = mismo estado"
    - "El esquema tiene users, access_requests, daily_entries y weekly_notes con las columnas source_* de Fase 6"
  artifacts:
    - path: "fava-control-tecnico/package.json"
      provides: "npm workspaces [backend, frontend] + scripts build/start"
      contains: "workspaces"
    - path: "fava-control-tecnico/backend/src/main.ts"
      provides: "helmet configurado para MSAL + ServeStatic + listen 0.0.0.0"
      contains: "login.microsoftonline.com"
    - path: "fava-control-tecnico/backend/prisma/schema.prisma"
      provides: "modelos User, AccessRequest, DailyEntry, WeeklyNote"
      contains: "model DailyEntry"
    - path: "fava-control-tecnico/backend/scripts/db-bootstrap.ts"
      provides: "creación idempotente de fava_app + ALTER DEFAULT PRIVILEGES"
      contains: "ALTER DEFAULT PRIVILEGES"
    - path: "fava-control-tecnico/backend/src/common/prisma/prisma.service.ts"
      provides: "PrismaClient con adapter PrismaPg + getter client (ALS tx ?? base)"
      contains: "AsyncLocalStorage"
  key_links:
    - from: "fava-control-tecnico/backend/src/config/env.ts"
      to: "arranque de la app"
      via: "zod parse que lanza si falta una variable"
      pattern: "safeParse|parse"
    - from: "fava-control-tecnico/backend/src/app.module.ts"
      to: "frontend/dist"
      via: "ServeStaticModule con exclude ['/api/{*path}']"
      pattern: "\\{\\*path\\}"
    - from: "fava-control-tecnico/backend/scripts/db-bootstrap.ts"
      to: "tablas futuras"
      via: "ALTER DEFAULT PRIVILEGES IN SCHEMA public"
      pattern: "DEFAULT PRIVILEGES"
---

<objective>
Crear el monorepo npm workspaces, el backend NestJS 11 con versiones pineadas, el esquema Prisma 7 (cjs) con las 4 tablas de la fase, y el bootstrap idempotente de los dos roles de Postgres. Todo lo que las demás plans necesitan como cimiento.

Purpose: INFRA-01 (NestJS modular + Prisma 7 + TS 5.9.x) e INFRA-02 (dos roles de Postgres reproducibles por script). Resuelve la pregunta abierta 4 del research: el spike RLS correrá sobre daily_entries/weekly_notes definitivas, no sobre tablas desechables.
Output: workspace compilable, Postgres local con dos roles, esquema migrado, test e2e de bootstrap en verde.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundaci-n-segura-y-desplegada/01-CONTEXT.md
@.planning/phases/01-fundaci-n-segura-y-desplegada/01-RESEARCH.md

Nota de repo: los commits NO llevan Co-Authored-By ni atribución a Claude.
</context>

<interfaces>
<!-- Contratos que las plans 02 y 03 consumirán. Crearlos EXACTAMENTE así. -->

PrismaService (backend/src/common/prisma/prisma.service.ts):
```ts
import { AsyncLocalStorage } from 'node:async_hooks';
export type TxClient = Omit<PrismaClient, '$connect' | '$disconnect' | '$on' | '$transaction' | '$extends'>;
export const als = new AsyncLocalStorage<TxClient>();

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit {
  // adapter: new PrismaPg({ connectionString: env.DATABASE_URL, max: 10, connectionTimeoutMillis: 5_000 })
  // super({ adapter, transactionOptions: { timeout: 10_000, maxWait: 5_000 } })
  get base(): PrismaClient { return this; }          // cliente sin tx (guard, lookups pre-RLS)
  get client(): TxClient { return als.getStore() ?? this; }  // tx del ALS si existe
}
```

Enum de roles (schema.prisma): `enum Role { T A S }` — coincide con `type Role = 'T'|'A'|'S'` del frontend.

Env vars validadas por zod (backend/src/config/env.ts) — exactamente estas 8:
- DATABASE_URL (runtime, rol fava_app)
- MIGRATE_DATABASE_URL (owner, migraciones y bootstrap)
- APP_DB_PASSWORD (password que el bootstrap asigna a fava_app)
- ENTRA_TENANT_ID
- ENTRA_API_CLIENT_ID
- ENTRA_REQUIRED_SCOPE (default: "access_as_user")
- SEED_SUPERADMIN_EMAIL
- PORT (default 3000)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Workspace raíz, docker-compose y scaffold NestJS con versiones pineadas</name>
  <files>fava-control-tecnico/package.json, fava-control-tecnico/docker-compose.yml, fava-control-tecnico/backend/package.json, fava-control-tecnico/backend/tsconfig.json, fava-control-tecnico/backend/nest-cli.json, fava-control-tecnico/backend/src/main.ts, fava-control-tecnico/backend/src/app.module.ts, fava-control-tecnico/backend/src/config/env.ts, fava-control-tecnico/backend/src/common/health/health.module.ts, fava-control-tecnico/backend/src/common/prisma/rls.interceptor.ts</files>
  <action>
1. Crear `fava-control-tecnico/package.json` raíz: `"private": true`, `"workspaces": ["backend", "frontend"]`, `"engines": { "node": "22.x" }`, scripts `build` (`npm -w frontend run build && npm -w backend run build`), `start` (`npm -w backend run start:prod`), `db:bootstrap` (`npm -w backend run db:bootstrap`). Añadir `.nvmrc` con `22`.
2. `docker-compose.yml`: postgres:17 (misma major que Railway hoy — verificar en Plan 06), puerto 5432, `POSTGRES_DB: fava`, `POSTGRES_PASSWORD` desde env con default dev, volumen nombrado. Servicio único `db`.
3. Scaffold del backend en `fava-control-tecnico/backend/` con `npx @nestjs/cli@11 new backend --package-manager npm --skip-git` (o estructura manual equivalente si el CLI da guerra). Después instalar exactamente el bloque Installation del RESEARCH: `@nestjs/core@11 @nestjs/common@11 @nestjs/platform-express@11 @nestjs/serve-static reflect-metadata rxjs @prisma/client@7.9.0 @prisma/adapter-pg@7.9.0 pg jose @nestjs/config zod helmet @nestjs/throttler @nestjs/terminus @nestjs/swagger nestjs-pino pino pino-http` y dev `prisma@7.9.0 @nestjs/cli@11 typescript@5.9.3 @types/node@22 @types/pg @types/express jest supertest @nestjs/testing`. CRÍTICO: `"typescript": "5.9.3"` SIN caret (npm latest es TS 7.x en Go, no compila decoradores). Node 22 en engines.
4. `src/config/env.ts`: esquema zod con las 8 variables del bloque interfaces. `parse()` en un módulo `EnvService` cargado en el boot — si falta una variable el proceso lanza y muere (requisito INFRA-01, criterio de Fase 8 gratis). ENTRA_REQUIRED_SCOPE default 'access_as_user', PORT default 3000 coerced number.
5. `src/main.ts`: helmet CONFIGURADO (no default) según Pitfall 2 del research:
```ts
const AAD = 'https://login.microsoftonline.com';
const base = helmet({ contentSecurityPolicy: { useDefaults: true, directives: {
  'connect-src': ["'self'", AAD], 'frame-src': ["'self'", AAD], 'form-action': ["'self'", AAD],
}}});
const bridge = helmet({ contentSecurityPolicy: { useDefaults: true, directives: {
  'connect-src': ["'self'", AAD], 'frame-src': ["'self'", AAD], 'form-action': ["'self'", AAD],
}}, crossOriginOpenerPolicy: false });
app.use((req, res, next) => (req.path === '/redirect.html' ? bridge : base)(req, res, next));
```
   `await app.listen(env.PORT, '0.0.0.0')`. Logger nestjs-pino con redacción de `req.headers.authorization`.
6. `src/app.module.ts`: ConfigModule global, ServeStaticModule `{ rootPath: join(process.cwd(), 'frontend', 'dist'), exclude: ['/api/{*path}'] }` (Express 5: sintaxis `{*path}`, NO `*`), ThrottlerModule (default 20 req/min), HealthModule (`GET /health` vía terminus, sin auth), y registro global `APP_INTERCEPTOR` → `RlsInterceptor`.
7. `src/common/prisma/rls.interceptor.ts`: SKELETON pass-through (`intercept(ctx, next) { return next.handle(); }`) con comentario `// Plan 01-02 lo implementa: tx-por-petición + set_config`. Existe ya para que el registro en app.module no cambie después (evita conflicto de archivos entre plans paralelas).
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm install && npm -w backend run build</automated>
  </verify>
  <done>Workspace instala con un npm install en la raíz; backend compila; tsc es 5.9.3 exacto (`npm ls typescript`); main.ts sirve el patrón helmet dual.</done>
</task>

<task type="auto">
  <name>Task 2: Esquema Prisma 7, migración inicial, db-bootstrap idempotente y seed</name>
  <files>fava-control-tecnico/backend/prisma/schema.prisma, fava-control-tecnico/backend/prisma/prisma.config.ts, fava-control-tecnico/backend/prisma/seed.ts, fava-control-tecnico/backend/prisma/migrations/, fava-control-tecnico/backend/scripts/db-bootstrap.ts, fava-control-tecnico/backend/src/common/prisma/prisma.service.ts</files>
  <action>
1. `prisma/schema.prisma` con generator Prisma 7: `provider = "prisma-client"`, `output` explícito, `moduleFormat = "cjs"` (sin cjs NestJS no arranca). Datasource postgresql con `env("DATABASE_URL")`.
   Modelos (forma definitiva mínima — resuelve Open Question 4 del research):
   - `User`: id uuid @default(uuid) @id, entraOid String? @unique, email String @unique, displayName String, roles Role[] (enum Role { T A S }), isActive Boolean @default(true), technicianId String? (uuid, sin FK — la tabla technicians es Fase 2), createdAt/updatedAt. Índice único en entraOid ya cubierto por @unique (Pitfall 5: lookup por petición sin seq scan).
   - `AccessRequest`: id uuid, entraOid String @unique, email String, displayName String, status String @default("pending") (pending|dismissed), createdAt/updatedAt. Mapear a `access_requests`.
   - `DailyEntry` → tabla `daily_entries`: id uuid, technicianId String @map("technician_id"), date DateTime @db.Date, status String @default("draft"), sourceYear Int? @map("source_year"), sourceSheet String? @map("source_sheet"), sourceRow Int? @map("source_row"), createdAt/updatedAt, `@@unique([technicianId, date])` (BIT-02 definitivo), índice en technicianId.
   - `WeeklyNote` → tabla `weekly_notes`: id uuid, technicianId String @map("technician_id"), weekStart DateTime @db.Date @map("week_start"), status String @default("draft"), sourceYear/sourceSheet/sourceRow como arriba, createdAt/updatedAt, índice en technicianId.
   Snake_case en @@map/@map para todas las tablas.
2. `prisma/prisma.config.ts` (hogar de config CLI en v7) con la ruta del seed.
3. `scripts/db-bootstrap.ts`: se conecta con MIGRATE_DATABASE_URL (owner) usando `pg` directo y ejecuta el SQL del Pattern 2 del research LITERAL: DO $$ IF NOT EXISTS crear rol `fava_app LOGIN NOBYPASSRLS NOSUPERUSER NOCREATEDB NOCREATEROLE`; `ALTER ROLE fava_app WITH PASSWORD` desde APP_DB_PASSWORD (parametrizado, nunca en el repo); GRANT CONNECT/USAGE/SELECT-INSERT-UPDATE-DELETE ON ALL TABLES/SEQUENCES; y la línea crítica `ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO fava_app` (sin ella cada migración futura deja tablas invisibles para la app). Idempotente: correr N veces = mismo estado. Script npm `db:bootstrap` en backend/package.json (tsx o ts-node).
   NOTA shadow-DB (Pitfall 4): los roles son de clúster, no de base — con el bootstrap corrido una vez en el clúster local, la shadow database de `migrate dev` ya ve `fava_app`. Documentar en un comentario del script: "correr db:bootstrap ANTES del primer migrate dev".
4. Levantar `docker compose up -d db`, correr `db:bootstrap` y generar la migración inicial: `npx prisma migrate dev --name init` con MIGRATE_DATABASE_URL. DATABASE_URL de runtime apunta a fava_app.
5. `prisma/seed.ts`: upsert por email de SEED_SUPERADMIN_EMAIL → User con roles [T,A,S] (Ivan-style: el dev conserva switcher completo), isActive true, entraOid null (el primer login real lo vincula por email y fija el OID — decisión del CONTEXT). Corre con MIGRATE_DATABASE_URL.
6. `src/common/prisma/prisma.service.ts`: EXACTAMENTE el contrato del bloque interfaces — PrismaClient con `new PrismaPg({ connectionString: env.DATABASE_URL, max: 10, connectionTimeoutMillis: 5_000 })` (Prisma 7: el pool NO se configura en la URL; cualquier ?connection_limit= se ignora), `transactionOptions: { timeout: 10_000, maxWait: 5_000 }`, export `als` (AsyncLocalStorage), getters `base` y `client`. Registrarlo en un PrismaModule global.
7. `backend/.env` local (gitignored) con las 8 variables apuntando al Postgres del compose; valores Entra placeholder hasta Plan 04.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run db:bootstrap && npm -w backend run db:bootstrap && npx -w backend prisma migrate deploy && npx -w backend prisma db seed</automated>
  </verify>
  <done>Bootstrap corre dos veces sin error (idempotencia probada); migración aplica; seed deja al dev como Super Admin; `psql`-check: fava_app existe con rolbypassrls = false.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test e2e de arranque + helper de BD para las plans siguientes</name>
  <files>fava-control-tecnico/backend/test/jest-e2e.json, fava-control-tecnico/backend/test/bootstrap.e2e-spec.ts, fava-control-tecnico/backend/test/helpers/db.ts</files>
  <behavior>
    - Test 1: la app Nest arranca con env completo y GET /health responde 200
    - Test 2: quitar ENTRA_TENANT_ID del env → crear la app lanza (boot falla, no la primera petición)
    - Test 3: helpers/db.ts expone ownerClient (MIGRATE_DATABASE_URL) y appClient (DATABASE_URL con fava_app) + truncateAll() que limpia las 4 tablas entre tests
  </behavior>
  <action>
Configurar `test/jest-e2e.json` (rootDir test, ts-jest o el transform que dejó el scaffold, testRegex .e2e-spec, SIN watch). Script `test:e2e` en backend/package.json con `--runInBand` (los tests comparten la BD local).
`test/helpers/db.ts`: dos PrismaClient con adapter PrismaPg — owner y app — y `truncateAll()` con `TRUNCATE ... CASCADE` vía owner. Este helper es Wave 0 para las plans 02 y 03: exportar también los UUIDs fijos de dos técnicos de prueba (`TEC_A`, `TEC_B`) para sembrar datos.
`test/bootstrap.e2e-spec.ts`: los 3 comportamientos del behavior block. Para el test 2 usar `Test.createTestingModule` con env manipulado (process.env clonado) y esperar el throw de zod.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- bootstrap</automated>
  </verify>
  <done>Suite bootstrap verde contra el Postgres del compose; app-boot falla con env incompleto; helpers listos para rls-isolation (Plan 02) y auth (Plan 03).</done>
</task>

</tasks>

<verification>
- `npm install && npm run build` en fava-control-tecnico/ compila ambos workspaces.
- `npm -w backend run db:bootstrap` × 2 sin error; `SELECT rolbypassrls FROM pg_roles WHERE rolname='fava_app'` → false.
- `npm -w backend run test:e2e -- bootstrap` verde.
- schema.prisma contiene daily_entries y weekly_notes con source_year/source_sheet/source_row (Fase 6 no migra dos veces).
</verification>

<success_criteria>
- INFRA-01: NestJS 11 modular + Prisma 7 cjs + TS 5.9.3 exacto, boot que falla con env incompleto.
- INFRA-02 (mitad local): dos roles de Postgres reproducibles por script versionado, no por psql interactivo.
- Plans 02 y 03 pueden arrancar en paralelo: PrismaService con ALS-getter y skeleton del interceptor ya registrados.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-segura-y-desplegada/01-01-SUMMARY.md`
</output>
