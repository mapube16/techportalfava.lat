---
phase: 01-fundaci-n-segura-y-desplegada
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - fava-control-tecnico/railway.toml
  - fava-control-tecnico/backend/scripts/smoke.ts
  - fava-control-tecnico/backend/package.json
  - fava-control-tecnico/docs/ENV.md
autonomous: false
requirements: [INFRA-02, INFRA-03]
user_setup:
  - service: railway
    why: "Deploy del servicio app + Postgres — requiere cuenta Railway autenticada en la CLI"
    env_vars:
      - name: RAILWAY_TOKEN
        source: "railway login interactivo (o token de cuenta) — auth gate al primer comando"
must_haves:
  truths:
    - "La app corre en una URL pública de Railway: / sirve el frontend, /health responde 200, /api/me sin token responde 401"
    - "/redirect.html se sirve SIN cabecera Cross-Origin-Opener-Policy (el puente MSAL sobrevive a helmet)"
    - "El runtime en Railway se conecta con fava_app (NOBYPASSRLS), no con el superusuario postgres que entrega Railway"
    - "Un usuario del tenant dev hace login con Microsoft en la URL pública y Layout muestra su nombre y rol traídos del API (criterio 1 del roadmap)"
    - "railway.toml + db-bootstrap + ENV.md permiten recrear el servicio en otra cuenta sin arqueología"
  artifacts:
    - path: "fava-control-tecnico/railway.toml"
      provides: "config-as-code del servicio: build, start, healthcheck, pre-deploy migrate"
      contains: "healthcheck"
    - path: "fava-control-tecnico/backend/scripts/smoke.ts"
      provides: "4 aserciones HTTP contra la URL desplegada"
      contains: "cross-origin-opener-policy"
  key_links:
    - from: "fava-control-tecnico/railway.toml"
      to: "prisma migrate deploy"
      via: "preDeployCommand con MIGRATE_DATABASE_URL (owner)"
      pattern: "migrate deploy"
    - from: "Railway service variables"
      to: "fava_app"
      via: "DATABASE_URL del runtime apunta a fava_app, NUNCA a la URL owner que autogenera Railway"
      pattern: "fava_app"
---

<objective>
Primer deploy real: servicio único en Railway (Nest sirviendo el build de Vite), Postgres con los dos roles bootstrapeados, migraciones en pre-deploy, smoke automatizado y la verificación humana del flujo completo de login con las 3 cuentas del tenant dev. Tres de los cinco criterios del roadmap solo son observables desplegados — este plan los cierra.

Purpose: INFRA-02 (deploy con dos roles y secretos en env) + INFRA-03/AUTH-01 verificados end-to-end (criterio 1 del roadmap).
Output: URL pública funcionando, smoke verde, checklist de 3 cuentas aprobado.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundaci-n-segura-y-desplegada/01-RESEARCH.md
@fava-control-tecnico/docs/ENV.md
@fava-control-tecnico/docs/ENTRA-SETUP.md

Nota de repo: los commits NO llevan Co-Authored-By ni atribución a Claude.
La cuenta Railway es personal por ahora; TODO lo del entorno debe quedar reproducible en el repo (Pitfall 8 — migración a cuenta empresa sin arqueología).
</context>

<tasks>

<task type="auto">
  <name>Task 1: railway.toml, provisión del servicio y bootstrap de la BD remota</name>
  <files>fava-control-tecnico/railway.toml, fava-control-tecnico/docs/ENV.md</files>
  <action>
1. `fava-control-tecnico/railway.toml` (Railway NO sigue el Root Directory para el config file — en el dashboard la ruta se declara ABSOLUTA desde el repo: `/fava-control-tecnico/railway.toml`; documentarlo en comentario del propio toml):
   - build: `npm ci && npm run build` (Railpack, workspace raíz)
   - deploy.startCommand: `npm start`
   - deploy.healthcheckPath: `/health`
   - deploy.preDeployCommand: `npm -w backend run migrate:deploy` (añadir script `migrate:deploy` = `prisma migrate deploy` en backend/package.json; usa MIGRATE_DATABASE_URL — correr migraciones como owner es correcto; el RUNTIME es el que no debe serlo)
2. CLI: `railway login` (si no hay sesión → auth gate: pedir al usuario que complete el login del navegador y reanudar), `railway init` (proyecto nuevo), `railway add --database postgres`, `railway link`. Configurar en el dashboard/CLI: Root Directory `/fava-control-tecnico`, config path `/fava-control-tecnico/railway.toml`.
3. Variables del servicio app vía `railway variables set` — la lista EXACTA de docs/ENV.md:
   - MIGRATE_DATABASE_URL = `${{Postgres.DATABASE_URL}}` (referencia de Railway, red privada — owner, solo migraciones/bootstrap/seed)
   - DATABASE_URL = la MISMA host/db pero con usuario `fava_app` y APP_DB_PASSWORD (construirla explícitamente; ANTI-PATTERN del research: correr el runtime con la URL owner deja RLS escrito y sin efecto, sin ningún síntoma)
   - APP_DB_PASSWORD (generar seguro), ENTRA_TENANT_ID, ENTRA_API_CLIENT_ID, ENTRA_REQUIRED_SCOPE, SEED_SUPERADMIN_EMAIL, PORT
   - Build-time del frontend: VITE_ENTRA_TENANT_ID, VITE_ENTRA_SPA_CLIENT_ID, VITE_API_SCOPE (Vite las hornea en el build del deploy)
4. Bootstrap remoto (una vez, desde local contra la URL PÚBLICA del Postgres de Railway — la privada no es alcanzable desde fuera): `MIGRATE_DATABASE_URL=<public url> APP_DB_PASSWORD=<pw> npm -w backend run db:bootstrap`, luego `prisma migrate deploy` y `prisma db seed` con la misma URL. Anotar en ENV.md el procedimiento exacto de "primer arranque en una cuenta nueva" (bootstrap → variables → deploy).
5. `railway up` (o push si se conecta el repo). Verificar en logs que ServeStatic encontró `frontend/dist` (trampa `process.cwd()` vs `__dirname` — research Pattern 1) y que el healthcheck pasó. Si la versión mayor de Postgres que provisionó Railway difiere de la del docker-compose local (17), alinear el compose y anotarlo en el SUMMARY.
  </action>
  <verify>
    <automated>curl -sf https://$(cd fava-control-tecnico && railway domain --json 2>/dev/null | node -pe "JSON.parse(require('fs').readFileSync(0)).domain" 2>/dev/null || echo PENDIENTE)/health</automated>
  </verify>
  <done>Servicio desplegado con healthcheck verde; runtime conecta como fava_app; migraciones corren en pre-deploy; todo el estado del entorno está en repo (toml + ENV.md + bootstrap).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoke post-deploy automatizado</name>
  <files>fava-control-tecnico/backend/scripts/smoke.ts, fava-control-tecnico/backend/package.json</files>
  <behavior>
    - GET {url}/health → 200
    - GET {url}/redirect.html → 200 Y la respuesta NO trae cabecera cross-origin-opener-policy (si la trae, helmet rompió el puente MSAL — Pitfall 2)
    - GET {url}/api/me sin Authorization → 401 (el guard global protege /api aun servido junto al estático)
    - GET {url}/ → 200 text/html (el frontend se sirve; ServeStatic + exclude '/api/{*path}' funciona con Express 5)
  </behavior>
  <action>
`scripts/smoke.ts`: los 4 checks del behavior con fetch nativo de Node 22, salida clara por check (✓/✗ + detalle), exit code 1 si cualquiera falla. Script npm `smoke` en backend: `tsx scripts/smoke.ts` aceptando la URL como argumento. Correrlo contra la URL real de Railway. Este smoke es obligatorio en CADA deploy de esta fase (tres de los cinco criterios solo son observables desplegados).
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run smoke -- https://<dominio-railway>.up.railway.app</automated>
  </verify>
  <done>4/4 checks verdes contra la URL pública. La ausencia de COOP en /redirect.html está probada por máquina, no por inspección.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación del flujo completo con las 3 cuentas del tenant dev (criterio 1)</name>
  <what-built>App desplegada en Railway con login Microsoft real: frontend servido por Nest, guard jose, RLS activo, pantallas de sesión del CONTEXT. Smoke automatizado en verde.</what-built>
  <action>
Preparación (una vez):
0. Añadir el redirect URI de producción en Entra: App registration SPA → Authentication → `https://<dominio-railway>.up.railway.app/redirect.html` (Claude da el dominio exacto).

Con la CONSOLA DEL NAVEGADOR ABIERTA (Chrome) en todo momento — no debe aparecer ningún error COOP ni CSP mencionando login.microsoftonline.com:

1. CUENTA INVITADA (la del seed, SEED_SUPERADMIN_EMAIL):
   a. Abrir la URL pública → pantalla de login FAVA → «Iniciar sesión con Microsoft» → autenticar.
   b. Esperado: vuelve a la app, Layout muestra TU nombre y email reales, switcher T·A·S visible (el seed tiene los 3 roles). Verificar en la pestaña Network que /api/me respondió status ok con roles de BD.
   c. En Usuarios: la sección «Solicitudes de acceso» existe (vacía por ahora).
2. CUENTA NO INVITADA (cualquier otra cuenta del tenant dev — crear una de prueba si no existe):
   a. Login → Esperado: pantalla «sin acceso» con el nombre de esa cuenta, botón «solicitar acceso» y «salir». NO entra a la app.
   b. Pulsar «solicitar acceso» → volver a entrar con la cuenta admin → la solicitud aparece en Usuarios con badge.
3. CUENTA DESACTIVADA:
   a. Con la cuenta admin, desactivar al usuario de prueba (o vía API/BD si la UI de usuarios llega en Fase 2: PATCH /api/users/:id/active).
   b. Login con esa cuenta → Esperado: mensaje específico «tu cuenta fue desactivada» — DISTINTO del de no-invitado.
4. Refrescar la página con sesión iniciada → la sesión sobrevive (acquireTokenSilent), sin re-login completo.
  </action>
  <verify>Los 4 puntos del checklist pasan con la consola del navegador limpia (cero errores COOP/CSP mencionando login.microsoftonline.com).</verify>
  <done>Criterio 1 del roadmap observado en vivo: login Microsoft en la URL pública con nombre y rol reales; pantallas no-invitado y desactivado conformes al CONTEXT; solicitud de acceso visible en Usuarios.</done>
  <resume-signal>Escribir "aprobado" si los 4 puntos pasan, o describir exactamente qué pantalla/paso falló y qué mostró la consola</resume-signal>
</task>

</tasks>

<verification>
- Smoke 4/4 contra la URL pública.
- Checklist de 3 cuentas aprobado por el usuario con consola limpia.
- `railway.toml`, ENV.md y db-bootstrap describen el 100% del entorno — cero configuración que viva solo en el dashboard o en la memoria.
</verification>

<success_criteria>
- Criterio 1 del roadmap: login Microsoft en la URL de Railway con nombre y rol reales del API.
- INFRA-02 completo: dos roles de Postgres en producción, secretos en env vars, runtime sin BYPASSRLS.
- Los criterios 2-5 (probados en local por Plans 02-03) corren sobre el MISMO esquema y patrón que ahora está desplegado.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-segura-y-desplegada/01-06-SUMMARY.md`
</output>
