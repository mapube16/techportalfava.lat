---
phase: 03-bit-cora-diaria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fava-control-tecnico/frontend/package.json
  - fava-control-tecnico/frontend/src/lib/fecha.ts
  - fava-control-tecnico/frontend/src/lib/fecha.test.ts
  - fava-control-tecnico/frontend/src/lib/draft.ts
  - fava-control-tecnico/frontend/src/lib/draft.test.ts
autonomous: true
requirements: [BIT-02, BIT-04]

must_haves:
  truths:
    - "El frontend tiene un runner de tests que corre en cada build, sin dependencias nuevas"
    - "`hoyLocal()` devuelve el día del calendario del DISPOSITIVO en Bogotá, Roma, São Paulo y Kiritimati"
    - "La aritmética de semanas del cliente no depende del huso ni del cambio de hora"
    - "Un borrador guardado se puede volver a leer; uno corrupto no rompe la pantalla; con la cuota agotada el guardado devuelve false y no lanza"
    - "El borrador detecta que el servidor escribió DESPUÉS que este dispositivo, y dice qué días"
  artifacts:
    - path: "fava-control-tecnico/frontend/src/lib/fecha.ts"
      provides: "aritmética de calendario sobre strings YYYY-MM-DD, sin objetos Date fuera del módulo"
      exports: ["hoyLocal", "sumarDias", "lunesDe", "diasDeSemana", "primerDiaMesAnterior"]
      min_lines: 35
    - path: "fava-control-tecnico/frontend/src/lib/draft.ts"
      provides: "borrador local como módulo puro, con Storage inyectable (sin React, sin window)"
      exports: ["claveBorrador", "guardar", "leer", "borrar", "enConflicto"]
      min_lines: 40
    - path: "fava-control-tecnico/frontend/src/lib/fecha.test.ts"
      provides: "los 4 husos del cliente con process.env.TZ en runtime"
      min_lines: 35
    - path: "fava-control-tecnico/frontend/src/lib/draft.test.ts"
      provides: "round-trip, JSON corrupto, cuota agotada y detección de conflicto"
      min_lines: 40
  key_links:
    - from: "fava-control-tecnico/frontend/package.json"
      to: "src/lib/*.test.ts"
      via: "el script build del workspace corre los tests antes de tsc"
      pattern: "node --import tsx --test"
---

<objective>
Sacar de los componentes las dos piezas del criterio 4 que **no tendrían forma de probarse dentro de un `.tsx`** —la fecha local del dispositivo y el borrador— y estrenar de paso el primer runner de tests que tiene el frontend, con **cero dependencias nuevas**.

Purpose: `hoyLocal()` es la mitad cliente del contrato de fecha, y su regla es la **opuesta** a la del servidor: aquí `toISOString()` es el bug. El borrador dentro del componente sería la única parte del criterio 4 sin verificación automática, y «se ve bien en el navegador» no es una prueba. `node --import tsx --test` está **verificado funcionando en este repo** (`tsx` ya viene hoisted por los workspaces): el frontend deja de tener 0 % de cobertura ejecutable sin instalar vitest ni jsdom.

Output: `frontend/src/lib/fecha.ts` y `draft.ts` como módulos puros con sus dos suites, y `npm -w frontend run build` ejecutándolas.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bit-cora-diaria/03-CONTEXT.md
@.planning/phases/03-bit-cora-diaria/03-RESEARCH.md
@.planning/phases/02-maestros-y-cat-logos/02-06-SUMMARY.md

@fava-control-tecnico/frontend/package.json
@fava-control-tecnico/frontend/tsconfig.json
@fava-control-tecnico/frontend/src/lib/api/useApiData.ts

<interfaces>
<!-- Extraído del repo. NO explores: esto es lo que necesitas. -->

`fava-control-tecnico/frontend/package.json` hoy:
```json
"scripts": { "dev": "vite", "build": "tsc && vite build", "preview": "vite preview" }
```
Sin runner de tests. `tsx@4.23.1` y `@types/node` YA están en `node_modules` de la raíz
(devDependencies de `backend`, hoisted por los workspaces). **No instales nada.**

`frontend/tsconfig.json`: `"include": ["src"]`, `strict: true`, `noEmit: true`,
`moduleResolution: "bundler"`. Los `.test.ts` caen dentro de `include`, así que `tsc`
los type-checa. `import { test } from 'node:test'` resuelve contra el `@types/node` de
la raíz. Si por lo que sea no resolviera, la salida es `"types": ["node"]` en
compilerOptions — **no** excluir los tests del typecheck.

La forma de fila que devolverá el API de la bitácora (plan 03-04) y que el borrador
guarda tal cual — `updatedAt` viaja precisamente para poder detectar el conflicto:
```ts
interface FilaDia {
  date: string;            // 'YYYY-MM-DD'
  projectId: string | null;
  machineModelId: string | null;
  conceptCode: string | null;
  phase: 'MONTAJE' | 'COLLAUDO' | null;
  description: string | null;
}
```

REGLA OPUESTA A LA DEL BACKEND, y es a propósito:
| Frontera | Correcto | Incorrecto |
|---|---|---|
| Navegador → string | getters LOCALES (`getFullYear/getMonth/getDate`) | `toISOString().slice(0,10)` |
| Servidor: string → Postgres | `new Date('YYYY-MM-DD')` (UTC por especificación) | `new Date(y, m-1, d)` |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runner de tests del frontend + `fecha.ts` del cliente</name>
  <files>
    fava-control-tecnico/frontend/package.json,
    fava-control-tecnico/frontend/src/lib/fecha.ts,
    fava-control-tecnico/frontend/src/lib/fecha.test.ts
  </files>
  <behavior>
    Con `process.env.TZ` asignado en runtime a `America/Bogota` (300), `Europe/Rome` (-120),
    `America/Sao_Paulo` (180) y `Pacific/Kiritimati` (-840):
    - Antes de nada, cada bloque afirma que el huso cambió de verdad:
      `new Date('2026-07-14T12:00:00Z').getTimezoneOffset()` es el offset esperado.
    - `hoyLocal(new Date('2026-07-14T03:30:00Z'))` → `'2026-07-13'` en Bogotá y
      `'2026-07-14'` en Roma, São Paulo y Kiritimati.
    - `hoyLocal(new Date('2026-07-14T22:30:00Z'))` → `'2026-07-14'` en Bogotá y São Paulo,
      `'2026-07-15'` en Roma y Kiritimati.
    - `sumarDias('2026-02-28', 1)` → `'2026-03-01'` (2026 no es bisiesto) y
      `sumarDias('2024-02-28', 1)` → `'2024-02-29'`.
    - `sumarDias('2026-12-31', 1)` → `'2027-01-01'`.
    - `lunesDe('2026-07-14')` (martes) → `'2026-07-13'`; `lunesDe('2026-07-19')` (domingo) → `'2026-07-13'`; `lunesDe('2026-07-13')` → `'2026-07-13'`.
    - `lunesDe('2026-03-29')` en `Europe/Rome` (el domingo del cambio de hora) sigue dando el lunes correcto: **la aritmética es sobre UTC y el DST no la toca**.
    - `diasDeSemana('2026-07-13')` → los 7 strings del 13 al 19.
    - `primerDiaMesAnterior('2026-01-05')` → `'2025-12-01'`.
  </behavior>
  <action>
1. En `frontend/package.json`, dos líneas:

```json
"test": "node --import tsx --test src/lib/fecha.test.ts src/lib/draft.test.ts",
"build": "npm run test && tsc && vite build"
```

Los ficheros van **listados por nombre**, no por glob: `npm` lanza los scripts por `cmd.exe` en Windows, que no expande globs, y las reglas de descubrimiento de `node --test` para `.ts` dependen de la minor de Node. Dos nombres explícitos son deterministas hoy y en el CI. `ponytail:` cuando llegue un tercer módulo se añade su nombre; el día que sean diez, `--test src/lib`.

El enganche va en el **build del workspace de frontend**, no en el de la raíz: el `npm run build` de la raíz ya llama a `npm -w frontend run build`, así que los tests entran en el build de Railway sin tocar el `package.json` de la raíz (que en esta wave lo está editando el plan 03-01).

2. Crear `frontend/src/lib/fecha.ts`. Todo opera **sobre strings**; el objeto `Date` solo aparece dentro de las funciones y siempre en UTC salvo en `hoyLocal`, que es justo donde tiene que ser local:

```ts
/**
 * REGLA OPUESTA A LA DEL BACKEND (backend/src/modules/daily-entries/fecha.ts), y es a
 * proposito: aqui el dia que importa es el del CALENDARIO DEL DISPOSITIVO.
 * `toISOString()` daria el dia ANTERIOR al este de UTC — verificado: las 00:30 del
 * 14/07 en Roma salen como '2026-07-13'.
 */
export const hoyLocal = (ahora: Date = new Date()): string => /* getters LOCALES */;

/** Aritmetica de calendario SOBRE STRINGS: el Date solo existe dentro, y en UTC.
 *  setUTCDate ya resuelve fin de mes, cambio de ano y bisiestos; sobre UTC el DST
 *  no interviene (en Roma, el domingo del cambio de hora tiene 23 h locales). */
export const sumarDias = (s: string, n: number): string => /* new Date(s + 'T00:00:00Z') */;

/** Lunes de la semana ISO de `s`. */
export const lunesDe = (s: string): string => /* -((getUTCDay() + 6) % 7) */;

/** Los 7 dias de la semana que empieza en `lunes`. La grilla los pinta en este orden. */
export const diasDeSemana = (lunes: string): string[] => /* ... */;

/** Suelo de la ventana de registro en el CLIENTE: «mes en curso y el anterior».
 *  El servidor calcula el suyo con tolerancia de huso y es el que manda (03-04). */
export const primerDiaMesAnterior = (hoy: string): string => /* sobre el string */;
```

3. `fecha.test.ts` con `node:test` + `node:assert/strict`. Estructura obligatoria:

```ts
import { describe, it, before, after } from 'node:test';
import assert from 'node:assert/strict';
import { hoyLocal, sumarDias, lunesDe, diasDeSemana, primerDiaMesAnterior } from './fecha';

// `TZ=x node` NO cambia el huso en este entorno (VERIFICADO: deja process.env.TZ en
// undefined y el offset en 300). Asignarlo en runtime SI funciona, y admite varios
// cambios dentro del mismo proceso.
const HUSOS: [zona: string, offset: number][] = [
  ['America/Bogota', 300], ['Europe/Rome', -120],
  ['America/Sao_Paulo', 180], ['Pacific/Kiritimati', -840],
];

for (const [zona, offset] of HUSOS) {
  describe(`dispositivo en ${zona}`, () => {
    before(() => { process.env.TZ = zona; });
    after(() => { process.env.TZ = 'America/Bogota'; });

    it('el huso cambio de verdad', () => {
      // Instante FIJO, no `new Date()`: con «ahora», Roma pasaria de -120 a -60 entre
      // julio y diciembre y la suite seria estacional.
      assert.equal(new Date('2026-07-14T12:00:00Z').getTimezoneOffset(), offset);
    });
    // ... el resto del <behavior>
  });
}
```

**Verificación en rojo obligatoria (registrar en el SUMMARY con los casos exactos):** sustituir el cuerpo de `hoyLocal` por `ahora.toISOString().slice(0, 10)`. Los dos instantes del `<behavior>` están elegidos para que eso tumbe **los cuatro husos** (el de las 03:30Z cae en Bogotá y São Paulo; el de las 22:30Z cae en Roma y Kiritimati). Si solo cae uno, la suite está probando un instante y no la regla.
  </action>
  <verify>
    <automated>cd fava-control-tecnico &amp;&amp; npm -w frontend run test</automated>
  </verify>
  <done>`npm -w frontend run test` pasa (TAP verde), `npm -w frontend run build` lo ejecuta antes de `tsc`, y la mutación a `toISOString()` tumba los cuatro husos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `draft.ts` — el borrador local como módulo puro</name>
  <files>
    fava-control-tecnico/frontend/src/lib/draft.ts,
    fava-control-tecnico/frontend/src/lib/draft.test.ts
  </files>
  <behavior>
    Con un `Storage` falso (objeto plano con `getItem`/`setItem`/`removeItem`), sin tocar `window`:
    - `claveBorrador(tecA, '2026-07-13')` !== `claveBorrador(tecA, '2026-07-20')` y !== `claveBorrador(tecB, '2026-07-13')`: dos semanas o dos técnicos no se pisan.
    - `guardar(st, clave, b)` → `true`, y `leer(st, clave)` devuelve las mismas entradas.
    - `guardar` sella `savedAt` con el reloj: el `savedAt` leído es >= el instante anterior a guardar.
    - `leer` sobre una clave que no existe → `null`.
    - `leer` sobre JSON corrupto (`st.setItem(clave, '{no es json')`) → `null`, **sin lanzar**: un borrador de una versión anterior no puede tumbar la pantalla de captura.
    - Con un `Storage` cuyo `setItem` lanza `QuotaExceededError` (modo privado de Safari, cuota agotada), `guardar` devuelve `false` y **no propaga**. Es el modo de fallo real que dejaría la pantalla en blanco.
    - `borrar(st, clave)` deja `leer` en `null`.
    - `enConflicto(b, filas)` devuelve **solo** las fechas que (a) están en el borrador y (b) tienen `Date.parse(updatedAt) > b.savedAt`. Una fila del servidor más vieja que el borrador NO es conflicto; una fila que el borrador no toca tampoco; el resultado se afirma como array de fechas, no como booleano.
  </behavior>
  <action>
Crear `frontend/src/lib/draft.ts` — sin React, sin `window`, `Storage` inyectado por parámetro. Es lo que lo hace testable con `node --test` sin DOM simulado:

```ts
export interface FilaDia { /* la del bloque <interfaces> */ }
export interface Borrador { entries: Record<string, FilaDia>; savedAt: number }

/** Una clave por (tecnico, semana): dos semanas abiertas no se pisan. */
export const claveBorrador = (technicianId: string, lunes: string) => `fava_draft_${technicianId}_${lunes}`;

/**
 * ponytail: try/catch en las DOS direcciones. localStorage lanza en modo privado de
 * Safari y al pasarse de cuota; un borrador que no se puede guardar no puede tumbar la
 * pantalla de captura. Devuelve si lo consiguio para que la UI avise en vez de mentir.
 */
export function guardar(st: Storage, clave: string, b: Borrador): boolean { /* sella savedAt: Date.now() */ }
export function leer(st: Storage, clave: string): Borrador | null { /* JSON corrupto -> null */ }
export function borrar(st: Storage, clave: string): void { /* try/catch */ }

/**
 * Deteccion de conflicto BARATA: no compara campo a campo, compara la marca del
 * servidor contra la del borrador. `updatedAt` del servidor > `savedAt` significa que
 * ese dia se escribio desde OTRO sitio DESPUES de que este dispositivo guardara: hay
 * dos versiones y decide el tecnico (decision bloqueada). Una comparacion de enteros
 * frente a cinco de strings con trim, y responde la pregunta correcta en vez de una
 * aproximada.
 */
export function enConflicto(b: Borrador, servidor: { date: string; updatedAt: string }[]): string[] { /* ... */ }
```

Nada más. Sin caducidad (decisión bloqueada), sin sincronización entre dispositivos (descartada por diseño), sin cola de reenvío (anti-feature declarada).

Escribir en el docstring del módulo, en una línea, la honestidad que pide el Pitfall 6: **«sin caducidad» significa que este código no lo borra**; el ITP de WebKit borra todo el almacenamiento escribible por script tras 7 días de Safari en uso sin visitar el sitio, e IndexedDB está en la misma lista, así que cambiar de tecnología no lo evitaría. La UI no debe prometer que el borrador es eterno.

`draft.test.ts` con `node:test`. El `Storage` falso son ~8 líneas (un `Map` + los tres métodos); el de cuota agotada es el mismo con `setItem` lanzando. No hace falta implementar `length`/`key`/`clear` más allá de lo que exija el tipo — si `strict` protesta, un `as unknown as Storage` **con comentario** es la salida.

**Verificación en rojo obligatoria (registrar en el SUMMARY):** quitar el `try/catch` de `guardar` y comprobar que el caso de cuota pasa de fallar la aserción a **lanzar**; cambiar `>` por `>=` en `enConflicto` y comprobar que el caso «una fila más vieja que el borrador no es conflicto» sigue verde pero el de igualdad cambia — si ningún caso se mueve, no hay caso de frontera y hay que añadirlo.
  </action>
  <verify>
    <automated>cd fava-control-tecnico &amp;&amp; npm -w frontend run test</automated>
  </verify>
  <done>Las dos suites pasan; `guardar` con cuota agotada devuelve `false` sin lanzar; `enConflicto` devuelve la lista de fechas, no un booleano.</done>
</task>

</tasks>

<verification>
```bash
cd fava-control-tecnico
npm -w frontend run test        # las 2 suites de node:test
npm -w frontend run build       # test -> tsc -> vite build
npm run build                   # el build de la raiz arrastra el de frontend
git diff --stat -- package.json package-lock.json frontend/package.json
# frontend/package.json cambia SOLO en `scripts`; package-lock.json NO cambia
```
</verification>

<success_criteria>
- `npm -w frontend run test` existe y pasa; es el primer test ejecutable del frontend.
- Los tests corren dentro de `npm -w frontend run build`, o sea dentro del build de Railway.
- Cero dependencias nuevas: `package-lock.json` sin cambios.
- Las 4 zonas del cliente afirman su offset **antes** de afirmar la fecha.
- Mutar `hoyLocal` a `toISOString()` tumba los cuatro husos (registrado).
- `draft.ts` no importa React ni referencia `window`, `localStorage` ni `document`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/03-bit-cora-diaria/03-02-SUMMARY.md`.

Incluir sí o sí:
- La firma exacta de los 5 exports de `fecha.ts` y los 5 de `draft.ts` — los consume el plan 03-05 y son su contrato.
- La invocación exacta del runner que funcionó (y cuál se descartó, si hubo que probar más de una).
- Los casos caídos en cada verificación en rojo.
</output>
