---
phase: 03-bit-cora-diaria
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04", "03-07"]
files_modified:
  - fava-control-tecnico/frontend/src/lib/api/daily-entries.ts
  - fava-control-tecnico/frontend/src/lib/api/projects.ts
  - fava-control-tecnico/frontend/src/ui.tsx
  - fava-control-tecnico/frontend/src/screens/Inbox.tsx
  - fava-control-tecnico/frontend/src/screens/Week.tsx
  - fava-control-tecnico/frontend/src/i18n.ts
autonomous: true
requirements: [BIT-01, BIT-02, BIT-04]

must_haves:
  truths:
    - "El técnico abre Semana y ve los 7 días reales de su semana traídos del API, no un mock"
    - "Puede navegar a semanas anteriores y siguientes, y arranca en la semana en curso"
    - "En móvil ve una tarjeta por día, no una grilla con scroll horizontal"
    - "Los días fuera de la ventana de registro salen deshabilitados con el motivo escrito, no revientan al guardar"
    - "Cerrar el navegador con la semana a medio llenar y volver a abrirlo conserva lo escrito"
    - "Si el servidor tiene una versión más nueva de un día que el borrador local, se muestran las dos y elige el técnico"
  artifacts:
    - path: "fava-control-tecnico/frontend/src/lib/api/daily-entries.ts"
      provides: "cliente tipado de las 3 rutas + los tipos que SON el contrato"
      exports: ["getWeek", "putDay", "deleteDay"]
      min_lines: 45
    - path: "fava-control-tecnico/frontend/src/screens/Week.tsx"
      provides: "grilla semanal (tarjetas en móvil) contra el API, con ventana y borrador local"
      min_lines: 150
  key_links:
    - from: "fava-control-tecnico/frontend/src/screens/Week.tsx"
      to: "/api/daily-entries"
      via: "getWeek dentro de useApiData, con la semana y dataVersion en las deps"
      pattern: "getWeek"
    - from: "fava-control-tecnico/frontend/src/screens/Week.tsx"
      to: "lib/draft.ts"
      via: "guardar con retardo en un useRef + flush en pagehide"
      pattern: "pagehide"
    - from: "fava-control-tecnico/frontend/src/screens/Week.tsx"
      to: "lib/fecha.ts"
      via: "lunesDe/diasDeSemana/hoyLocal — cero aritmética de fechas dentro del componente"
      pattern: "lunesDe"
---

<objective>
Cablear `Week.tsx` al API real: la grilla semanal pasa a ser **la fuente de verdad** de la captura (decisión bloqueada: «la grilla manda; tocar una fila abre el drawer para editar ese día»), con navegación entre semanas, ventana temporal visible y el borrador local del criterio 4.

Purpose: es la pantalla donde el técnico ve qué días le faltan. Hoy pinta `WEEK` de `data.ts` con fechas cableadas («20–26 Jul 2026») y arrastra un bloque de firma y otro de gastos que son de las **Fases 4 y 5**. Y el borrador —lo que hace que la mala conectividad de la planta no cueste trabajo escrito— no existe.

Output: el cliente tipado de la bitácora, `ConceptPill` sin acoplamiento a `i18n`, y una `Week.tsx` que lee, navega, bloquea lo que está fuera de ventana y no pierde lo escrito.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bit-cora-diaria/03-CONTEXT.md
@.planning/phases/03-bit-cora-diaria/03-RESEARCH.md
@.planning/phases/03-bit-cora-diaria/03-02-SUMMARY.md
@.planning/phases/03-bit-cora-diaria/03-03-SUMMARY.md
@.planning/phases/03-bit-cora-diaria/03-04-SUMMARY.md
@.planning/phases/02-maestros-y-cat-logos/02-06-SUMMARY.md

@fava-control-tecnico/frontend/src/screens/Week.tsx
@fava-control-tecnico/frontend/src/screens/ProjectDetail.tsx
@fava-control-tecnico/frontend/src/lib/api/client.ts
@fava-control-tecnico/frontend/src/lib/api/useApiData.ts
@fava-control-tecnico/frontend/src/lib/api/projects.ts
@fava-control-tecnico/frontend/src/lib/api/catalogs.ts
@fava-control-tecnico/frontend/src/lib/fecha.ts
@fava-control-tecnico/frontend/src/lib/draft.ts
@fava-control-tecnico/frontend/src/ui.tsx
@fava-control-tecnico/frontend/src/state.tsx

<interfaces>
<!-- Extraído del repo. NO explores: esto es lo que necesitas. -->

De 03-02 (ya en disco), los módulos puros y probados:
```ts
// frontend/src/lib/fecha.ts
hoyLocal(ahora?: Date): string        // getters LOCALES: aqui toISOString() es EL BUG
sumarDias(s: string, n: number): string
lunesDe(s: string): string
diasDeSemana(lunes: string): string[] // los 7 en orden
primerDiaMesAnterior(hoy: string): string

// frontend/src/lib/draft.ts
claveBorrador(technicianId: string, lunes: string): string
guardar(st: Storage, clave: string, b: Borrador): boolean   // false = cuota agotada
leer(st: Storage, clave: string): Borrador | null
borrar(st: Storage, clave: string): void
enConflicto(b: Borrador, servidor: {date, updatedAt}[]): string[]   // las fechas en conflicto
```

Patrón de pantalla cableada (02-06), dos líneas y tres estados:
```tsx
const { data, setData, error } = useApiData(() => getWeek(from, to), [from, to, state.dataVersion]);
if (error) return <ApiState error={error} ... />;
if (!data)  return <ApiState ... />;
```
`apiFetch` ya desenvuelve el `message` del error del backend: la UI ramifica por **código**
(`USUARIO_SIN_TECNICO`, `FECHA_FUTURA`, …), nunca por un JSON entero.

`state.me` es el `MeResponse`; con `status: 'ok'`, `state.me.user.technicianId` es el
`technicianId` que necesita `claveBorrador`. `state.mobile` ya existe y gobierna la
vista móvil del resto de pantallas. `state.dataVersion` + `refresh()` invalidan.

`ConceptPill` HOY (`ui.tsx`):
```tsx
export function ConceptPill({ code, lang }: { code: string; lang: Lang }) {
  const cc = CONCEPTS.find((x) => x.c === code) || CONCEPTS[0];   // <- de i18n
```
Lo usan `Week.tsx` y `Inbox.tsx`. `CONCEPT_COLOR` (mapa código→color) sí puede seguir
viniendo de `i18n`: es decoración y el guarda-rail lo permite explícitamente.

El patrón de temporizador del repo (`state.tsx`): `useRef<number|undefined>` +
`window.setTimeout` + `clearTimeout` en el cleanup. Ya está escrito dos veces; no metas
un `useDebounce` de librería por 6 líneas.
</interfaces>
</context>

<mobile>
## Móvil — obligatorio, no opcional (auditoría UX 2026-07-26)

El técnico captura **desde el móvil en planta**: es el caso de uso central de BIT-01.
El plan 03-07 (misma fase, wave anterior) ya dejó los primitivos listos —
`useIsMobile()`, tokens `--tap`/`--fs-input`, barra lateral colapsable, contraste
corregido —, así que aquí **se hereda, no se reinventa**.

Reglas para `Week.tsx`:
- **Móvil = tarjetas apiladas, una por día.** La grilla horizontal de hoy pone la
  descripción del trabajo en ~180px de ancho a 390px de pantalla: cuatro palabras por
  línea. Escritorio conserva la tabla.
- Los controles de la tarjeta (concepto, proyecto, máquina) usan los primitivos ya
  corregidos: nada por debajo de 44px de alto.
- La navegación entre semanas debe alcanzarse **con el pulgar**, sin zoom.
- Nada de anchos fijos en px para columnas: si algo necesita ancho, va por token.
- El botón «igual que ayer» tiene que ser cómodo de pulsar — es la acción que ahorra
  seis capturas idénticas.

Verificación: además del build, una sonda manual a 390px descrita en el SUMMARY, y
comprobar que ningún elemento desborda horizontalmente.
</mobile>

<tasks>

<task type="auto">
  <name>Task 1: Cliente tipado de la bitácora y `ConceptPill` sin `i18n`</name>
  <files>
    fava-control-tecnico/frontend/src/lib/api/daily-entries.ts,
    fava-control-tecnico/frontend/src/lib/api/projects.ts,
    fava-control-tecnico/frontend/src/ui.tsx,
    fava-control-tecnico/frontend/src/screens/Inbox.tsx
  </files>
  <action>
1. Crear `frontend/src/lib/api/daily-entries.ts` copiando el bloque **«Contrato definitivo del API»** de `03-04-SUMMARY.md` — igual que 02-06 copió el de 02-05. Los tipos del dominio viven **junto a su cliente**, que es donde está el contrato (patrón establecido en 02-06); `types.ts` se queda con lo de interfaz.

```ts
export interface DailyEntryRow { /* date, projectId, projectName, machineModelId,
  machineCode, conceptCode, phase, description, status, updatedAt */ }
export interface WeekResponse { minDate: string; maxDate: string; entries: DailyEntryRow[] }
export interface DayInput { /* projectId, machineModelId, conceptCode, phase, description */ }

/** La fecha viaja SOLO en la URL: el backend rechaza un `date` en el cuerpo con
 *  400 FECHA_EN_EL_CUERPO. No la metas en el body «por comodidad». */
export const getWeek   = (from: string, to: string) => apiFetch<WeekResponse>(`/daily-entries?from=${from}&to=${to}`);
export const putDay    = (date: string, body: DayInput) => apiSend<DailyEntryRow>(`/daily-entries/${date}`, 'PUT', body);
export const deleteDay = (date: string) => apiFetch<void>(`/daily-entries/${date}`, { method: 'DELETE' });
```

Con el aviso escrito al lado de `getWeek`: **`updatedAt` no es decoración** — es lo único que permite a `enConflicto()` responder «¿alguien escribió después que yo?» sin comparar cinco campos con `trim`.

2. En `lib/api/projects.ts`, añadir el tipo y la función de la proyección de técnico, copiando el bloque de `03-03-SUMMARY.md`:
```ts
/** Lo que ve un TECNICO en GET /api/projects: solo nombre y maquinas, y SOLO activos.
 *  Es la MISMA ruta que `listProjects()` — el servidor reparte por rol. No hay ni debe
 *  haber un endpoint aparte. */
export interface ProjectForTechnician { id: string; name: string; machines: { machineModelId: string; code: string; description: string | null }[] }
export const listProjectsForTechnician = () => apiFetch<ProjectForTechnician[]>('/projects');
```

3. `ConceptPill` recibe la etiqueta **por prop** y deja de resolverla desde `i18n` (Pitfall 5 del research):
```tsx
export function ConceptPill({ code, label }: { code: string; label: string }) {
  // Solo el color viene de i18n (CONCEPT_COLOR): es decoracion. La ETIQUETA la trae
  // quien pinta, desde GET /api/catalogs. Sin esto, Week.tsx heredaria las etiquetas
  // cableadas sin que `check-no-free-text` se entere: `ui.tsx` no esta —ni puede
  // estar— en su lista de archivos.
```
`label` es **obligatorio**, no opcional: un opcional deja abierta justo la puerta que esto cierra.

`ConceptCode` (usado por `PdfPreview.tsx`) solo pinta color: pásalo a `CONCEPT_COLOR` y déjalo como está.

4. `Inbox.tsx` es el otro llamante y es pantalla de la **Fase 4** sobre el mock `state.week`. Pasa la etiqueta en el sitio de la llamada (una línea, `CONCEPTS.find(...)?.[state.lang] ?? d.concept`). El acoplamiento queda **visible** en una pantalla que la Fase 4 va a reescribir, en vez de escondido en `ui.tsx`.
  </action>
  <verify>
    <automated>cd fava-control-tecnico &amp;&amp; npx --prefix frontend tsc --noEmit -p frontend/tsconfig.json &amp;&amp; npm -w frontend run build</automated>
  </verify>
  <done>`tsc --noEmit` limpio; `ConceptPill` no importa `CONCEPTS`; el cliente tipado expone las 3 rutas y `ProjectForTechnician`.</done>
</task>

<task type="auto">
  <name>Task 2: `Week.tsx` — grilla de 7 días, navegación y ventana temporal</name>
  <files>
    fava-control-tecnico/frontend/src/screens/Week.tsx,
    fava-control-tecnico/frontend/src/i18n.ts
  </files>
  <action>
Reescribir el cuerpo de `Week.tsx`. Estructura y decisiones bloqueadas que hay que respetar al pie de la letra:

**Semana y navegación.** Estado local `lunes`, inicializado a `lunesDe(hoyLocal())` — «al entrar se abre la semana en curso». Botones anterior/siguiente = `sumarDias(lunes, ∓7)`. La navegación hacia atrás se corta cuando la semana entera cae por debajo de `data.minDate`, y hacia delante cuando el lunes supera `data.maxDate`. Toda la aritmética sale de `lib/fecha.ts`: **cero `new Date` dentro del componente**.

**Carga.** `useApiData(() => getWeek(lunes, sumarDias(lunes, 6)), [lunes, state.dataVersion])`, con los dos estados de `ApiState`. El código `USUARIO_SIN_TECNICO` tiene su propio mensaje ES/IT: «tu usuario todavía no está vinculado a un técnico; pídeselo a un administrador». Sin eso, el técnico ve una pantalla de error genérica o —peor— una semana vacía, y cree que la app le borró el trabajo.

**Las 7 filas se generan de `diasDeSemana(lunes)`, no de `entries`.** Un día sin registro es una fila **vacía y visible**: enseñar los huecos es el propósito de la pantalla. `entries` se indexa por `date` para rellenar.

**Móvil.** `state.mobile` → lista vertical de tarjetas, una por día; escritorio → filas de la grilla. Es el patrón que ya usan las demás pantallas. **Nada de scroll horizontal.**

**Ventana temporal.** Una fila cuya fecha esté fuera de `[data.minDate, data.maxDate]` se pinta **deshabilitada y con el motivo visible**, no simplemente clicable-y-que-reviente:
- Por encima de `maxDate` → «todavía no se puede registrar».
- Por debajo de `minDate` → «fuera de la ventana de registro; pídeselo a un administrador».

Esto no es un detalle de estilo: **el 1 de septiembre la semana del 27 de julio al 2 de agosto queda partida** — 5 días irregistrables y 2 registrables en la misma pantalla. Es el peor día del mes para la adopción y tiene que estar diseñado, no descubierto. Los límites los manda el **servidor** (`data.minDate`/`maxDate`); el cálculo del cliente (`hoyLocal`, `primerDiaMesAnterior`) solo sirve para el `min`/`max` del `<input type="date">` del drawer. Si algún día divergen, diverge del lado seguro.

**Etiquetas de concepto.** Cargar `getCatalogs()` (abierto a `T·A·S`) y pasar `label` a `ConceptPill`. **Ninguna etiqueta de concepto sale de `i18n`** — solo el color.

**Retirar del render** el bloque de firma (`SignatureBox`) y el de gastos: son de las Fases 5 y 4, hoy pintan mocks y en esta pantalla estorban a lo que el técnico viene a hacer. El botón «enviar» que hoy hace `showToast('submitted')` y navega a `notes` **se quita también**: el envío es BIT-05/NOTA-01, Fase 4. Déjalo escrito en el summary para que la Fase 4 sepa que tiene que traerlos de vuelta con datos reales.

Textos nuevos en `i18n.ts`, en **es e it** (el toggle de idioma existe y una clave a medias sale como `undefined` en pantalla).
  </action>
  <verify>
    <automated>cd fava-control-tecnico &amp;&amp; npm -w frontend run build</automated>
  </verify>
  <done>La grilla pinta los 7 días de la semana en curso desde el API, navega entre semanas, muestra tarjetas en móvil y deshabilita con motivo los días fuera de ventana.</done>
</task>

<task type="auto">
  <name>Task 3: El borrador local en la pantalla — autoguardado con retardo y aviso de conflicto</name>
  <files>
    fava-control-tecnico/frontend/src/screens/Week.tsx,
    fava-control-tecnico/frontend/src/i18n.ts
  </files>
  <action>
Conectar `lib/draft.ts` (ya probado en 03-02) a la pantalla. **Toda la lógica sigue en el módulo**: aquí solo van los efectos de React.

**Clave.** `claveBorrador(state.me.user.technicianId, lunes)` — una por (técnico, semana), así dos semanas abiertas no se pisan.

**Autoguardado con retardo** (decisión bloqueada: «guarda mientras escribe, no solo al salir del campo»), con el patrón de temporizador que el repo ya usa dos veces en `state.tsx`:
```tsx
const t = useRef<number | undefined>(undefined);
useEffect(() => {
  clearTimeout(t.current);
  t.current = window.setTimeout(() => guardar(localStorage, clave, { entries: borrador, savedAt: 0 }), 400);
  return () => clearTimeout(t.current);
}, [borrador, clave]);
```
**El retardo es del BORRADOR LOCAL, no del `PUT`.** Escribir en la base cada 300 ms envenena `updated_at` y, desde la Fase 4, llena el `audit_log` append-only de ruido. El `PUT` va cuando el técnico lo pide o al cerrar el drawer (eso es 03-06).

**Cierre brusco:**
```tsx
// pagehide dispara donde beforeunload no (Safari iOS). localStorage es SINCRONO, asi
// que la escritura se completa; IndexedDB no lo garantiza — esa es la razon de fondo
// para no cambiar de tecnologia, no la cuota.
window.addEventListener('pagehide', flush);
```

**Indicador** (discreción de Claude): un punto/etiqueta discreto «borrador sin enviar» en las filas que tienen cambios locales no confirmados por el servidor. Que se entienda sin leer.

**Conflicto** (decisión bloqueada: «avisar y que el técnico elija, mostrando ambas versiones»):
- Al cargar la semana, `enConflicto(borrador, data.entries)` da las fechas en las que el servidor escribió **después** que este dispositivo.
- Para cada una, un aviso que muestra **las dos versiones** (concepto + descripción bastan para reconocerlas) y **dos acciones**: «quedarme con lo mío» (el borrador gana y queda pendiente de guardar) y «usar lo del servidor» (se descarta esa entrada del borrador).
- Nunca se decide en silencio en ninguna de las dos direcciones.

**Cuota agotada:** si `guardar()` devuelve `false`, avisar una vez de que el borrador no se está guardando en este dispositivo. Es el modo de fallo real de Safari en modo privado y es el que dejaría al técnico creyendo que su trabajo está a salvo.

**Honestidad en el texto** (Pitfall 6): **no** prometer en la UI que el borrador es eterno. «Sin caducidad» significa que este código no lo borra; el ITP de WebKit borra todo el almacenamiento escribible por script tras 7 días de Safari en uso sin visitar el sitio, y cambiar a IndexedDB no lo evitaría.

`ponytail:` el borrador **no** vive en `state.tsx`. Ese provider no persiste nada y ya se decidió que ninguna lista de dominio vive ahí (02-06). El borrador es de la pantalla y del `localStorage`.

**Sonda manual obligatoria** (el frontend no tiene runner de integración; es lo que hizo 02-06 y hay que repetirlo): levantar el backend local con `DEV_AUTH_ENABLED=true`, entrar como técnico, llenar dos días **sin guardar**, cerrar la pestaña, reabrir, y comprobar que siguen ahí. Registrar el resultado en el summary con los payloads reales.
  </action>
  <verify>
    <automated>cd fava-control-tecnico &amp;&amp; npm -w frontend run test &amp;&amp; npm -w frontend run build</automated>
  </verify>
  <done>Cerrar y reabrir el navegador con la semana a medio llenar conserva lo escrito; un día que el servidor cambió después muestra las dos versiones y deja elegir; con la cuota agotada la pantalla avisa y sigue funcionando.</done>
</task>

</tasks>

<verification>
```bash
cd fava-control-tecnico
npm -w frontend run test     # las 2 suites de node:test de 03-02 siguen verdes
npm -w frontend run build    # test -> tsc -> vite build
npm run build                # raiz: check:no-free-text + check:fecha-servidor + los dos workspaces
grep -rn "toISOString" frontend/src/screens/Week.tsx frontend/src/lib/api/daily-entries.ts   # VACIO
grep -rn "new Date" frontend/src/screens/Week.tsx                                            # VACIO
git diff --stat -- package.json package-lock.json frontend/package.json                      # VACIO
```
Sonda manual contra el backend local (el frontend no tiene runner de integración): sesión de técnico, `GET /api/daily-entries`, `PUT` de un día, recarga, cierre y reapertura de pestaña. Payloads y respuestas al summary.
</verification>

<success_criteria>
- `Week.tsx` no importa de `../data` (comprobable con grep) y no contiene `new Date` ni `toISOString`.
- Los 7 días se generan del calendario, no de las filas que devuelve el API: los huecos se ven.
- La ventana temporal que gobierna la UI es la del **servidor** (`minDate`/`maxDate` del GET).
- `ConceptPill` recibe la etiqueta por prop; ninguna etiqueta de concepto sale de `i18n`.
- El borrador sobrevive al cierre de la pestaña y el conflicto muestra ambas versiones.
- Los textos nuevos existen en `es` **y** en `it`.
- Cero dependencias nuevas.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/03-bit-cora-diaria/03-05-SUMMARY.md`.

Incluir sí o sí:
- El contrato de props con el que `Week.tsx` abre el drawer (qué fecha y qué fila le pasa, y qué espera de vuelta): es **la entrada directa** del plan 03-06 y sin ella ese plan tendría que adivinarlo.
- Qué se retiró del render (firma, gastos, botón de enviar) y para qué fase, para que la Fase 4 lo traiga de vuelta con datos reales en vez de creer que se perdió.
- El resultado de la sonda manual, con payloads.
- Cómo quedó el diseño del indicador de borrador y del aviso de conflicto (era discreción de Claude: documéntalo o la Fase 4 lo rehará distinto).
</output>
