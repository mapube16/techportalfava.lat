---
phase: 03-bit-cora-diaria
plan: 06
type: execute
wave: 4
depends_on: ["03-05", "03-07"]
files_modified:
  - fava-control-tecnico/frontend/src/components/LogDayDrawer.tsx
  - fava-control-tecnico/frontend/src/screens/Week.tsx
  - fava-control-tecnico/frontend/src/data.ts
  - fava-control-tecnico/frontend/src/i18n.ts
  - fava-control-tecnico/scripts/check-no-free-text.mjs
autonomous: true
requirements: [BIT-01, BIT-02, BIT-03, BIT-04]

must_haves:
  truths:
    - "El técnico toca un día de la grilla, el drawer se abre con ESE día y lo edita"
    - "El proyecto sale de los proyectos activos del API; la máquina, de las del proyecto elegido"
    - "El concepto sale del catálogo del API y no de una lista cableada"
    - "Un día de LR/NR/IL se guarda sin proyecto; uno de trabajo sin proyecto lo rechaza el servidor y la pantalla lo explica"
    - "«Igual que ayer» copia proyecto, máquina, concepto, fase y descripción del día anterior"
    - "El build falla si alguien vuelve a meter un mock o un catálogo cableado en Week.tsx o LogDayDrawer.tsx"
  artifacts:
    - path: "fava-control-tecnico/frontend/src/components/LogDayDrawer.tsx"
      provides: "edición de UN día contra el API, con los 4 selectores cerrados y la descripción"
      min_lines: 130
    - path: "fava-control-tecnico/scripts/check-no-free-text.mjs"
      provides: "las 9 pantallas cubiertas, con Week y LogDayDrawer dentro"
      contains: "LogDayDrawer.tsx"
  key_links:
    - from: "fava-control-tecnico/frontend/src/components/LogDayDrawer.tsx"
      to: "/api/daily-entries/:date"
      via: "putDay del cliente tipado, con la fecha SOLO en la URL"
      pattern: "putDay"
    - from: "fava-control-tecnico/frontend/src/components/LogDayDrawer.tsx"
      to: "/api/projects"
      via: "listProjectsForTechnician — la máquina sale de las del proyecto elegido"
      pattern: "listProjectsForTechnician"
    - from: "fava-control-tecnico/scripts/check-no-free-text.mjs"
      to: "screens/Week.tsx y components/LogDayDrawer.tsx"
      via: "el array ARCHIVOS, que hoy solo cubre las 7 pantallas de la Fase 2"
      pattern: "Week.tsx"
---

<objective>
Cerrar la fase: el drawer deja de ser un alta suelta con mocks y pasa a **editar el día que la grilla le pasa**, con proyecto, máquina y concepto salidos del API; `data.ts` pierde `MACHINES` y `LOG_PROJECTS`; y el guarda-rail `check-no-free-text` se amplía para cubrir las dos pantallas nuevas.

Purpose: **la ampliación del guarda-rail no es un pie de página, es el cierre de la fase.** Su array `ARCHIVOS` solo lista las 7 pantallas del cutover de la Fase 2: tal como está hoy, esta fase se puede cerrar con `CONCEPTS` de `i18n` y `MACHINES`/`LOG_PROJECTS` de `data.ts` dentro del drawer y con `npm run build` **en verde**. Ampliarlo convierte «retiramos los mocks» en algo que el build de Railway comprueba en cada deploy.

Output: `LogDayDrawer.tsx` contra el API, `data.ts` sin los dos mocks de bitácora, y el guarda-rail cubriendo 9 archivos.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bit-cora-diaria/03-CONTEXT.md
@.planning/phases/03-bit-cora-diaria/03-RESEARCH.md
@.planning/phases/03-bit-cora-diaria/03-04-SUMMARY.md
@.planning/phases/03-bit-cora-diaria/03-05-SUMMARY.md
@.planning/phases/02-maestros-y-cat-logos/02-06-SUMMARY.md

@fava-control-tecnico/frontend/src/components/LogDayDrawer.tsx
@fava-control-tecnico/frontend/src/screens/Week.tsx
@fava-control-tecnico/frontend/src/components/NewProjectModal.tsx
@fava-control-tecnico/frontend/src/lib/api/daily-entries.ts
@fava-control-tecnico/frontend/src/lib/api/projects.ts
@fava-control-tecnico/frontend/src/lib/api/catalogs.ts
@fava-control-tecnico/frontend/src/lib/fecha.ts
@fava-control-tecnico/frontend/src/data.ts
@fava-control-tecnico/scripts/check-no-free-text.mjs

<interfaces>
<!-- Extraído del repo. NO explores: esto es lo que necesitas. -->

`LogDayDrawer.tsx` HOY — sus 3 imports vetados son exactamente los que el guarda-rail
busca, y por eso la Task 3 tiene que ir DESPUÉS de la 1:
```tsx
import { CONCEPTS } from '../i18n';               // <- IMPORTS_VETADOS
import { LOG_PROJECTS, MACHINES } from '../data'; // <- IMPORTS_VETADOS
```
Su estado es local y suelto (`concept`, `machine`, `desc`), la fecha está cableada
(`defaultValue="2026-07-23"`) y `save()` solo hace `showToast('saved')`.
Se monta desde `Layout.tsx:240` con `{state.logOpen ? <LogDayDrawer /> : null}`, y
`state.logOpen` lo encienden `Home.tsx` y `Notes.tsx`.

`scripts/check-no-free-text.mjs`:
```js
/** Las 7 pantallas del cutover de la Fase 2. */
const ARCHIVOS = [ 'screens/Projects.tsx', ..., 'components/InviteUserModal.tsx' ];
const IMPORTS_VETADOS = [
  [/import\s*\{([^}]*)\}\s*from\s*['"]\.\.\/i18n['"]/g, ['CONCEPTS'], '...'],
  [/import\s*\{([^}]*)\}\s*from\s*['"]\.\.\/data['"]/g, ['MACHINES','CURRENCIES'], '...'],
];
```
Reglas del script: rechaza un `<input>` que alimente concepto / rol / moneda, y esos
imports. Un `<select>`, botones tipo chip o un `<datalist>` cerrado **valen** — el
drawer ya usa chips para el concepto y eso se queda.

`data.ts` — quién consume cada mock (verificado con grep, y **corrige** a la lista de
«deprecados» del research):
| Mock | Consumidores hoy | Lo retira |
|---|---|---|
| `MACHINES`, `LOG_PROJECTS` | **solo** `LogDayDrawer.tsx` | **esta fase** |
| `WEEK` (`state.week`) | `Inbox.tsx`, `PdfPreview.tsx` | Fase 4/5 — **NO se toca aquí** |
| `CURRENT_TECH` | `Home.tsx`, `Notes.tsx` | Fase 4 — **NO se toca aquí** |
| `NOTES`, `EXPENSES`, `AUDIT` | notas / gastos / auditoría | Fase 4 |
Quitar `WEEK` o `CURRENT_TECH` aquí rompe cuatro pantallas de otras fases. Es el mismo
caso que 02-06 con `MACHINES` y se resuelve igual: se quedan, con su fila en la tabla.

Del API (contratos ya cerrados):
```ts
listProjectsForTechnician(): ProjectForTechnician[]   // {id, name, machines:[{machineModelId, code, description}]}
getCatalogs(): { concepts: {code, labelEs, labelIt, sortOrder}[], ... }   // abierto a T·A·S
putDay(date, { projectId, machineModelId, conceptCode, phase, description }): DailyEntryRow
```
Códigos de error que la pantalla verá de verdad: `PROYECTO_REQUERIDO_POR_CONCEPTO`,
`MAQUINA_NO_ES_DEL_PROYECTO`, `PROYECTO_INACTIVO`, `FECHA_FUTURA`,
`FECHA_DEMASIADO_ANTIGUA`, `USUARIO_SIN_TECNICO`. La tabla completa, en 03-04-SUMMARY.
</interfaces>
</context>

<mobile>
## Móvil — obligatorio (auditoría UX 2026-07-26)

`LogDayDrawer` es **la pantalla que el técnico usa con guantes**, al sol, en planta.
Hereda los primitivos de 03-07; aquí se aplica a la forma del drawer:

- **A pantalla completa en móvil** (hoy es una hoja inferior con `max-width: 440`).
  En escritorio se mantiene como está.
- Los botones de concepto son hoy los peores objetivos táctiles de la app (~27px en una
  rejilla de 4 columnas). Con `--tap` deben pasar de 44px; si a 390px cuatro columnas
  no dan ese tamaño, **bajar a 3 o 2 columnas en móvil** — la rejilla se adapta, el
  objetivo táctil no se negocia.
- El `textarea` de la descripción a 16px o más: por debajo, Safari hace zoom al
  enfocarlo y descuadra el drawer entero.
- Los chips de máquina, igual: 44px de alto.
- El botón de guardar tiene que quedar alcanzable sin que el teclado virtual lo tape.

Verificación: build, y sonda manual a 390px con el teclado abierto.
</mobile>

<tasks>

<task type="auto">
  <name>Task 1: `LogDayDrawer.tsx` — editar UN día contra el API</name>
  <files>
    fava-control-tecnico/frontend/src/components/LogDayDrawer.tsx,
    fava-control-tecnico/frontend/src/screens/Week.tsx,
    fava-control-tecnico/frontend/src/i18n.ts
  </files>
  <action>
El drawer pasa de «alta suelta» a **editor de un día concreto**, que es la decisión bloqueada: «la grilla manda; tocar una fila abre el drawer para editar ese día; el drawer deja de ser un alta suelta».

**Entrada.** Recibe la fecha y la fila actual (si existe) con el contrato que dejó escrito `03-05-SUMMARY.md`. La fecha se **muestra** (`<input type="date">` en solo lectura o texto, tú decides) pero **no se elige** dentro del drawer: elegirla aquí reabriría la puerta del duplicado que la grilla cierra por construcción.

**Los cuatro selectores, todos cerrados:**
| Campo | Origen | Widget |
|---|---|---|
| Proyecto | `listProjectsForTechnician()` — solo activos | `<select>` |
| Máquina | `machines` **del proyecto elegido**, no del catálogo global | chips (los que ya hay) |
| Concepto | `getCatalogs().concepts`, etiqueta `labelEs`/`labelIt` según `state.lang` | chips (los que ya hay), color de `CONCEPT_COLOR` |
| Fase | `MONTAJE` / `COLLAUDO` | `<select>` o chips |

Cambiar de proyecto **limpia la máquina** si la seleccionada no está entre las del proyecto nuevo: si no, el `PUT` se lleva un `MAQUINA_NO_ES_DEL_PROYECTO` que el técnico no puede explicarse.

**BIT-03 en la UI.** Con concepto `LR`/`NR`/`IL`, el selector de proyecto y el de máquina se **ocultan o deshabilitan** (no se envían). Con los otros cinco, el proyecto es obligatorio y el botón de guardar lo dice antes de intentarlo. La validación del servidor sigue siendo la que manda: si aun así responde `PROYECTO_REQUERIDO_POR_CONCEPTO`, se muestra el código como hace el resto de la app (02-06: se muestra el código del servidor tal cual tras «No se pudo guardar»; traducir ~30 códigos es tabla muerta hasta que FAVA diga cuáles ve).

**«Igual que ayer»** (decisión bloqueada, con justificación real: el PDF de Ivan Cortés tiene 6 días seguidos con el mismo texto). Copia **los cinco** campos —proyecto, máquina, concepto, fase y descripción— del día anterior. El día anterior sale de las `entries` que ya tiene la grilla: `sumarDias(fecha, -1)`. Si ese día no tiene registro, el botón no se pinta.

**Guardado.** `putDay(fecha, {...})` al pulsar guardar. **Nada de debounce por tecla hacia el servidor**: el retardo es del borrador local (03-05) y escribir en la base cada 300 ms envenena `updated_at` y, desde la Fase 4, el `audit_log` append-only. Al volver 200, la fila que responde el `PUT` sustituye la de la grilla **sin refetch** (por eso el servidor la devuelve; precedente exacto: el `PUT` de sold-days devuelve el `delta`), se limpia esa entrada del borrador y se cierra el drawer.

**Descripción.** El `<textarea>` que ya existe, ahora conectado. La validación local de «no vacío» se mantiene solo para los conceptos de trabajo: un `LR` no necesita descripción.

En `Week.tsx`, cablear la apertura (tocar una fila → el drawer con esa fecha), el retorno del `PUT` y el cierre. Las filas fuera de ventana no abren nada.

Textos nuevos en `i18n.ts`, en **es e it**.
  </action>
  <verify>
    <automated>cd fava-control-tecnico &amp;&amp; npm -w frontend run build</automated>
  </verify>
  <done>Tocar un día abre el drawer con ese día; proyecto, máquina, concepto y fase salen del API; «igual que ayer» copia los 5 campos; guardar hace `PUT` y actualiza la fila sin recargar la semana.</done>
</task>

<task type="auto">
  <name>Task 2: Retirar los mocks de bitácora y ampliar el guarda-rail</name>
  <files>
    fava-control-tecnico/frontend/src/data.ts,
    fava-control-tecnico/scripts/check-no-free-text.mjs
  </files>
  <action>
1. **`data.ts`:** borrar `MACHINES` y `LOG_PROJECTS` (sus únicos consumidores son el drawer, que ya no los usa) y actualizar la tabla de la cabecera con la fase que retira cada uno de los que quedan.

   **`WEEK` y `CURRENT_TECH` se quedan.** El research los listaba como «deprecados en esta fase»; verificado con grep, `WEEK` lo consumen `Inbox.tsx` y `PdfPreview.tsx` y `CURRENT_TECH` lo consumen `Home.tsx` y `Notes.tsx` — cuatro pantallas de las Fases 4 y 5. Borrarlos aquí rompe el build por cuatro sitios y no cierra ningún requisito de BIT-01..04. Es el mismo caso que 02-06 con `MACHINES`, y se resuelve igual: se quedan, con su fila en la tabla y su fase. **Escríbelo en el summary como corrección explícita al research**, o el verificador de fase buscará un `WEEK` que no debe haber desaparecido.

2. **Ampliar `scripts/check-no-free-text.mjs`**: añadir `'screens/Week.tsx'` y `'components/LogDayDrawer.tsx'` al array `ARCHIVOS` y actualizar su comentario («Las 7 pantallas del cutover de la Fase 2» → 9, nombrando las 2 de la bitácora). El script pasa de `7/7` a `9/9`.

   Añadir también `LOG_PROJECTS` a la lista de identificadores vetados del import de `'../data'`: sin eso, el día que alguien reintroduzca el mock de proyectos el script lo dejaría pasar.

   **El agujero que queda y que hay que cerrar a mano:** `ui.tsx` importa de `'./i18n'` (una sola barra), así que **no puede** estar en la lista —la regex de `IMPORTS_VETADOS` busca `'../i18n'`— y `Week.tsx` heredaría las etiquetas cableadas a través de `ConceptPill` sin que el script se entere. Lo cerró el plan 03-05 pasando `label` por prop. **Verifica que sigue cerrado** (`grep "CONCEPTS" frontend/src/ui.tsx` no debe devolver ningún uso dentro de `ConceptPill`) y anota el agujero en el summary para quien amplíe el script en la Fase 4.

3. Dejar `npm run build` **en verde** en la raíz: es el commit que pone el guarda-rail en verde y lo deja cubriendo las dos pantallas nuevas, que es el patrón que 02-06 estableció («un guarda-rail que llega a verde se engancha en el mismo commit que lo pone verde»).

**Verificación en rojo obligatoria (registrar en el SUMMARY con el `archivo:línea`):**
- Reintroducir temporalmente `import { CONCEPTS } from '../i18n';` en `LogDayDrawer.tsx` → el script debe salir `8/9` con `exit 1`, y `npm run build` debe **fallar en el primer paso**. Retirarlo.
- Ídem con `import { MACHINES } from '../data';` en `Week.tsx`.
Si alguna de las dos sale verde, el archivo no entró en `ARCHIVOS` y la fase se estaría cerrando con el agujero abierto.
  </action>
  <verify>
    <automated>cd fava-control-tecnico &amp;&amp; node scripts/check-no-free-text.mjs &amp;&amp; npm run build</automated>
  </verify>
  <done>`check-no-free-text` reporta `9/9 archivos limpios` con `exit 0`; `data.ts` ya no exporta `MACHINES` ni `LOG_PROJECTS`; las dos roturas temporales lo ponen en rojo nombrando archivo y línea.</done>
</task>

</tasks>

<verification>
```bash
cd fava-control-tecnico
node scripts/check-no-free-text.mjs         # 9/9 archivos limpios, exit 0
npm run check:fecha-servidor                # sigue verde
npm -w frontend run test                    # las 2 suites de node:test
npm run build                               # los dos guarda-railes + los dos workspaces
grep -rn "from '../data'" frontend/src/screens/Week.tsx frontend/src/components/LogDayDrawer.tsx   # VACIO
grep -rn "MACHINES\|LOG_PROJECTS" frontend/src/                                                     # VACIO
git diff --stat -- package.json package-lock.json frontend/package.json                             # VACIO
```
Sonda manual final contra el backend local (`DEV_AUTH_ENABLED=true`), como hizo 02-06 — el frontend no tiene runner de integración y los tipos están escritos a mano:
1. Sesión de técnico → Semana → los 7 días con sus huecos.
2. Registrar los 7 días con proyecto, máquina, concepto, fase y descripción. Recargar: **exactamente lo mismo** (criterio 1 del roadmap).
3. Un día con `LR` sin proyecto → se acepta. Un día con `DC` sin proyecto → se rechaza y la pantalla lo explica (criterio 3).
4. Media semana llena sin enviar → cerrar el navegador → reabrir → sigue ahí (criterio 4).
5. «Igual que ayer» sobre un día en blanco → copia los cinco campos.
Payloads y respuestas al summary.
</verification>

<success_criteria>
- Ni `Week.tsx` ni `LogDayDrawer.tsx` importan de `../data` ni de `../i18n` un catálogo.
- `check-no-free-text` cubre **9** archivos y sale en verde dentro de `npm run build`.
- `data.ts` ya no exporta `MACHINES` ni `LOG_PROJECTS`; `WEEK` y `CURRENT_TECH` siguen ahí **a propósito**, documentado.
- La máquina del drawer sale de las del proyecto elegido, nunca del catálogo global.
- Los 5 criterios del roadmap para la Fase 3 son demostrables desde la pantalla.
- Cero dependencias nuevas.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/03-bit-cora-diaria/03-06-SUMMARY.md`.

Marcar `requirements-completed: [BIT-01, BIT-02, BIT-03, BIT-04]` — **este** es el plan que los hace verificables desde la interfaz, exactamente como 02-06 cerró CAT-01..CAT-05 después de que cinco planes los dejaran abiertos a propósito.

Incluir sí o sí:
- El resultado de la sonda manual, punto por punto contra los 5 criterios del roadmap, con payloads.
- La corrección explícita al research sobre `WEEK`/`CURRENT_TECH` (se quedan, y por qué).
- El agujero de `ui.tsx` en el guarda-rail: por qué no puede estar en `ARCHIVOS` y cómo quedó cerrado a mano.
- Qué le queda a la Fase 4 en estas dos pantallas: el envío, el bloqueo en solo lectura (BIT-05), y el bloque de firma y gastos que 03-05 retiró del render.
</output>
