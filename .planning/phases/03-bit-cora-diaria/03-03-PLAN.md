---
phase: 03-bit-cora-diaria
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - fava-control-tecnico/backend/src/modules/projects/projects.controller.ts
  - fava-control-tecnico/backend/src/modules/projects/projects.service.ts
  - fava-control-tecnico/backend/test/projects.e2e-spec.ts
autonomous: true
requirements: [BIT-01]

must_haves:
  truths:
    - "Un técnico autenticado recibe 200 en GET /api/projects (hoy recibe 403)"
    - "Ese técnico ve exactamente nombre y máquinas: ni valor de contrato, ni OA, ni cliente, ni días vendidos"
    - "Un proyecto desactivado no aparece en la lista del técnico, y sí en la del admin"
    - "Las máquinas de cada proyecto llegan con su `machineModelId`, que es lo que la bitácora escribe"
    - "El técnico sigue recibiendo 403 en el resto de rutas de /api/projects"
  artifacts:
    - path: "fava-control-tecnico/backend/src/modules/projects/projects.service.ts"
      provides: "LISTA_TECNICO como select propio + listarParaTecnico()"
      contains: "LISTA_TECNICO"
    - path: "fava-control-tecnico/backend/src/modules/projects/projects.controller.ts"
      provides: "@Roles('T','A','S') solo en el @Get() y el reparto por rol"
      contains: "listarParaTecnico"
  key_links:
    - from: "fava-control-tecnico/backend/src/modules/projects/projects.controller.ts"
      to: "projects.service.listarParaTecnico"
      via: "un if por roles del actor dentro del mismo handler"
      pattern: "listarParaTecnico"
    - from: "fava-control-tecnico/backend/test/projects.e2e-spec.ts"
      to: "el JSON completo de la respuesta"
      via: "sonda de secretos sobre JSON.stringify, además del conjunto exacto de claves"
      pattern: "JSON\\.stringify"
---

<objective>
Relajar `GET /api/projects` para que un **Técnico** pueda listar los proyectos activos, con una **proyección distinta**: solo nombre y máquinas. Es el bloqueo que 02-06 dejó nombrado y firmado («`LogDayDrawer` sigue con mock a propósito: la Fase 3 tiene que RELAJAR el `@Roles` del `GET /api/projects` a `T`, **no crear un endpoint nuevo**»).

Purpose: sin esto, el selector de proyecto de la captura no existe y el drawer se queda con `LOG_PROJECTS` cableado en `data.ts`. Y sin la proyección, relajar el permiso entregaría a los ~15 técnicos el valor de contrato, el n° de OA y los días vendidos de cada obra — información comercial que la decisión bloqueada excluye explícitamente.

Output: una ruta, dos constantes `select`, un `if` por rol, y dos aserciones distintas que prueban que no se filtra nada.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bit-cora-diaria/03-CONTEXT.md
@.planning/phases/03-bit-cora-diaria/03-RESEARCH.md
@.planning/phases/02-maestros-y-cat-logos/02-05-SUMMARY.md
@.planning/phases/02-maestros-y-cat-logos/02-06-SUMMARY.md

@fava-control-tecnico/backend/src/modules/projects/projects.controller.ts
@fava-control-tecnico/backend/src/modules/projects/projects.service.ts
@fava-control-tecnico/backend/test/projects.e2e-spec.ts
@fava-control-tecnico/backend/test/helpers/fixtures.ts

<interfaces>
<!-- Extraído del repo. NO explores: esto es lo que necesitas. -->

`projects.controller.ts` hoy:
```ts
@Controller('api/projects')
@Roles('A', 'S')                 // <- se queda EN LA CLASE. Solo el @Get() se relaja.
export class ProjectsController {
  @Get()  listar() { return this.service.listar(); }
  ...
}
```

Precedente EXACTO del repo (02-03, `catalogs.controller.ts`): `@Roles('S')` en la
CLASE y `@Roles('T','A','S')` en el método; el guard hace `getAllAndOverride`, o sea
que el método pisa a la clase. Al revés —clase abierta y `@Roles` restrictivo por
método— el olvido de un decorador en un endpoint futuro cae del lado inseguro.

`projects.service.ts` hoy: `DETALLE` y `LISTA` son constantes `as const` con el
comentario «el `select` ES el contrato». `LISTA` incluye INACTIVOS a propósito
(«filtra el selector, no el endpoint») y devuelve `machineCodes: string[]`.

Relación de máquinas en el esquema:
  Project.machines: ProjectMachine[]  →  ProjectMachine.machineModel: MachineModel
  MachineModel { id, code, description, isActive }

`test/helpers/fixtures.ts` es un **contrato cerrado** (02-01): NO lo modifiques. Si te
falta un fixture, defínelo dentro de `projects.e2e-spec.ts`.
Da: `crearTecnico()`, `crearProyecto()`, `crearJornadaAprobada()`.
`test/helpers/app.ts` da `createTestApp()` y `crearUsuario({ roles, technicianId })`.
`test/helpers/db.ts` da `truncateAll()`, `ownerClient`, `TEC_A`/`TEC_B`, `MAQ_TEST`.

RLS no ayuda aquí: la política `proj_read` es `USING (TRUE)` — **no oculta ni una
columna ni un proyecto al técnico**. El aislamiento comercial es de capa de servicio y
hay que probarlo como tal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `LISTA_TECNICO` y el reparto por rol, con la prueba de fuga</name>
  <files>
    fava-control-tecnico/backend/src/modules/projects/projects.service.ts,
    fava-control-tecnico/backend/src/modules/projects/projects.controller.ts,
    fava-control-tecnico/backend/test/projects.e2e-spec.ts
  </files>
  <behavior>
    - Un token de Técnico contra `GET /api/projects` devuelve **200** (hoy es 403).
    - `Object.keys(res.body[0]).sort()` es **exactamente** `['id', 'machines', 'name']`.
    - Con el proyecto sembrado con `contractValue: 4150000.5`, `oaNumber: 'OA-SECRETO'`, `normalHours: 9` y `clientName: 'CLIENTE-SECRETO'`, ninguna de las cadenas `'4150000'`, `'OA-SECRETO'`, `'CLIENTE-SECRETO'`, `'contractValue'`, `'soldDays'`, `'currencyCode'` aparece en `JSON.stringify(res.body)` — la sonda caza la fuga **anidada** que el conjunto de claves de primer nivel no ve.
    - Cada elemento de `machines` tiene exactamente `{ machineModelId, code, description }`.
    - Un token de Admin y uno de Super Admin siguen recibiendo la forma ANTERIOR (`clientName`, `contractValue` como `number`, `machineCodes`): los ~45 casos existentes de la suite no se tocan y siguen verdes.
  </behavior>
  <action>
1. En `projects.service.ts`, junto a `LISTA` y `DETALLE`, añadir:

```ts
/**
 * Lo que ve un TECNICO. Es un `select` PROPIO y no un subconjunto calculado de LISTA:
 * asi una columna nueva del esquema (o de LISTA) NO puede aparecer aqui sin que alguien
 * la escriba a mano. contractValue / oaNumber / normalHours / currencyCode / clientName
 * son informacion comercial y la decision bloqueada dice «solo nombre y maquinas».
 *
 * Nada de `delete p.contractValue` ni de `omit`: lo que no se pide no se puede filtrar
 * por error.
 */
const LISTA_TECNICO = {
  id: true,
  name: true,
  machines: { select: { machineModel: { select: { id: true, code: true, description: true } } } },
} as const;
```

y el método, que aplana la relación a lo que consume el drawer (`machineModelId` es lo que la bitácora escribe en `daily_entries.machine_model_id`):

```ts
/** Solo ACTIVOS: «proyectos cerrados no aparecen en la lista» (decision bloqueada).
 *  Los dias YA registrados contra un proyecto cerrado se siguen viendo — eso lo
 *  resuelve el `projectName` denormalizado de GET /api/daily-entries (03-04). */
async listarParaTecnico() { /* findMany where isActive, orderBy name asc, machines por code asc */ }
```

2. En `projects.controller.ts`, **solo** el `@Get()`:

```ts
@Get()
@Roles('T', 'A', 'S')   // la clase sigue en @Roles('A','S'); el guard hace getAllAndOverride
listar(@CurrentUser() actor: UserModel) {
  const admin = actor.roles.some((r) => r === 'A' || r === 'S');
  return admin ? this.service.listar() : this.service.listarParaTecnico();
}
```

El `if` va por **roles**, no por RLS, y va aquí y no en el servicio: `proj_read` es `USING (TRUE)`, así que el motor no ayuda y la decisión es de capa de servicio. Un usuario con roles `['T','A']` es admin (mismo criterio que `ROLE_RANK` del frontend y que `app.is_admin` del interceptor).

3. En `test/projects.e2e-spec.ts`, un `describe` nuevo al final (**no toques los casos existentes**) con un usuario de rol `T` vinculado a un técnico. La sonda de secretos y el conjunto exacto de claves son **dos aserciones**, no una: cada una caza lo que la otra no.

**Verificación en rojo obligatoria (registrar en el SUMMARY con el número de casos):** añadir `clientName: true` a `LISTA_TECNICO` y comprobar que caen **los dos** casos (el del conjunto de claves y el de la sonda). Si solo cae uno, falta la otra aserción.
  </action>
  <verify>
    <automated>cd fava-control-tecnico &amp;&amp; npm -w backend run test:e2e -- projects</automated>
  </verify>
  <done>Un técnico recibe 200 con `{id, name, machines}` y cero dato comercial; los 45 casos previos de la suite siguen verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: La lista del técnico solo trae proyectos activos, y el resto de rutas sigue cerrada</name>
  <files>
    fava-control-tecnico/backend/test/projects.e2e-spec.ts,
    fava-control-tecnico/backend/src/modules/projects/projects.service.ts
  </files>
  <behavior>
    - Sembrados dos proyectos, uno con `isActive: false`: el técnico recibe **solo el activo**; el admin recibe **los dos** (`LISTA` no filtra por diseño). Las dos mitades en el mismo caso, comparando contra el conteo del owner y no contra «> 0».
    - Un proyecto sin máquinas asociadas devuelve `machines: []`, no revienta ni se omite de la lista.
    - Las máquinas llegan ordenadas por `code` ascendente (dos modelos sembrados en orden inverso lo demuestran).
    - Los proyectos llegan ordenados por `name` ascendente.
    - Con el mismo token de técnico: `GET /api/projects/:id` → **403**, `POST /api/projects` → 403, `PATCH /api/projects/:id` → 403, `PATCH /api/projects/:id/active` → 403, `PUT /api/projects/:id/machines` → 403, `PUT /api/projects/:id/sold-days` → 403. Es la mitad que demuestra que se relajó **un método**, no la clase.
  </behavior>
  <action>
Ampliar el `describe` de la Task 1. El bloque de los seis 403 se escribe como `it.each` sobre `[método, ruta]` para que el mensaje del fallo **nombre la ruta abierta**: un booleano no serviría de nada el día que alguien mueva el decorador a la clase.

Los modelos de máquina extra se crean en el propio spec con un prefijo propio y se borran en `afterAll` — los catálogos **no se truncan** (02-01) y una segunda pasada de la suite chocaría con el `@unique` de `machine_models.code`.

`ponytail:` no hace falta paginar ni buscar: son 4 proyectos reales y el techo del research está en ~500 filas.

**Verificación en rojo obligatoria (registrar en el SUMMARY):** quitar `where: { isActive: true }` de `listarParaTecnico` y comprobar que cae el caso del proyecto desactivado; mover `@Roles('T','A','S')` del método a la clase y comprobar que caen los **seis** casos del `it.each`.
  </action>
  <verify>
    <automated>cd fava-control-tecnico &amp;&amp; npm -w backend run test:e2e -- projects</automated>
  </verify>
  <done>El técnico ve solo proyectos activos, ordenados, con sus máquinas ordenadas; y recibe 403 en las 6 rutas restantes del controlador.</done>
</task>

</tasks>

<verification>
```bash
cd fava-control-tecnico
npm -w backend run test:e2e -- projects     # los ~45 previos + los nuevos
npm -w backend run test:e2e                 # 14 suites, --runInBand, SIN paralelizar entre procesos
npm -w backend run db:seed                  # repone el Super Admin que truncateAll() se lleva
npm run build
git diff --stat -- package.json package-lock.json backend/package.json   # VACIO
```
Si la suite completa falla en `catalogs`, `users-invite` o `technicians`, **re-ejecútala antes de tocar nada**: `truncateAll()` es global y dos procesos sobre la misma base se tumban mutuamente (riesgo conocido, tres reproducciones en 02-03, cero cambios de código en las tres).
</verification>

<success_criteria>
- `GET /api/projects` responde 200 a un Técnico y sigue respondiendo 403 en las otras 6 rutas del controlador.
- La respuesta del técnico tiene **exactamente** las claves `id`, `name`, `machines`.
- No existe un segundo endpoint tipo `/api/projects/for-technician` (prohibido por el CONTEXT y por 02-06).
- No existe ningún `delete`, `omit` ni borrado de campos después de leer: la garantía está en el `select`.
- `@Roles('A','S')` sigue en la clase.
- Cero dependencias nuevas y cero migraciones.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/03-bit-cora-diaria/03-03-SUMMARY.md`.

Incluir sí o sí:
- El bloque `ts` con la forma **exacta** de `ProjectForTechnician` (`{id, name, machines: {machineModelId, code, description}[]}`) — el plan 03-05 escribe su cliente tipado copiándolo.
- Los casos caídos en cada una de las cuatro verificaciones en rojo.
- Si algún caso previo de `projects.e2e-spec.ts` tuvo que tocarse, cuál y por qué (no debería hacer falta ninguno).
</output>
