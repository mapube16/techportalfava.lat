---
phase: 02-maestros-y-cat-logos
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - fava-control-tecnico/backend/src/modules/users/users.controller.ts
  - fava-control-tecnico/backend/src/modules/users/users.service.ts
  - fava-control-tecnico/backend/test/users-invite.e2e-spec.ts
autonomous: true
requirements: [CAT-05]
must_haves:
  truths:
    - "Un Admin invita a un Técnico y la fila queda creada con entra_oid null: el primer login real la vincula por email"
    - "Un Admin que intenta invitar a alguien con rol Admin recibe 403; un Super Admin lo consigue"
    - "Un Admin vincula un usuario a un técnico y GET /api/me de ese usuario devuelve su technicianId — que es de donde sale la GUC app.technician_id de la Fase 3"
    - "Un técnico ya vinculado a otro usuario no se puede reasignar sin desvincular: 409, no un 500 de constraint"
    - "GET /api/users devuelve technicianId para que la pantalla Usuarios pueda mostrar el vínculo"
  artifacts:
    - path: "fava-control-tecnico/backend/src/modules/users/users.controller.ts"
      provides: "POST /api/users y PATCH /api/users/:id/technician"
      contains: "technician"
    - path: "fava-control-tecnico/backend/test/users-invite.e2e-spec.ts"
      provides: "CAT-05 probado: invitación con escalada y vínculo usuario↔técnico"
      min_lines: 60
  key_links:
    - from: "fava-control-tecnico/backend/src/modules/users/users.service.ts"
      to: "users.technician_id"
      via: "vínculo que alimenta app.technician_id en el RlsInterceptor"
      pattern: "technicianId"
    - from: "fava-control-tecnico/backend/src/modules/me/me.controller.ts"
      to: "GET /api/me"
      via: "technicianId ya viaja en la respuesta: el test lo comprueba end-to-end"
      pattern: "technicianId"
---

<objective>
Cerrar CAT-05 con las dos únicas piezas que la Fase 1 no dejó hechas: crear un usuario (invitar) y vincularlo a un técnico. El vínculo **no es opcional**: `app.technician_id` — la GUC que aísla la bitácora — sale de `users.technician_id`, y hoy esa columna existe pero nadie la escribe. Sin esto la Fase 3 arranca con todos los técnicos viendo cero registros propios.

Purpose: CAT-05 completo y precondición dura de la Fase 3.
Output: 2 endpoints sobre el módulo `users` existente + una suite e2e.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-maestros-y-cat-logos/02-RESEARCH.md
@fava-control-tecnico/backend/src/modules/users/users.controller.ts
@fava-control-tecnico/backend/src/modules/users/users.service.ts
@fava-control-tecnico/backend/src/modules/me/me.controller.ts
@fava-control-tecnico/backend/test/users-roles.e2e-spec.ts

Regla de repo: los commits NO llevan Co-Authored-By ni ninguna atribución a Claude.
`UsersModule` YA está registrado en `app.module.ts`: **este plan no toca `app.module.ts`** (lo tiene 02-03 en esta misma wave).
No modificar `test/helpers/db.ts` ni `fixtures.ts` (propiedad de 02-01).
</context>

<interfaces>
<!-- Lo que YA existe en users.service.ts (01-03). Reutilizar, no reescribir. -->

```ts
const CAMPOS = { id: true, displayName: true, email: true, roles: true, isActive: true } as const;
const esAdmin = (roles: Role[]) => roles.some((r) => r === 'A' || r === 'S');

class UsersService {
  listar(): Promise<...>;                                        // GET /api/users
  asignarRoles(actor: UserModel, targetId: string, next: Role[]); // errores: SOLO_SUPER_ADMIN_ASIGNA_ADMIN,
                                                                  // NO_PUEDES_QUITARTE_SUPER_ADMIN, DEBE_QUEDAR_UN_SUPER_ADMIN
  cambiarActivo(actor: UserModel, targetId: string, isActive: boolean); // + SOLO_SUPER_ADMIN_DESACTIVA_ADMINS
}
```
```ts
// users.controller.ts: @Controller('api/users') @Roles('A','S') — la regla condicional vive en el SERVICIO
// me.controller.ts: MeResponse.user ya incluye technicianId: string | null  (no hay que tocarlo)
// schema (02-01): User.technicianId es @unique + FK a Technician con onDelete: Restrict
```

Superficie nueva:
| Método | Ruta | Rol | Body |
|---|---|:--:|---|
| POST | `/api/users` | A S | `{ email, displayName, roles?: Role[], technicianId?: string \| null }` |
| PATCH | `/api/users/:id/technician` | A S | `{ technicianId: string \| null }` |

`GET /api/users` amplía `CAMPOS` con `technicianId` (contrato consumido por `frontend/src/lib/api/users.ts` en 02-06).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (TDD): POST /api/users — invitar reutilizando la escalada que ya existe</name>
  <files>fava-control-tecnico/backend/src/modules/users/users.controller.ts, fava-control-tecnico/backend/src/modules/users/users.service.ts, fava-control-tecnico/backend/test/users-invite.e2e-spec.ts</files>
  <behavior>
    - Admin invita `{ email, displayName, roles: ['T'] }` → 201, fila creada con `entraOid: null` e `isActive: true`. El email se guarda en minúsculas y sin espacios (igual que hace `seed.ts`).
    - Admin invita `{ roles: ['T','A'] }` → 403 `SOLO_SUPER_ADMIN_ASIGNA_ADMIN`. Super Admin con el mismo body → 201.
    - Sin `roles` en el body → por defecto `['T']`.
    - Email ya existente → 409, no un 500 de constraint único.
    - Email con formato inválido o `displayName` vacío → 400.
    - Un Técnico (rol T) → 403 (lo da el `@Roles('A','S')` de clase que ya está).
    - El usuario invitado, al hacer login con un token cuyo `email` coincide, queda vinculado por el flujo de `entra_oid` que ya existe (01-03): el test lo comprueba pidiendo `/api/me` con un token de ese email y esperando `status: 'ok'`.
  </behavior>
  <action>
Añadir `crear(actor, datos)` a `users.service.ts`, **encima de la misma regla de escalada que ya existe**: extraer la comprobación `esAdmin(next) && !actor.roles.includes('S')` a un helper privado si hace falta, pero no duplicar la condición. El anti-lockout no aplica aquí (crear un usuario nunca deja la app sin Super Admin).

`entraOid: null` a propósito y con comentario: la identidad definitiva la fija el primer login real por coincidencia de email (patrón de `seed.ts` y del guard de 01-03). Invitar no manda ningún correo — V1X-01 (Resend) está diferido y esta fase no lo adelanta.

`technicianId` opcional en el POST: si viene, se aplica la misma validación de la Task 2 (mismo helper, no una copia).

Normalización de email: `trim().toLowerCase()`, idéntica a `seed.ts`, o el usuario invitado como `Nombre@Fava.com` nunca vinculará su login.

Duplicado: capturar `P2002` → `ConflictException('EMAIL_YA_REGISTRADO')`.

RED primero.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- users-invite</automated>
  </verify>
  <done>Los 7 comportamientos pasan; `users-roles.e2e-spec.ts` sigue verde (la escalada no se duplicó ni se relajó).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (TDD): PATCH /api/users/:id/technician — el vínculo del que depende la Fase 3</name>
  <files>fava-control-tecnico/backend/src/modules/users/users.controller.ts, fava-control-tecnico/backend/src/modules/users/users.service.ts, fava-control-tecnico/backend/test/users-invite.e2e-spec.ts</files>
  <behavior>
    - Admin envía `{ technicianId }` sobre un usuario sin vínculo → 200, y `GET /api/users` devuelve ese `technicianId`.
    - El usuario vinculado pide `GET /api/me` → `user.technicianId` es el esperado (prueba end-to-end de la precondición de la Fase 3).
    - `{ technicianId: null }` desvincula → 200.
    - Vincular a un técnico que **ya está** asignado a otro usuario → 409 `TECNICO_YA_VINCULADO`, no un 500 del `@unique`.
    - `technicianId` inexistente → 400/404 con código propio, nunca 500 de FK.
    - Técnico (rol T) → 403.
    - Tras vincular, una petición del usuario vinculado corre con `app.technician_id` fijado: el test lo comprueba de forma observable (por ejemplo, que ese usuario solo ve sus propias `daily_entries` sembradas frente a las de otro técnico).
  </behavior>
  <action>
`vincularTecnico(actor, targetId, technicianId | null)` en el servicio. Un solo `update`; los dos errores esperados se traducen desde los códigos de Prisma (`P2002` → 409 `TECNICO_YA_VINCULADO`, `P2003` → 400 `TECNICO_INEXISTENTE`) en vez de precomprobar con dos `SELECT` que además abren una carrera.

Ampliar la constante `CAMPOS` con `technicianId` — es el contrato que consume la pantalla Usuarios. El comentario actual de `CAMPOS` dice «Fase 2 lo expande; no adelantar campos»: esta es esa expansión, y no se añade nada más.

El último caso del `<behavior>` es el que justifica todo el plan: **sin escribir esa columna, la GUC llega vacía y el técnico no ve ni sus propios registros.** Escribirlo aunque parezca redundante con las suites de RLS: aquí se prueba el camino completo (endpoint → columna → guard → interceptor → política).
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- users-invite</automated>
  </verify>
  <done>Los 7 comportamientos pasan, incluida la comprobación observable del aislamiento vía el vínculo.</done>
</task>

</tasks>

<verification>
```bash
cd fava-control-tecnico
npm -w backend run build
npm -w backend run test:e2e -- "users"      # users-roles + users-invite
npm -w backend run test:e2e                 # sin regresiones
```
</verification>

<success_criteria>
- `POST /api/users` existe y respeta la escalada de roles sin duplicar la regla.
- `PATCH /api/users/:id/technician` existe y `GET /api/me` devuelve el `technicianId` resultante.
- `GET /api/users` incluye `technicianId`.
- Ni un solo 500 en los caminos de conflicto (`P2002`/`P2003` traducidos).
- Cero dependencias nuevas; `app.module.ts` intacto.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-maestros-y-cat-logos/02-04-SUMMARY.md` declarando explícitamente que la precondición de la Fase 3 (`app.technician_id` poblado) queda cubierta y cómo se prueba.
</output>
