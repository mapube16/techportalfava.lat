---
phase: 02-maestros-y-cat-logos
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - fava-control-tecnico/backend/src/modules/catalogs/
  - fava-control-tecnico/backend/src/modules/technicians/
  - fava-control-tecnico/backend/src/app.module.ts
  - fava-control-tecnico/backend/test/catalogs.e2e-spec.ts
  - fava-control-tecnico/backend/test/technicians.e2e-spec.ts
autonomous: true
requirements: [CAT-01, CAT-02]
must_haves:
  truths:
    - "Un técnico autenticado obtiene los 4 catálogos de un solo GET /api/catalogs (la Fase 3 los necesita para registrar el día)"
    - "Un Super Admin edita la etiqueta ES/IT de un concepto; un Admin recibe 403; nadie puede crear ni borrar un concepto (el endpoint no existe: 404)"
    - "Un Super Admin da de alta un rol técnico nuevo y aparece en el catálogo; desactivarlo lo saca de los selectores pero no de los registros históricos"
    - "Un Admin crea un técnico externo sin cuenta Entra y lo ve en la lista; al desactivarlo sus daily_entries siguen siendo legibles"
    - "No existe DELETE en ningún endpoint de catálogos ni de técnicos: 404"
  artifacts:
    - path: "fava-control-tecnico/backend/src/modules/catalogs/catalogs.controller.ts"
      provides: "GET /api/catalogs + PATCH de etiquetas + ABM de roles, monedas y modelos"
      contains: "api/catalogs"
    - path: "fava-control-tecnico/backend/src/modules/technicians/technicians.controller.ts"
      provides: "GET/POST /api/technicians, PATCH /:id y /:id/active"
      contains: "api/technicians"
    - path: "fava-control-tecnico/backend/test/catalogs.e2e-spec.ts"
      provides: "CAT-01 probado por API: roles por endpoint, listas cerradas, sin DELETE"
      min_lines: 60
    - path: "fava-control-tecnico/backend/test/technicians.e2e-spec.ts"
      provides: "CAT-02 probado por API: alta sin Entra, baja que conserva historia"
      min_lines: 50
  key_links:
    - from: "fava-control-tecnico/backend/src/app.module.ts"
      to: "CatalogsModule y TechniciansModule"
      via: "imports del módulo raíz"
      pattern: "CatalogsModule"
    - from: "fava-control-tecnico/backend/src/modules/technicians/technicians.service.ts"
      to: "role_types"
      via: "FK validada: un roleTypeId inexistente responde 400/404, nunca 500"
      pattern: "roleTypeId"
---

<objective>
Los dos módulos que alimentan a todos los demás: el catálogo cerrado (conceptos con etiquetas editables, roles técnicos, monedas, modelos de máquina) y el maestro de técnicos, que existe sin cuenta Entra.

Purpose: CAT-01 y CAT-02. `GET /api/catalogs` es además la primera pieza que consume la Fase 3, por eso se abre a los tres roles desde ya y no se toca dos veces.
Output: ~12 endpoints con el patrón de `users`, y dos suites e2e.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-maestros-y-cat-logos/02-RESEARCH.md
@fava-control-tecnico/backend/src/modules/users/users.controller.ts
@fava-control-tecnico/backend/src/modules/users/users.service.ts
@fava-control-tecnico/backend/src/modules/users/users.module.ts
@fava-control-tecnico/backend/test/users-roles.e2e-spec.ts

Regla de repo: los commits NO llevan Co-Authored-By ni ninguna atribución a Claude.
Cero dependencias nuevas: validación de body a mano (3 líneas) + `ParseUUIDPipe`, precedente 01-03. Nada de `class-validator`.
No modificar `test/helpers/db.ts` ni `test/helpers/fixtures.ts` (propiedad de 02-01; otros planes de esta wave los leen a la vez).
`src/app.module.ts` es propiedad de este plan en la wave 2 — el módulo `projects` lo registra 02-05 en la wave 3.
</context>

<interfaces>
<!-- Patrones ya establecidos (Fase 1). Usar directamente. -->

```ts
// common/auth/roles.decorator.ts
export const Roles = (...roles: Role[]) => SetMetadata(ROLES, roles);   // 'T' | 'A' | 'S'
// common/auth/current-user.decorator.ts → @CurrentUser() actor: UserModel  (fila de BD, activa)
// common/prisma/prisma.service.ts → this.prisma.client  (ya dentro de la tx del RlsInterceptor)

// Convención de rutas: NO hay setGlobalPrefix. El controlador declara la ruta completa:
@Controller('api/catalogs')  ·  @Controller('api/technicians')

// Enums generados por Prisma 7 (ojo con los nombres — 01-03 se tropezó con esto):
import type { Role, ConceptCode, Phase, EmploymentType } from '../../generated/prisma/enums';
import type { UserModel, TechnicianModel } from '../../generated/prisma/models';
```

**Contrato exacto de `GET /api/catalogs`** (lo consume `frontend/src/lib/api/catalogs.ts` en 02-06; cambiar un nombre aquí = pantalla en blanco):
```ts
{
  concepts:      { code: string; labelEs: string; labelIt: string; sortOrder: number }[];  // ordenado por sortOrder
  roleTypes:     { id: string; name: string; isActive: boolean }[];                        // ordenado por name
  currencies:    { code: string; symbol: string; isActive: boolean }[];
  machineModels: { id: string; code: string; description: string | null; isActive: boolean }[];
}
```

**Contrato exacto de técnico** (`frontend/src/lib/api/technicians.ts`):
```ts
{ id: string; fullName: string; roleTypeId: string; roleTypeName: string;
  employmentType: 'INTERNO' | 'EXTERNO'; isActive: boolean; userId: string | null }
```

Superficie de API de este plan:
| Método | Ruta | Rol |
|---|---|:--:|
| GET | `/api/catalogs` | T A S |
| PATCH | `/api/catalogs/concepts/:code` | **S** — solo `labelEs`/`labelIt` |
| POST · PATCH | `/api/catalogs/role-types` · `/:id` | **S** — PATCH incluye `isActive` |
| POST · PATCH | `/api/catalogs/currencies` · `/:code` | **S** — `code` = 3 letras mayúsculas |
| POST · PATCH | `/api/catalogs/machine-models` · `/:id` | **S** — `code` + `description` |
| GET · POST | `/api/technicians` | A S |
| PATCH | `/api/technicians/:id` · `/:id/active` | A S |
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (TDD): módulo catalogs — un GET para todo, ABM por Super Admin, conceptos que no pueden crecer</name>
  <files>fava-control-tecnico/backend/src/modules/catalogs/catalogs.module.ts, fava-control-tecnico/backend/src/modules/catalogs/catalogs.controller.ts, fava-control-tecnico/backend/src/modules/catalogs/catalogs.service.ts, fava-control-tecnico/backend/src/app.module.ts, fava-control-tecnico/backend/test/catalogs.e2e-spec.ts</files>
  <behavior>
    - `GET /api/catalogs` con token de Técnico → 200 con las 4 listas; los 8 conceptos vienen ordenados por `sortOrder` y con las etiquetas ES/IT que sembró la migración.
    - `PATCH /api/catalogs/concepts/DC` con `{ labelEs: 'Jornada completa' }`: Super Admin → 200 y el `GET` siguiente lo refleja; Admin → 403; Técnico → 403.
    - `PATCH /api/catalogs/concepts/XX` (código que no es del enum) → 400, no 500.
    - `POST /api/catalogs/concepts` y `DELETE /api/catalogs/concepts/DC` → **404** (los endpoints no existen).
    - `POST /api/catalogs/role-types { name }`: Super Admin → 201; Admin → 403. Nombre duplicado de un rol **desactivado** → 409 con código `YA_EXISTE_INACTIVO` (la UI ofrecerá reactivar), no un 500 de constraint.
    - `PATCH /api/catalogs/role-types/:id { isActive: false }` → 200; el rol sigue apareciendo en `GET /api/catalogs` con `isActive: false` (los selectores filtran en el cliente; el listado no oculta).
    - `POST /api/catalogs/currencies { code: 'usd' }` → se normaliza a `USD`; `code` de 2 o 4 letras → 400.
    - `POST /api/catalogs/machine-models { code, description }` → 201; `code` duplicado → 409.
    - No existe `DELETE` en ninguna ruta de `/api/catalogs` → 404.
  </behavior>
  <action>
Un módulo, un controlador, un servicio (los 4 catálogos son CRUD trivial: partirlos en 4 servicios sería ceremonia).

- `@Controller('api/catalogs')` **sin** `@Roles` a nivel de clase, porque el GET es T·A·S y el resto es S. `@Roles('T','A','S')` en el GET y `@Roles('S')` en cada mutación.
- `GET /api/catalogs` = 4 `findMany` con `select` explícito, en paralelo con `Promise.all`. Corre dentro de la tx del `RlsInterceptor`, así que usa `this.prisma.client`.
- El PATCH de concepto valida `:code` contra los valores del enum importado de `generated/prisma/enums` (no una lista de strings copiada a mano) y acepta **solo** `labelEs` y `labelIt`: cualquier otra clave del body se ignora en silencio, y `code` en el body se rechaza con 400.
- Duplicados: capturar `P2002` de Prisma y traducirlo a `ConflictException`. Si la fila existente está inactiva, el código de error es `YA_EXISTE_INACTIVO` y la respuesta incluye su `id`/`code` para que la UI ofrezca reactivar. Es más honesto que un índice parcial y evita duplicados fantasma (`@unique` normal: desactivar NO libera el nombre).
- Registrar `CatalogsModule` en `src/app.module.ts` (junto a `UsersModule`).

Suite `test/catalogs.e2e-spec.ts` con `createTestApp()` + `crearUsuario()` + `signTestToken` (3 líneas por caso, patrón de `users-roles.e2e-spec.ts`). RED primero: escribir la suite completa, verla fallar, e implementar.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- catalogs</automated>
  </verify>
  <done>Todos los comportamientos de `<behavior>` pasan; `npm -w backend run build` compila; ninguna ruta `DELETE` responde algo distinto de 404.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (TDD): módulo technicians — alta sin cuenta Entra y baja que conserva la historia</name>
  <files>fava-control-tecnico/backend/src/modules/technicians/technicians.module.ts, fava-control-tecnico/backend/src/modules/technicians/technicians.controller.ts, fava-control-tecnico/backend/src/modules/technicians/technicians.service.ts, fava-control-tecnico/backend/src/app.module.ts, fava-control-tecnico/backend/test/technicians.e2e-spec.ts</files>
  <behavior>
    - `POST /api/technicians { fullName, roleTypeId, employmentType: 'EXTERNO' }` con token de Admin → 201, y el técnico existe **sin ninguna fila en `users`** (CAT-02: técnicos históricos sin cuenta Entra).
    - `GET /api/technicians` devuelve activos **e inactivos** (el listado no filtra; filtran los selectores), ordenado por `fullName`, con `roleTypeName` resuelto y `userId` cuando hay usuario vinculado.
    - `POST` con `roleTypeId` inexistente → 400 o 404 con código propio, nunca un 500 de FK.
    - `POST` con `employmentType: 'FREELANCE'` → 400 (el enum no admite texto libre).
    - `PATCH /api/technicians/:id { fullName, roleTypeId, employmentType }` → 200.
    - `PATCH /api/technicians/:id/active { isActive: false }` → 200; el técnico sigue en la lista marcado inactivo y sus `daily_entries` siguen siendo legibles (el test siembra una jornada con `crearJornadaAprobada` y la relee después de la baja).
    - `DELETE /api/technicians/:id` → 404: no existe. Desactivar, nunca borrar.
    - Técnico (rol T) sobre cualquier ruta de `/api/technicians` → 403.
  </behavior>
  <action>
`@Controller('api/technicians')` con `@Roles('A','S')` a nivel de clase (entrar es cosa de A y S; no hay reglas condicionales que justifiquen bajarlo al servicio, a diferencia de `users`).

- `GET` con `include`/`select` que resuelva `roleType.name` → `roleTypeName` plano en la respuesta, y el `userId` del usuario vinculado (relación inversa `user`), para que la pantalla Técnicos sepa cuál tiene cuenta.
- `aliases` NO se expone ni se edita en esta fase: la columna existe para la Fase 6 (MIG-01) y nadie la escribe todavía. No inventar UI ni endpoint para ella.
- Validación de body a mano: `fullName` no vacío tras `trim`, `roleTypeId` UUID válido (`ParseUUIDPipe` en el path, comprobación explícita en el body), `employmentType` ∈ valores del enum importado.
- FK inexistente: capturar `P2003` y responder `BadRequestException('ROL_TECNICO_INEXISTENTE')`.
- Registrar `TechniciansModule` en `src/app.module.ts`.

RED primero: suite completa en rojo, después el módulo.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- technicians</automated>
  </verify>
  <done>Todos los comportamientos de `<behavior>` pasan; la jornada sembrada sigue legible tras desactivar al técnico; no hay ninguna ruta DELETE.</done>
</task>

</tasks>

<verification>
```bash
cd fava-control-tecnico
npm -w backend run build
npm -w backend run test:e2e -- "catalogs|technicians"
npm -w backend run test:e2e            # sin regresiones en las suites de Fase 1 ni en rls-maestros
```
</verification>

<success_criteria>
- `GET /api/catalogs` responde a los tres roles con las 4 listas y el contrato exacto declarado arriba.
- Los conceptos solo se pueden **editar de etiqueta**; crear o borrar es 404 en la API y 42501 en el motor (02-01).
- Roles, monedas y modelos tienen ABM de Super Admin con desactivación, nunca borrado.
- Técnicos: alta sin Entra, baja no destructiva, sin DELETE.
- Cero dependencias nuevas.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-maestros-y-cat-logos/02-03-SUMMARY.md` con el contrato JSON definitivo de `/api/catalogs` y `/api/technicians` (lo consume 02-06 al escribir los tipos del cliente).
</output>
