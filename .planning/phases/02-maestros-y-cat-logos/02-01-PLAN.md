---
phase: 02-maestros-y-cat-logos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fava-control-tecnico/backend/prisma/schema.prisma
  - fava-control-tecnico/backend/prisma/migrations/
  - fava-control-tecnico/backend/prisma/seed.ts
  - fava-control-tecnico/backend/test/helpers/db.ts
  - fava-control-tecnico/backend/test/helpers/fixtures.ts
  - fava-control-tecnico/backend/test/rls-maestros.e2e-spec.ts
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05]
must_haves:
  truths:
    - "Las 8 tablas nuevas existen tras `prisma migrate deploy` en una base limpia, con sus FKs y sus @@unique"
    - "Los 8 conceptos (DC MD DFD DVSF DVRC LR NR IL) existen con etiqueta ES e IT sin correr `db:seed`: los siembra la migración"
    - "Un técnico (app.is_admin=off) LEE catálogos, proyectos y técnicos y recibe filas — no listas vacías"
    - "Un técnico no puede escribir en ninguna de las 8 tablas nuevas: 42501 o 0 filas afectadas"
    - "Insertar un 9º concepto o borrar uno como fava_app es imposible por motor (no hay política de INSERT ni de DELETE)"
    - "Las 5 suites e2e de la Fase 1 siguen verdes con los FKs nuevos sobre daily_entries y weekly_notes"
  artifacts:
    - path: "fava-control-tecnico/backend/prisma/schema.prisma"
      provides: "8 modelos nuevos, 3 enums, 5 columnas y 4 FKs sobre lo existente"
      contains: "model ProjectSoldDays"
    - path: "fava-control-tecnico/backend/prisma/migrations/"
      provides: "migración Prisma del esquema + migración SQL a mano de RLS con siembra de conceptos"
      contains: "FORCE ROW LEVEL SECURITY"
    - path: "fava-control-tecnico/backend/test/helpers/db.ts"
      provides: "dos listas (transaccionales vs catálogos) y baseline con TEC_A/TEC_B como técnicos reales"
      contains: "CATALOGOS"
    - path: "fava-control-tecnico/backend/test/helpers/fixtures.ts"
      provides: "crearTecnico / crearProyecto / crearJornadaAprobada para las waves 2-3"
      min_lines: 30
    - path: "fava-control-tecnico/backend/test/rls-maestros.e2e-spec.ts"
      provides: "prueba de las políticas sobre las 8 tablas + control anti-mentira"
      min_lines: 80
  key_links:
    - from: "fava-control-tecnico/backend/prisma/schema.prisma"
      to: "technicians.id"
      via: "FK desde daily_entries.technician_id, weekly_notes.technician_id y users.technician_id"
      pattern: "references: \\[id\\]"
    - from: "fava-control-tecnico/backend/test/helpers/db.ts"
      to: "technicians"
      via: "truncateAll() repone TEC_A/TEC_B como filas reales tras truncar"
      pattern: "TEC_A"
    - from: "fava-control-tecnico/backend/test/rls-maestros.e2e-spec.ts"
      to: "pg_class"
      via: "aserción relrowsecurity + relforcerowsecurity sobre las 8 tablas"
      pattern: "relforcerowsecurity"
---

<objective>
Cerrar el esquema COMPLETO de la fase en una sola migración de Prisma + una migración SQL de RLS, antes de escribir un solo controlador. Incluye las 5 columnas nuevas de `daily_entries` y los FKs que hoy faltan, y repara en el mismo commit las dos trampas de test que esos FKs activan.

Purpose: con el esquema cerrado, los módulos (waves 2-3) y el cutover de pantallas (waves 3-4) son mecánicos y paralelizables. Con el esquema abierto, cada pantalla negocia su propio contrato y la Fase 6 migra dos veces.
Output: 8 tablas nuevas + 3 enums + RLS con políticas + los 8 conceptos sembrados por migración + helpers de test que no arrastran estado.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-maestros-y-cat-logos/02-RESEARCH.md
@fava-control-tecnico/backend/prisma/schema.prisma
@fava-control-tecnico/backend/prisma/migrations/20260725221504_rls/migration.sql
@fava-control-tecnico/backend/test/helpers/db.ts

Regla de repo: los commits NO llevan Co-Authored-By ni ninguna atribución a Claude.
Cero dependencias nuevas en toda la Fase 2. Nada de `class-validator`, `zod` en DTOs ni preview features de Prisma.
</context>

<interfaces>
<!-- Lo que ya existe. Usar directamente, sin explorar el repo. -->

```ts
// backend/src/common/prisma/prisma.service.ts
export const als: AsyncLocalStorage<TxClient>;
class PrismaService { get base(): PrismaClient; get client(): TxClient; }
// pool max 10 · transactionOptions { timeout: 10_000, maxWait: 5_000 }

// backend/test/helpers/db.ts (LO QUE VAS A MODIFICAR)
export const TEC_A = '11111111-1111-4111-8111-111111111111';
export const TEC_B = '22222222-2222-4222-8222-222222222222';
export const ownerClient: PrismaClient;  // MIGRATE_DATABASE_URL (owner, salta RLS)
export const appClient: PrismaClient;    // DATABASE_URL (fava_app, NOBYPASSRLS)
export async function truncateAll(): Promise<void>;
export async function disconnectAll(): Promise<void>;

// backend/test/helpers/app.ts
export async function createTestApp(tenantId?: string): Promise<INestApplication>;
export function crearUsuario(d: {email; displayName?; entraOid?; roles?; isActive?; technicianId?}): Promise<User>;
```

Contexto RLS = exactamente 3 GUCs fijadas por `RlsInterceptor` con `set_config(..., TRUE)`:
`app.user_id`, `app.technician_id`, `app.is_admin` (`'on' | 'off'`). **No añadir una cuarta.**

Comandos de base (Postgres 17 local, puerto 55432):
`npm -w backend run db:migrate` · `npm -w backend run db:seed` · `npm -w backend run test:e2e -- <filtro>`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Esquema completo en una migración + reparación de los helpers que los FKs rompen</name>
  <files>fava-control-tecnico/backend/prisma/schema.prisma, fava-control-tecnico/backend/prisma/migrations/, fava-control-tecnico/backend/test/helpers/db.ts</files>
  <action>
Añadir a `backend/prisma/schema.prisma` (respetando el estilo actual: `@@map` snake_case, comentarios `///` que explican el porqué):

**3 enums nuevos**
```prisma
/// Los 8 codigos son FIJOS (decision bloqueada del CONTEXT). Enum de Postgres =
/// el catalogo no puede crecer ni por bug ni por SQL suelto. Las etiquetas
/// visibles (mutables) viven en la tabla concepts.
enum ConceptCode { DC MD DFD DVSF DVRC LR NR IL  @@map("concept_code") }
enum Phase       { MONTAJE COLLAUDO              @@map("phase") }
enum EmploymentType { INTERNO EXTERNO            @@map("employment_type") }
```

**8 modelos nuevos** — copiar la § Data Model de `02-RESEARCH.md` literalmente (`Concept`, `RoleType`, `Currency`, `MachineModel`, `Technician`, `Project`, `ProjectMachine`, `ProjectSoldDays`) y añadirles los campos de relación que Prisma exige en el lado inverso.

Tres precisiones sobre el research que hay que aplicar:
1. **`daily_entries.concept_code` NO lleva relación a `concepts`.** El tipo enum ya lo constriñe; una FK además obligaría a leer `concepts` en cada escritura de bitácora sin ganar ninguna garantía. Declarar solo `conceptCode ConceptCode?  @map("concept_code")`. `Concept` queda sin relaciones.
2. **`onDelete: Restrict` EXPLÍCITO en todas las FKs nuevas.** Prisma pone `SetNull` por defecto en las relaciones opcionales: un borrado accidental de proyecto vaciaría en silencio el `project_id` de la bitácora. Nada de `Cascade` en ningún sitio.
3. **`weekly_notes.technician_id` también recibe su FK** (el research solo nombra `daily_entries` y `users`). Es la misma columna suelta, cuesta dos líneas y las suites que la usan siembran ya TEC_A/TEC_B. Cerrar el esquema significa cerrarlo entero.

**Sobre lo existente:**
- `DailyEntry` += `projectId String? @map("project_id") @db.Uuid` (+relación), `machineModelId String? @map("machine_model_id") @db.Uuid` (+relación), `conceptCode ConceptCode? @map("concept_code")`, `phase Phase?`, `roleTypeId String? @map("role_type_id") @db.Uuid` (+relación); y `technicianId` pasa a ser FK real a `Technician`.
  - Comentario obligatorio en `phase`: nullable a propósito — las hojas diarias del Excel no tienen columna de fase y TODO el histórico entra con `NULL`; la agregación necesita un bucket «sin fase».
  - Comentario obligatorio en `roleTypeId`: 5 de los 14 técnicos del Excel tienen más de un `Tipo` (Ivan Cortés: Software 652 filas + Capo Elettricista 78). Sin esta columna la Fase 6 pierde información.
- `User.technicianId` pasa a `@unique` + FK a `Technician` (un usuario ↔ un técnico; es de donde sale la GUC `app.technician_id`).
- `Project.clientNit` lleva este comentario, literal, junto a la columna:
  `/// CAT-03. OJO Fase 5: NO es el «NIT:» que imprime la Nota — ese es el de FAVA (901137532-4), constante del membrete. Esta columna NUNCA va en esa casilla del PDF.`

Generar la migración: `cd fava-control-tecnico && npx prisma migrate dev --name maestros --schema backend/prisma/schema.prisma` (ver `backend/prisma.config.ts`; si el flag de schema estorba, ejecutar desde `backend/`). Revisar el SQL generado antes de darlo por bueno.

**En el MISMO commit** (Pitfall 3: sin esto las suites verdes de la Fase 1 se ponen rojas con un error de FK y parecerá que la migración rompió RLS), reescribir `backend/test/helpers/db.ts`:
- Dos listas: `TABLAS_TX` = `daily_entries, weekly_notes, project_sold_days, project_machines, projects, technicians, access_requests, users` (se truncan) y `CATALOGOS` = `concepts, role_types, currencies, machine_models` (**nunca** se truncan: los conceptos los sembró la migración y un `TRUNCATE ... CASCADE` se los llevaría).
- `truncateAll()` mantiene nombre y firma —las 3 suites de la Fase 1 la llaman en `beforeEach` y no se tocan— pero deja la base en un **baseline conocido**: trunca `TABLAS_TX`, garantiza los catálogos con `seedCatalogos()` y repone `TEC_A`/`TEC_B` como filas reales de `technicians`. Comentario `// ponytail:` explicando que conserva el nombre para no tocar 3 suites que no cambian de intención.
- `export async function seedCatalogos()` idempotente (upsert de un `RoleType` de prueba, una `Currency` y un `MachineModel` con códigos fijos exportados como constantes: `ROL_TEST`, `CUR_TEST`, `MAQ_TEST`).

Verificar en verde las 5 suites de Fase 1 **sin editarlas**. Si alguna necesita cambios, es señal de que el baseline está mal: arreglar el helper, no la suite.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run db:migrate && npm -w backend run build && npm -w backend run test:e2e</automated>
  </verify>
  <done>`prisma migrate deploy` aplica en base limpia; `npm -w backend run build` (incluye `prisma generate`) compila; las 5 suites de Fase 1 (bootstrap, rls-isolation, rls-transaction, auth, tenant-swap, users-roles, dev-auth) pasan sin un solo cambio en sus archivos.</done>
</task>

<task type="auto">
  <name>Task 2: Migración SQL de RLS para las 8 tablas + siembra estructural de los 8 conceptos</name>
  <files>fava-control-tecnico/backend/prisma/migrations/, fava-control-tecnico/backend/prisma/seed.ts, fava-control-tecnico/backend/test/helpers/fixtures.ts</files>
  <action>
Crear a mano `backend/prisma/migrations/<timestamp>_rls_maestros/migration.sql` (timestamp POSTERIOR al de la migración de la Task 1), con el mismo estilo idempotente que `20260725221504_rls/migration.sql` — `DROP POLICY IF EXISTS` antes de cada `CREATE POLICY`, porque `migrate dev` la corre también contra la shadow database. Prisma no genera ni preserva políticas: por eso va a mano y versionada.

**Patrón para las 7 tablas de maestros** (`role_types`, `currencies`, `machine_models`, `technicians`, `projects`, `project_machines`, `project_sold_days`):
```sql
ALTER TABLE "projects" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "projects" FORCE  ROW LEVEL SECURITY;
DROP POLICY IF EXISTS proj_read  ON "projects";
DROP POLICY IF EXISTS proj_write ON "projects";
-- Sin ESTA politica la tabla no da error: da CERO filas, y el bug parece del frontend.
CREATE POLICY proj_read  ON "projects" FOR SELECT TO fava_app USING (TRUE);
-- FOR ALL reutiliza USING como WITH CHECK: cubre INSERT, UPDATE y DELETE.
CREATE POLICY proj_write ON "projects" FOR ALL    TO fava_app
  USING (current_setting('app.is_admin', TRUE) = 'on');
```
Lectura abierta a propósito: en la Fase 3 el técnico necesita leer proyectos, máquinas y conceptos para registrar su día. El aislamiento que exige AUTH-03 es sobre **registros**, no sobre catálogos.

**`concepts` es la excepción**: `FOR SELECT USING (TRUE)` + `FOR UPDATE` con `USING` y `WITH CHECK` sobre `app.is_admin`. Al **no existir** política de INSERT ni de DELETE, el default-deny de Postgres las bloquea a nivel de motor: los 8 códigos son fijos por motor, no por convención.

**No** añadir una GUC `app.is_super`: tocaría `rls.interceptor.ts` (archivo compartido) para una garantía que `@Roles('S')` ya da en la capa de servicio.

En la misma migración, la siembra estructural de los 8 conceptos con `INSERT ... ON CONFLICT ("code") DO NOTHING` (idempotente: shadow DB, CI y re-deploy). Textos ES/IT exactos de `frontend/src/i18n.ts:10-19`, `sort_order` 1..8 en ese orden. Van en la MIGRACIÓN y no en `seed.ts` porque son estructura: si un deploy olvida `db:seed`, el catálogo vacío rompe la captura de la Fase 3 y la pantalla Config.

Ampliar `backend/prisma/seed.ts` (sin tocar el upsert del Super Admin) con los catálogos de arranque que **sí** son ABM del usuario, tomando los valores de los mocks que esta fase retira, para que la app no arranque vacía:
- `role_types`: Mecánico, Meccatronico, Eléctrico, Software (upsert por `name`)
- `currencies`: USD $, EUR €, CLP $, ARS $, DOP RD$ (upsert por `code`)
- `machine_models`: CTA1000, PC4500, PL6000 (upsert por `code`, `description` null)

Crear `backend/test/helpers/fixtures.ts` — es el contrato de fixtures que consumen las waves 2 y 3, **ningún plan posterior lo modifica** (si a un spec le falta un fixture, se lo define en su propio archivo):
```ts
export function crearTecnico(d?: Partial<{fullName; roleTypeId; employmentType; isActive}>): Promise<Technician>;
export function crearProyecto(d?: Partial<{name; clientName; locality; country; supply; contractNumber; currencyCode}>): Promise<Project>;
export function crearJornadaAprobada(d: {technicianId; projectId?; roleTypeId?; phase?; date}): Promise<DailyEntry>;
```
Todos siembran con `ownerClient` (salta RLS: los fixtures no son el sujeto del test) y rellenan los obligatorios con valores por defecto sensatos.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run db:migrate && npm -w backend run db:seed && npm -w backend run test:e2e -- bootstrap</automated>
  </verify>
  <done>`migrate deploy` sobre base limpia deja las 8 tablas con RLS y los 8 conceptos ya presentes SIN correr `db:seed`; re-aplicar el `.sql` con `psql -v ON_ERROR_STOP=1` no da error (idempotencia real, no la que finge `migrate deploy` al no reejecutar); `db:seed` deja roles, monedas y modelos de arranque; `fixtures.ts` exporta las 3 funciones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (TDD): rls-maestros.e2e-spec.ts — leer todos, escribir admin, y el enum que no puede derivar</name>
  <files>fava-control-tecnico/backend/test/rls-maestros.e2e-spec.ts</files>
  <behavior>
    - Conectado como `fava_app` con `app.is_admin='off'` (técnico): SELECT sobre `concepts`, `role_types`, `currencies`, `machine_models`, `technicians`, `projects`, `project_machines`, `project_sold_days` devuelve las filas sembradas — **conteo igual al que ve el owner**, no > 0 (una lista vacía es el fallo silencioso del Pitfall 1).
    - Técnico (`is_admin='off'`): INSERT en cada una de las 8 tablas → `42501`; UPDATE y DELETE → 0 filas afectadas o `42501`. Afirmar el **SQLSTATE**, nunca el texto (el cluster traduce los mensajes).
    - Admin (`is_admin='on'`, `technician_id=''`): INSERT/UPDATE/DELETE en las 7 tablas de maestros funcionan. El caso `technician_id=''` es el que revienta si alguien copia un `::uuid` sin `NULLIF`.
    - Admin sobre `concepts`: UPDATE de `label_es`/`label_it` → OK. INSERT de un 9º concepto → `42501`. DELETE de un concepto → `42501`. **Ni siquiera el admin puede**: no hay política que lo permita.
    - Anti-mentira: `pg_class.relrowsecurity` y `relforcerowsecurity` = `true` en las 8 tablas nuevas, y `current_user = 'fava_app'` dentro de la transacción de prueba.
  </behavior>
  <action>
Replicar la forma de `test/rls-isolation.e2e-spec.ts`: helpers locales `comoTecnico(fn)` / `comoAdmin(fn)` de 6 líneas que abren `appClient.$transaction` y hacen el mismo `set_config(..., TRUE)` de las 3 GUCs que hace el interceptor. Siembra con `ownerClient` + los fixtures de la Task 2.

Antes de dar la suite por buena, **verificarla en rojo**: `ALTER TABLE projects DISABLE ROW LEVEL SECURITY` debe tumbar los casos de escritura, y borrar la política `proj_read` debe tumbar los de lectura. Dejar constancia del resultado en el summary del plan (es el patrón anti-mentira que dejó montado 01-02).
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- rls-maestros</automated>
  </verify>
  <done>La suite pasa conectada como `fava_app`; verificada en rojo apagando RLS en una tabla y quitando una política de SELECT; el bloque anti-mentira cubre las 8 tablas.</done>
</task>

</tasks>

<verification>
```bash
cd fava-control-tecnico
npm -w backend run db:migrate      # aplica las 2 migraciones sobre base limpia
npm -w backend run db:seed
npm -w backend run build
npm -w backend run test:e2e        # 5 suites de Fase 1 + rls-maestros, todo verde
```
Comprobación manual una sola vez: `psql -v ON_ERROR_STOP=1 -f <la migración de RLS>` dos veces seguidas sin error.
</verification>

<success_criteria>
- 8 tablas nuevas + 3 enums + 5 columnas y 4 FKs sobre lo existente, en **dos** migraciones (una generada, una a mano).
- Los 8 conceptos existen sin `db:seed`.
- `relforcerowsecurity = true` en las 8 tablas; `concepts` sin política de INSERT ni de DELETE.
- Las suites de la Fase 1 siguen verdes **sin editarlas**.
- `test/helpers/{db,fixtures}.ts` listos para que las waves 2-3 arranquen en paralelo.
- Cero dependencias nuevas.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-maestros-y-cat-logos/02-01-SUMMARY.md`.
Dejar anotado explícitamente para la Fase 5: el `NIT:` del encabezado de la Nota es el de FAVA, no `projects.client_nit`.
</output>
