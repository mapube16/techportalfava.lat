---
phase: 02-maestros-y-cat-logos
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - fava-control-tecnico/backend/src/modules/projects/
  - fava-control-tecnico/backend/src/app.module.ts
  - fava-control-tecnico/backend/scripts/smoke.ts
  - fava-control-tecnico/backend/test/projects.e2e-spec.ts
  - fava-control-tecnico/backend/test/sold-days.e2e-spec.ts
autonomous: true
requirements: [CAT-03, CAT-04]
must_haves:
  truths:
    - "Un admin crea un proyecto con cliente, NIT, localidad, suministro, n° de contrato, país y máquinas, y los recupera idénticos en el detalle"
    - "contractValue viaja en el JSON como number, no como string: money() no imprime NaN"
    - "La matriz de días vendidos tiene una fila por rol técnico del catálogo — más los roles inactivos que ya tengan vendido o ejecutado — nunca una lista cableada"
    - "El delta es sold − executed, calculado en el servidor: pasarse de lo vendido sale NEGATIVO"
    - "Un body de días vendidos que traiga delta o executed se rechaza con 400, y no existe ninguna columna delta en la base"
    - "Un PUT de celda con el mismo valor no escribe: updated_at no se mueve (no envenenar el audit_log que llega en Fase 4)"
    - "Las jornadas con phase NULL aparecen en un bucket «sin fase» de la agregación en vez de desaparecer"
    - "Quitar una máquina de un proyecto con jornadas registradas funciona y las jornadas conservan su modelo"
  artifacts:
    - path: "fava-control-tecnico/backend/src/modules/projects/projects.controller.ts"
      provides: "CRUD de proyecto + máquinas + días vendidos como 3 recursos separados"
      contains: "api/projects"
    - path: "fava-control-tecnico/backend/src/modules/projects/sold-days.service.ts"
      provides: "upsert por celda y agregación de ejecutados con el delta del lado del servidor"
      contains: "upsert"
    - path: "fava-control-tecnico/backend/test/sold-days.e2e-spec.ts"
      provides: "CAT-04 probado: delta, negativos, idempotencia, bucket sin fase, rechazo de delta"
      min_lines: 80
    - path: "fava-control-tecnico/backend/test/projects.e2e-spec.ts"
      provides: "CAT-03 probado: 7 campos del encabezado, Decimal como number, máquinas"
      min_lines: 70
  key_links:
    - from: "fava-control-tecnico/backend/src/modules/projects/projects.service.ts"
      to: "daily_entries"
      via: "agregación de ejecutados con COALESCE(de.role_type_id, t.role_type_id) y status approved"
      pattern: "COALESCE"
    - from: "fava-control-tecnico/backend/src/modules/projects/projects.controller.ts"
      to: "role_types"
      via: "las filas de la matriz salen del catálogo, no de una constante"
      pattern: "roleType"
    - from: "fava-control-tecnico/backend/scripts/smoke.ts"
      to: "GET /api/projects"
      via: "check post-deploy autenticado (Pitfall 7: privilegios por defecto de las tablas nuevas)"
      pattern: "api/projects"
---

<objective>
El módulo de proyectos: el encabezado literal de la Nota Semanal, la selección de máquinas y la matriz vendido/ejecutado/delta por rol×fase. Es el corazón de la fase y el que fija cómo se calcula el delta para siempre.

Purpose: CAT-03 y CAT-04.
Output: 8 endpoints, dos suites e2e y el smoke ampliado que caza el fallo de privilegios de Railway.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-maestros-y-cat-logos/02-RESEARCH.md
@fava-control-tecnico/backend/src/modules/users/users.controller.ts
@fava-control-tecnico/backend/src/common/prisma/rls.interceptor.ts
@fava-control-tecnico/backend/scripts/smoke.ts
@fava-control-tecnico/backend/test/helpers/fixtures.ts

Regla de repo: los commits NO llevan Co-Authored-By ni ninguna atribución a Claude.
Cero dependencias nuevas. Validación de body a mano + `ParseUUIDPipe` (precedente 01-03).
`src/app.module.ts` es propiedad de este plan en la wave 3.
No modificar `test/helpers/*` (propiedad de 02-01).
</context>

<interfaces>
**Superficie de API**
| Método | Ruta | Rol | Notas |
|---|---|:--:|---|
| GET | `/api/projects` | A S | lista + códigos de máquina para los chips |
| GET | `/api/projects/:id` | A S | detalle + máquinas (con `entryCount`) + matriz |
| POST · PATCH | `/api/projects` · `/:id` | A S | encabezado + comercial. **Ni máquinas ni días vendidos entran aquí** |
| PATCH | `/api/projects/:id/active` | A S | desactivar, nunca borrar |
| PUT | `/api/projects/:id/machines` | A S | reemplaza la selección completa (idempotente) |
| PUT | `/api/projects/:id/sold-days` | A S | **una celda**: `{ roleTypeId, phase, soldDays }` |

Tres recursos, tres endpoints: el autoguardado por celda necesita un endpoint pequeño y aislado. Un `PATCH /api/projects/:id` genérico que además tocase máquinas o días vendidos es anti-patrón declarado.

**Contrato exacto** (lo consume `frontend/src/lib/api/projects.ts` en 02-06 — cambiar un nombre aquí es una pantalla en blanco):
```ts
// GET /api/projects  →  ProjectListItem[]
{ id; name; clientName; country; oaNumber: string|null; contractNumber;
  contractValue: number|null; currencyCode: string|null; normalHours: number|null;
  isActive: boolean; machineCodes: string[] }

// GET /api/projects/:id  →  ProjectDetail
{ id; name;
  clientName; clientNit: string|null; locality; country; supply; contractNumber;   // encabezado de la Nota
  oaNumber: string|null; contractValue: number|null; currencyCode: string|null; normalHours: number|null;
  isActive: boolean;
  machines: { machineModelId: string; code: string; description: string|null; entryCount: number }[];
  matrix: { roleTypeId: string; roleTypeName: string; roleTypeActive: boolean;
            phase: 'MONTAJE'|'COLLAUDO'|null;      // null = bucket «sin fase» (histórico del Excel)
            sold: number; executed: number; delta: number }[] }
```
Forma **plana**, nunca `Record<Phase, Record<RoleType, number>>`: las filas son datos, no claves conocidas en compilación.

```ts
// Ya existe y se usa tal cual
this.prisma.client                      // dentro de la tx del RlsInterceptor
@Roles('A','S')  ·  @CurrentUser() actor: UserModel
import type { Phase } from '../../generated/prisma/enums';   // MONTAJE | COLLAUDO
// test/helpers/fixtures.ts (02-01): crearTecnico, crearProyecto, crearJornadaAprobada
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (TDD): CRUD del proyecto con el encabezado literal de la Nota + Decimal como number + smoke ampliado</name>
  <files>fava-control-tecnico/backend/src/modules/projects/projects.module.ts, fava-control-tecnico/backend/src/modules/projects/projects.controller.ts, fava-control-tecnico/backend/src/modules/projects/projects.service.ts, fava-control-tecnico/backend/src/app.module.ts, fava-control-tecnico/backend/scripts/smoke.ts, fava-control-tecnico/backend/test/projects.e2e-spec.ts</files>
  <behavior>
    - `POST /api/projects` con los 7 campos del encabezado (`clientName`, `clientNit`, `locality`, `country`, `supply`, `contractNumber`, más `name`) + comercial → 201; `GET /api/projects/:id` los devuelve **idénticos** campo por campo (criterio 1 del roadmap).
    - `typeof body.contractValue === 'number'` en la respuesta de `GET /api/projects` y de `GET /api/projects/:id`; con `contractValue` nulo, `null` (no `"null"`, no `"0"`).
    - `locality` y `country` son dos campos independientes: crear con `locality: 'Santiago de los Caballeros'` y `country: 'República Dominicana'` los devuelve separados (KPI-04 agrupa por país; la Nota los imprime unidos).
    - `POST` sin `clientName` / `locality` / `supply` / `contractNumber` → 400 (son los que imprime la Nota; sin ellos el PDF sale mutilado).
    - `currencyCode` inexistente → 400/404 con código propio, nunca 500 de FK.
    - `PATCH /api/projects/:id` con `soldDays` o `machines` en el body → esos campos se ignoran (o 400): no es la vía.
    - `PATCH /api/projects/:id/active { isActive: false }` → 200; `GET /api/projects` lo sigue listando con `isActive: false`.
    - `DELETE /api/projects/:id` → 404.
    - Técnico (rol T) sobre cualquier ruta → 403.
  </behavior>
  <action>
Módulo `projects` con la estructura de `users`. `@Controller('api/projects')` + `@Roles('A','S')` a nivel de clase.

**Serialización de `Decimal` (Pitfall 5, confianza MEDIA en el research — verificar, no suponer):** antes de escribir el mapeo, lanzar el servidor y hacer un `curl` de un proyecto con `contract_value` para ver qué emite Nest realmente. Sea string o número, el controlador mapea **explícitamente**:
```ts
// Prisma representa @db.Decimal con Decimal de decimal.js. Los valores reales de
// contrato (~4,15 M con 2 decimales) estan muy por debajo de 2^53: Number es exacto.
contractValue: p.contractValue === null ? null : Number(p.contractValue),
```
y el test afirma `typeof === 'number'`. La conversión explícita hace que la respuesta sea la misma en los dos casos; el hallazgo del `curl` va al summary.

`createdById` se rellena con `actor.id` (metadato, sin FK declarada).

`machineCodes` de la lista sale de un `include` de `machines.machineModel.code`; no hacer N+1.

**Ampliar `backend/scripts/smoke.ts`** (Pitfall 7: `ALTER DEFAULT PRIVILEGES` es por rol, y ocho tablas nuevas lo estrenan — en Railway pueden nacer sin permisos para `fava_app` y la app responde `permission denied for table projects` justo después de un `migrate deploy` exitoso; en local no pasa porque es el mismo rol):
- Checks nuevos sin credenciales: `GET /api/catalogs` sin `Authorization` → 401 y `GET /api/projects` sin `Authorization` → 401 (prueban que la ruta existe y que ServeStatic no se la come).
- Check **autenticado opcional**: si existen `SMOKE_DEV_EMAIL` y `SMOKE_DEV_PASSWORD` en el entorno, hacer `POST /api/dev-auth/login`, y con el token pedir `GET /api/catalogs` y `GET /api/projects` esperando **200**. Sin esas variables, imprimir `↷ omitido (sin SMOKE_DEV_*)` y no contar como fallo. Es el único check que caza el fallo de privilegios; sin él, «todo verde en local y permission denied en Railway».
- Mantener el estilo del archivo: `fetch` nativo, una línea por check, `exit 1` si alguno falla.

RED primero: suite `projects.e2e-spec.ts` completa antes del módulo.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- projects</automated>
  </verify>
  <done>Los 9 comportamientos pasan; `npm -w backend run smoke` corre contra el build local con los checks nuevos; el hallazgo real sobre la serialización de `Decimal` queda escrito en el summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (TDD): PUT /api/projects/:id/machines — selección por proyecto con aviso informado</name>
  <files>fava-control-tecnico/backend/src/modules/projects/projects.service.ts, fava-control-tecnico/backend/src/modules/projects/projects.controller.ts, fava-control-tecnico/backend/test/projects.e2e-spec.ts</files>
  <behavior>
    - `PUT /api/projects/:id/machines { machineModelIds: [...] }` reemplaza la selección completa; repetir el mismo PUT deja el mismo estado (idempotente).
    - `GET /api/projects/:id` devuelve por máquina seleccionada su `entryCount`: número de `daily_entries` **de ese proyecto** con ese `machine_model_id`. En esta fase siempre será 0 salvo en el test, que siembra una jornada para probar el camino.
    - Quitar del PUT una máquina con `entryCount > 0` → 200; la `daily_entry` sembrada **conserva su `machine_model_id`** (apunta al modelo global, no a la fila de selección). Esta es la decisión bloqueada «se avisa y se permite»: el servidor permite, el aviso lo da la UI con el `entryCount`.
    - `machineModelIds` con un id inexistente → 400, y la selección previa no se toca (todo o nada, dentro de la transacción de la petición).
    - `machineModelIds: []` → 200 y proyecto sin máquinas (el mínimo de una máquina es validación de formulario, no de dominio).
  </behavior>
  <action>
`delete many` + `createMany` dentro de la misma transacción de la petición (ya la abre el `RlsInterceptor`; no abrir una anidada). `project_machines` es selección pura y nada la referencia: sus filas **sí** se borran de verdad — no es un maestro, la regla «desactivar nunca borrar» no aplica aquí.

`entryCount` con un `groupBy` sobre `daily_entries` filtrando por `projectId`, no con un `count` por máquina en bucle.

Comentario obligatorio junto al modelo: la jornada apunta a `machine_models` (global) y no a `project_machines`; si apuntara a la selección, quitar una máquina sería un FK roto o un borrado en cascada de bitácora.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- projects</automated>
  </verify>
  <done>Los 5 comportamientos pasan; la jornada sembrada sobrevive a la retirada de su máquina del proyecto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (TDD): matriz vendido/ejecutado/delta — filas del catálogo, upsert por celda, delta del servidor</name>
  <files>fava-control-tecnico/backend/src/modules/projects/sold-days.service.ts, fava-control-tecnico/backend/src/modules/projects/projects.controller.ts, fava-control-tecnico/backend/src/modules/projects/projects.service.ts, fava-control-tecnico/backend/test/sold-days.e2e-spec.ts</files>
  <behavior>
    - `GET /api/projects/:id` devuelve una fila de matriz por **cada `role_type` activo × cada fase** (`MONTAJE`, `COLLAUDO`), con `sold`, `executed` y `delta`. Crear un rol nuevo en el catálogo añade filas sin tocar código.
    - Un `role_type` **desactivado** que tiene `sold > 0` o `executed > 0` sigue apareciendo, con `roleTypeActive: false`. Si desapareciera, el total del proyecto cambiaría solo y el KPI se descuadraría en silencio.
    - `PUT /api/projects/:id/sold-days { roleTypeId, phase, soldDays: 10 }` → 200 y `GET` devuelve `sold: 10`.
    - Con `sold: 10` y **una** jornada aprobada de ese rol y fase: `executed: 1`, `delta: 9`. Con `sold: 0` y esa misma jornada: `delta: -1`. **El delta es `sold − executed`** — el prototipo (`ProjectDetail.tsx:35`) lo tiene invertido y aquí se fija la convención correcta, la del Excel (`Resoconto` fila 39: `20 | 332 | -312`).
    - Bajar `sold` por debajo de `executed` está **permitido**: nada de `CHECK (sold >= executed)`. El negativo es un hecho real del negocio.
    - Body con `delta` o `executed` → 400 `CAMPO_CALCULADO_NO_ADMITIDO`. Ninguna columna `delta` existe en la base (02-02 lo prueba por introspección).
    - `soldDays` negativo, no entero o > 9999 → 400.
    - Dos `PUT` seguidos con el **mismo** valor: solo la primera escribe. El test compara `updated_at` antes y después del segundo. Sin esto, cada blur sin edición mete una fila en el `audit_log` append-only que llega en la Fase 4.
    - Jornadas aprobadas con `phase = NULL` (todo el histórico del Excel) aparecen en una fila de matriz con `phase: null` y su `executed`, no se descartan.
    - Solo cuentan jornadas con `status = 'approved'` (coherente con KPI-01): una jornada en `draft` no mueve `executed`.
    - Jornada cuyo `daily_entries.role_type_id` es NULL: cuenta bajo el rol del maestro del técnico (`COALESCE`).
  </behavior>
  <action>
**Upsert de celda** — `sold-days.service.ts`, exactamente el Code Example 3 del research: comparar primero con `findUnique` sobre la clave natural `projectId_roleTypeId_phase` y **salir sin escribir si el valor no cambia**; si cambia, `upsert`. Una sola ida a la base en el camino de escritura, y cero escrituras en el de no-cambio.

`PUT` con el valor absoluto sobre la clave natural: reintentar es seguro y dos admins concurrentes convergen al último. Techo declarado en un comentario `// ponytail: último que escribe gana — correcto para 2 administradores; si algún día hay contención real, bloqueo optimista por updated_at`, no en una capa de bloqueo.

**Agregación de ejecutados** — una sola expresión SQL, el Code Example 5 del research, con sus tres comentarios al lado porque las tres son preguntas abiertas con FAVA:
```sql
SELECT COALESCE(de.role_type_id, t.role_type_id) AS role_type_id, de.phase, COUNT(*)::int AS days
  FROM daily_entries de JOIN technicians t ON t.id = de.technician_id
 WHERE de.project_id = $1::uuid AND de.status = 'approved'
 GROUP BY 1, 2
```
1. el rol de la jornada manda, el del maestro es respaldo (Ivan Cortés cuenta como `Software` unos días y como `Capo Elettricista` otros);
2. `COUNT(*)` cuenta `MD` como día completo — es lo que hace el Excel, así cuadra la conciliación de MIG-03; si FAVA pide medio día se cambia **aquí y solo aquí**;
3. `de.phase` puede ser NULL y **no** se descarta: descartarlo sería una mentira silenciosa en el tablero.

Corre con `this.prisma.client.$queryRaw` dentro de la tx de la petición.

**Composición de la matriz** en `projects.service.ts`: producto de (roles activos ∪ roles con dato) × (`MONTAJE`, `COLLAUDO`), más las filas `phase: null` que devuelva la agregación. `delta = sold − executed` calculado aquí, una sola vez, en el servidor. El cliente solo pinta.

RED primero: `sold-days.e2e-spec.ts` con los 11 comportamientos antes de la implementación. El caso del delta negativo y el de la idempotencia son los dos que más fácil se cuelan verdes por accidente: escribirlos con valores concretos.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- sold-days</automated>
  </verify>
  <done>Los 11 comportamientos pasan; el delta sale negativo al sobreejecutar; el segundo PUT idéntico no mueve `updated_at`; no existe ni un campo `delta` aceptado por la API.</done>
</task>

</tasks>

<verification>
```bash
cd fava-control-tecnico
npm -w backend run build
npm -w backend run test:e2e -- "projects|sold-days"
npm -w backend run test:e2e             # suite completa, sin regresiones
npm -w backend run smoke                # contra el build local
```
</verification>

<success_criteria>
- Los 7 campos del encabezado de la Nota se capturan y se recuperan idénticos.
- `contractValue` es un `number` en el JSON, con test.
- Las filas de la matriz salen del catálogo de roles; ninguna lista cableada en el código.
- `delta = sold − executed`, en el servidor, con el caso negativo probado.
- Ningún endpoint acepta `delta`; ninguna columna `delta` existe.
- El smoke caza el fallo de privilegios de Railway antes que un usuario.
- Cero dependencias nuevas.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-maestros-y-cat-logos/02-05-SUMMARY.md` con: el contrato JSON definitivo de `/api/projects` y `/api/projects/:id` (lo consume 02-07), y el hallazgo real sobre la serialización de `Decimal`.
</output>
