---
phase: 02-maestros-y-cat-logos
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - fava-control-tecnico/backend/test/no-free-text.e2e-spec.ts
  - fava-control-tecnico/scripts/check-no-free-text.mjs
  - fava-control-tecnico/package.json
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04]
must_haves:
  truths:
    - "«Sin texto libre» es demostrable por introspección, no por promesa: existen las FKs de technicians.role_type_id, projects.currency_code, project_sold_days.role_type_id y daily_entries.{project_id, machine_model_id, role_type_id}"
    - "daily_entries.concept_code es el tipo enum concept_code de Postgres, no varchar ni CHECK"
    - "No existe ninguna columna llamada delta (ni executed) en ninguna tabla del esquema"
    - "Ninguna de las 7 pantallas del cutover alimenta concepto, rol técnico o moneda desde un input de texto libre, y el script lo comprueba en cada build"
  artifacts:
    - path: "fava-control-tecnico/backend/test/no-free-text.e2e-spec.ts"
      provides: "prueba estructural del criterio 4 contra information_schema y pg_type"
      min_lines: 50
    - path: "fava-control-tecnico/scripts/check-no-free-text.mjs"
      provides: "guarda-raíl de repo sobre los 7 archivos del cutover (el frontend no tiene runner)"
      min_lines: 20
  key_links:
    - from: "fava-control-tecnico/scripts/check-no-free-text.mjs"
      to: "fava-control-tecnico/package.json"
      via: "script npm invocable en el phase gate"
      pattern: "check:no-free-text"
    - from: "fava-control-tecnico/backend/test/no-free-text.e2e-spec.ts"
      to: "information_schema"
      via: "consulta de constraints y tipos de columna"
      pattern: "information_schema"
---

<objective>
Convertir el criterio 4 del roadmap («concepto, rol y moneda solo se eligen de listas cerradas — ninguno acepta texto libre en ninguna pantalla») en dos comprobaciones automáticas: una contra el catálogo del sistema de Postgres y otra contra los archivos del frontend, que no tiene runner de tests.

Purpose: sin esto, «sin texto libre» es una afirmación del summary. Con esto, romperlo pone la suite en rojo.
Output: una suite e2e de introspección y un script de repo enganchado a npm.
</objective>

<execution_context>
@C:/Users/maxim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-maestros-y-cat-logos/02-RESEARCH.md
@fava-control-tecnico/backend/test/helpers/db.ts
@fava-control-tecnico/backend/test/rls-isolation.e2e-spec.ts

Regla de repo: los commits NO llevan Co-Authored-By ni ninguna atribución a Claude.
No modificar `test/helpers/db.ts` ni `test/helpers/fixtures.ts`: son propiedad de 02-01 y otros planes de esta wave los leen a la vez.
</context>

<interfaces>
```ts
// backend/test/helpers/db.ts (02-01)
export const ownerClient: PrismaClient;   // owner, para $queryRaw contra catálogos del sistema
export const appClient: PrismaClient;     // fava_app
export async function truncateAll(): Promise<void>;
export async function disconnectAll(): Promise<void>;
```

Tablas y columnas que la introspección debe encontrar (esquema cerrado por 02-01):
`technicians.role_type_id → role_types.id` · `projects.currency_code → currencies.code`
`project_sold_days.{project_id, role_type_id}` · `daily_entries.{project_id, machine_model_id, role_type_id}`
`daily_entries.concept_code` de tipo `USER-DEFINED` / `udt_name = 'concept_code'`
`daily_entries.phase` y `project_sold_days.phase` de tipo `phase`

Archivos del cutover que vigila el script (rutas relativas a `fava-control-tecnico/frontend/src/`):
`screens/Projects.tsx`, `screens/ProjectDetail.tsx`, `screens/Techs.tsx`, `screens/Users.tsx`,
`screens/Config.tsx`, `components/NewProjectModal.tsx`, `components/InviteUserModal.tsx`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (TDD): no-free-text.e2e-spec.ts — el criterio 4 por introspección</name>
  <files>fava-control-tecnico/backend/test/no-free-text.e2e-spec.ts</files>
  <behavior>
    - Para cada par (tabla, columna, tabla referenciada) de la lista de arriba existe una FOREIGN KEY en `information_schema.table_constraints` + `key_column_usage` + `constraint_column_usage`. Falla nombrando la columna concreta que no la tiene.
    - `information_schema.columns` dice que `daily_entries.concept_code` tiene `data_type = 'USER-DEFINED'` y `udt_name = 'concept_code'` (no `character varying`, no `text`).
    - Los 8 valores del enum `concept_code` en `pg_enum` son exactamente `DC, MD, DFD, DVSF, DVRC, LR, NR, IL` — ni uno más, ni uno menos, y el test los compara como conjunto ordenado.
    - `information_schema.columns` NO devuelve ninguna fila cuyo `column_name` sea `delta` o `executed` en el esquema `public`. El delta se calcula; no se guarda ni se digita (criterio 3).
    - `technicians.employment_type` es el enum `employment_type`, no texto.
  </behavior>
  <action>
Suite e2e sin app de Nest: solo `ownerClient.$queryRaw` contra `information_schema` y `pg_enum`. Sin `truncateAll()` (esta suite no escribe nada y truncar sería ruido para las que corren en paralelo).

Escribirla PRIMERO en rojo contra una aserción falsa deliberada (por ejemplo, exigir una FK que no existe) para comprobar que el mensaje de fallo dice **qué columna** falta; después ajustar a la realidad del esquema. Un test de introspección que solo sabe decir «expected true, received false» no ahorra un minuto de depuración.

Mensajes de fallo en el `expect`, no en un comentario: `expect(faltantes).toEqual([])` con `faltantes` como lista de strings `"technicians.role_type_id"`.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && npm -w backend run test:e2e -- no-free-text</automated>
  </verify>
  <done>La suite pasa contra el esquema de 02-01 y falla con un mensaje que nombra la columna si se le quita una FK.</done>
</task>

<task type="auto">
  <name>Task 2: check-no-free-text.mjs — el guarda-raíl del frontend sin runner</name>
  <files>fava-control-tecnico/scripts/check-no-free-text.mjs, fava-control-tecnico/package.json</files>
  <action>
Script de ~30 líneas, Node puro (sin dependencias), que lee los 7 archivos del cutover y falla con `exit 1` si encuentra cualquiera de estas cosas:

1. Una línea que contenga `<input` y, en la misma línea o en la etiqueta, un binding a `concept`, `roleType`, `role_type`, `currency` o `cur` (regex sobre el bloque de la etiqueta). Estos tres datos se eligen: `<select>`, botones tipo chip o `<datalist>` con opción cerrada.
2. Un `import` de `CONCEPTS` desde `../i18n` en cualquiera de los 7 archivos: las etiquetas de concepto vienen del API, no de la constante (el mapa código→color sí puede seguir viniendo de `i18n`, y por eso el chequeo mira el identificador `CONCEPTS`, no el módulo).
3. Un `import` de `MACHINES` o `CURRENCIES` desde `../data`: son los mocks que esta fase retira.

Salida: una línea por hallazgo con `archivo:línea` y el motivo, y un resumen `N/N archivos limpios`. Cero hallazgos = `exit 0`.

Registrar en `fava-control-tecnico/package.json` (raíz del workspace): `"check:no-free-text": "node scripts/check-no-free-text.mjs"`.

El script debe pasar **hoy** contra el frontend tal cual está (Config.tsx usa la constante `CONCEPTS`, así que ese caso saldrá en rojo — es correcto: 02-06 lo arregla). Si sale en rojo al crearlo, dejarlo documentado en el summary como deuda que cierran 02-06/02-07/02-08, y NO ablandar la regla para que pase. El script es el contrato; las pantallas se adaptan a él.
  </action>
  <verify>
    <automated>cd fava-control-tecnico && node scripts/check-no-free-text.mjs; test $? -le 1</automated>
  </verify>
  <done>`npm run check:no-free-text` existe y ejecuta; informa por archivo y línea; devuelve 0 cuando los 7 archivos están limpios (lo estarán tras la wave 4). Documentado en el summary qué hallazgos quedan abiertos y qué plan los cierra.</done>
</task>

</tasks>

<verification>
```bash
cd fava-control-tecnico
npm -w backend run test:e2e -- no-free-text
npm run check:no-free-text     # rojo esperado hasta la wave 4; verde en el phase gate
```
</verification>

<success_criteria>
- El criterio 4 tiene dos pruebas automáticas: motor de base de datos + archivos del frontend.
- La suite de introspección nombra la columna concreta cuando falla.
- El script queda enganchado a npm y es parte del phase gate.
- Cero dependencias nuevas.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-maestros-y-cat-logos/02-02-SUMMARY.md` con la lista de hallazgos abiertos del script y el plan que cierra cada uno.
</output>
